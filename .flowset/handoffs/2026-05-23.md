# 2026-05-23 — Sprint 017 M3 3/4 종결 (T14/T15/T16 머지 + T17 Sprint 018+ 위임)

## 메타

- **Sprint**: 017 M3 3/4 종결 — T14 (#235 `14f7fdd` Ollama provider spike) + T15 (#235 동반 spec only) + T16 (#236 `bc155f4` Ollama chat 통합). T17 (로컬 임베딩 통합) Sprint 018+ 위임 — Schema v06 결정 후.
- **누적 PR (Sprint 017)**: M0 5 + M1 5 + M2 5 + M3 2 = **17 PR + 본 docs**
- **단위 테스트**: 1643 → **1673** (+30, T14 +22 OllamaProvider / T15 spec only / T16 +8 chatProviderSelect)
- **KI 변동 0** (Phase 3 spike)
- **자기 검증 루프**: 46 → **48회차 SUCCESS** (T14 + T16)

## 1. T14 — Ollama provider adapter spike (PR #235 `14f7fdd`)

산출물 (+679 / 0 / 5 파일):
- 신규 `src/ai/providers/OllamaProvider.ts` (247) — ProviderAdapter 인터페이스 구현
  - `providerType='local'` + `supportsChat=true` + `supportsEmbed=false`
  - raw fetch + fetchImpl 주입 (codex Q1 권고 — npm 의존성 회피)
  - `/api/chat` (POST, non-streaming) + `/api/tags` validate
  - 디폴트 `llama3.2:3b`, available 4종 (llama3.1:8b / qwen2.5:7b / mistral:7b)
  - `responseFormat='json_object'` → `format='json'` 매핑 (Ollama JSON mode)
  - tokens prompt_eval_count / eval_count, cost=0 (로컬)
  - 404 → bad_request + `ollama pull` 안내 / 429 rate_limit / 5xx server_error / fetch throw network — ProviderError 코드 매핑 정합
- 수정 `src/ai/providers/index.ts` + `src/ai/index.ts` (hotfix) — Ollama export wiring
- 신규 `tests/unit/ai/OllamaProvider.test.ts` (307) — 22 회귀 (info / validate 3 / chat 정상 7 / chat 오류 8 / embed unsupported 1 + 상수 정합)

scope (codex 019e500b Q4/Q5):
- embed() — `ProviderError('unsupported')` throw. 768 dim (nomic-embed-text) vs vec_pages 1024 dim mismatch + semantic space 차이. T15 spec 종합 결정 위임.
- chatStream() — Ollama NDJSON 지원하나 본 spike scope 외 (Q8 권고).

codex 019e500b 사전 협의 — Q1~Q9 9 권고 모두 흡수.

codex 019e5017 dual review — NEEDS_CHANGES #1 `src/ai/index.ts` top-level barrel export 누락 → hotfix `01a60f4` 흡수. NOTABLE 8 정합 path.

evaluator: **Pass 10 / Partial 0 / Fail 0** → 머지 권고.

학습 #18 **24번째 확증**.

## 2. T15 — sentence-transformers spec (PR #235 동반)

산출물 (`.flowset/specs/sprint-017-local-llm-spike.md` +122):
- §3 3 옵션 매트릭스 비교 — Ollama nomic-embed-text / Python sidecar (sentence-transformers) / onnxruntime-node
- §3.2 권고 — 옵션 A (Ollama nomic-embed-text) 우선 통합 — dep 추가 0 + Python sidecar process 관리 부담 회피 + onnxruntime-node native binary 부담 회피
- §3.3 BLOCKER — embedding space 분리 정책. 해소 옵션 3종 (B1 embedding_space_id 컬럼 / B2 dimension 별 vec0 table / B3 Workspace.embedding_model)
- §3.3 권고 — **B3 + B2 조합** (Workspace.embedding_model 박고 dimension 별 vec0 table — 가장 격리 강력 + migration 단순)
- §4 G-020 정식화 권고 (Sprint 017 M5 T25 종합 evaluator 시점)
- §5 후속 작업 — T16 / T17 (Sprint 018+) / Schema v06 진입 조건

본 spec 박은 시점에 T17 (로컬 임베딩 통합) 자체는 **Sprint 018+ 위임** 권고.

## 3. T16 — Ollama chat 통합 (PR #236 `bc155f4`)

산출물 (+137 / -5 / 3 파일):
- 신규 `src/main/chatProviderSelect.ts` (36) — `selectChatProviderIds(allowedProviders, defaultProviderId)` pure helper
  - allowedProviders 명시 (non-empty) → 그대로 사용 (BYOK 정합, KI-003)
  - 미명시 또는 빈 배열 + `defaultProviderId='local'` → `['local']` 단일
  - 그 외 (codex / unknown string / null / undefined) → `['openai']` 디폴트 (KI-003 보존)
  - codex Q6: local 시 OpenAI fallback 금지 (사용자 명시 존중)
- 수정 `src/main/services.ts` (+42 / -5):
  - OllamaProvider import + `ensureLocalProvider()` 함수 신규 (credential 무관, idempotent Map.set)
  - `rebuildProvider` 안 'local' 분기 (ensureLocalProvider 위임)
  - `rebuildAllProviders` 후미 + `initServices` IPC 등록 전 박음
  - `registerChatIpc` candidates 로직을 selectChatProviderIds(userSettingStore.defaultProviderId) 로 교체 + ChatService 에 effective candidates 전달 (BLOCKING hotfix)
- 신규 `tests/unit/main/chatProviderSelect.test.ts` (59) — 8 회귀

codex 019e502d 사전 협의 — Q1~Q9 9 권고 모두 흡수.

codex 019e503a dual review — BLOCKING (ChatService BYOK 차단) + NEEDS_CHANGES 2 (wording / UI 위임) + NOTABLE 7 모두 본 PR `0df2ebc` hotfix 흡수 또는 정합 path:
- BLOCKING: `allowedProviders.includes(providerType)` 가 DEFAULT `['openai']` 받아 'local' 차단 → ChatService 에 effective candidates 전달 (helper 결과)
- NEEDS_CHANGES #1: chatProviderSelect 빈 배열 wording 정합 — "미명시" 동의 명시
- NEEDS_CHANGES #2: GeneralPanel UI 옵션 미박음 → codex Q4 합의 backend-only scope 정합, 후속 PR 위임

evaluator: **Pass 9 / Partial 0 / Fail 0** → 머지 권고.

학습 #18 **25번째 확증**.

## 4. T17 — 로컬 임베딩 통합 (Sprint 018+ 위임)

진입 조건 (spec §3.4):
- Schema v06 결정 (B3 Workspace.embedding_model + B2 dimension 별 vec0 table)
- G-014 dry-run + 자동 백업 마이그레이션
- 사용자 별도 승인 (외부 dependency 또는 Python sidecar 결정 시)

## 5. 검증 (T14 + T16 합산)

| 검증 | 결과 |
|---|---|
| `npm run lint` | PASS |
| `npm run typecheck` | PASS (node + web) |
| `npm test` | 1643 → **1673 (+30)** — 93/93 파일 |
| `npm run build` | PASS |
| CI 6/6 | T14 PR #235 PASS / T16 PR #236 PASS |

## 6. KI 변동 (Sprint 017 M3 T14/T15/T16 시점)

- **KI 변동 0** — Phase 3 spike (코드 신규 + 통합). 신규 KI 등록 안 함.
- T17 위임 시점 schema v06 결정 후 별도 KI 등록 가능 (Workspace.embedding_model 마이그레이션 위험).

## 7. 자기 검증 루프

**48회차 SUCCESS** (Sprint 015~017 누적 48 PR). Sprint 017 M3 3/4 (T14/T15/T16) 정합 closure.

## 8. Contract 정정

`.flowset/contracts/sprint-017.md` §2 M3 T16 명시 식별자 `UserSetting.providerPreference: 'ollama'` → 실제 박힘 `UserSetting.defaultProviderId: 'local'` (기존 필드 재활용 + ProviderType.'local' enum 정합). 본 docs PR 안에 정합 표기 박음.

## 9. 다음 진입점 (사용자 결정)

옵션 A — **Sprint 017 M4 진입**:
- T18 Markdown Export (워크스페이스 → Markdown 폴더 구조)
- T19 Notion Export (BYOK Notion API token + OS Keychain 위임)
- T20 R3-B UserLevelEstimator 실 학습 로직
- T21 워크스페이스 공유 설계 시안

옵션 B — **Sprint 017 종료 선언**:
- M0~M3 가 본격 성과 (M2 KI 4 closed + M3 spike + chat 통합)
- M4/M5 Sprint 018+ 위임
- Sprint 018 contract 시안 (Phase 3 종료 + MVP 진입 검토)

옵션 C — **T17 단독 진행**:
- Schema v06 결정 + G-014 dry-run 마이그레이션
- Workspace.embedding_model 추가 + vec_pages_768 별도 table
- IndexingService 분기 + OllamaProvider.embed 활성화

---

**자기 검증 루프 48회차 SUCCESS** (Sprint 015~017 누적 48 PR — 자기 검증 시스템 안정 운영).
