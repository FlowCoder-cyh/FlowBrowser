# 2026-05-22 — Sprint 017 M0 5/5 종결 + M1 4/4 종결 (T06/T07/T08/T09 + docs)

## 메타

- **Sprint**: 017 M0 5/5 종결 + **M1 4/4 종결** (T06 #223 / T07 #224 / T08 #226 / **T09 #228 `d710fde`**) → **M1 closure**
- **누적 PR (Sprint 017)**: 코드 8 (#219 T01 / #220 T02 / #221 T03 / #223 T06 / #224 T07 / #226 T08 / **#228 T09**) + docs 3 (#222 T04+T05 / #225 T07 handoff / **#227 T09 handoff 예정**) = **11 PR** (T09 docs 머지 후 12)
- **단위 테스트**: 1436 → **1605** (+169, T01 +17 / T02 +19 / T03 +10 / T06 +62 / T07 +18 / T08 +28 / T09 +15)
- **codex 사전/dual review finding**: 누적 **40건 모두 본 PR 내 hotfix 흡수 또는 KI 위임** — 학습 #18 **13~19번째 확증**
- **KI 변동 (Sprint 017 M0~M1 누적)**: KI-027 신규 +1 + closed (T02) / KI-009 closed (T03) / KI-028 신규 +1 (T02 open) / KI-029 신규 +1 (T07 open) / KI-024 closed (T08) / KI-030 신규 +1 (T08 open) / T09 KI 변동 0 — 합산: closed 3 / open +4

## 1. T01 PR #219 — drainUntil helper

산출물: `tests/unit/main/_helpers/drainUntil.ts` (64) + `tests/unit/main/_helpers/drainUntil.test.ts` (210) — 17 케이스. 회귀 +17 / 1436 → 1453.

scope (contract §7 리스크 6 정합): 본 PR = helper 신규만. BackgroundTranslationQueue.test.ts refactor 는 T01b 후속 PR 분리.

dual review hotfix 흡수 4건 (codex Minor 1 chain promise continuation + NB 3 NaN/Infinity/비정수 입력 검증 + async docstring + evaluator Partial PR 미개설). 머지 commit `7ab9de9`.

## 2. T02 PR #220 — pageId/visitId 빈 문자열 normalize

산출물:
- 신규: `src/storage/idNormalize.ts` (19) — `normalizeOptionalId(value): string | null`
- 신규: `tests/unit/storage/idNormalize.test.ts` (57) — 7 cover
- 수정: `src/main/noteHandlers.ts` (+4/-2) IPC 경계 / `src/main/NoteService.ts` (+5/-2) Service 경계 / `src/storage/HighlightStore.ts` (+9/-3) Storage 경계 (add + listByPage)
- 수정: 4 단위 회귀 파일 +136/-4 (noteHandlers +4 / NoteService +3 / HighlightStore +5 신규 + 1 정정 + idNormalize 7)
- 수정: `.flowset/known-issues.md` — KI-027 신규 + closed + KI-028 신규 + 통계 갱신

회귀 +19 / 1453 → 1472. dual review hotfix 흡수 (codex Minor wording + Finding 1 산식 정합 + Separate KI candidate → KI-028 별도 등록). 머지 commit `3d77d76`.

## 3. T03 PR #221 — MemoryStatsPanel React 단위 회귀 (KI-009 closed)

산출물:
- 신규: `tests/unit/renderer/MemoryStatsPanel.test.tsx` (295) — 10 케이스 (mount / workspaceId 변경 / 폴링 / 3 error variant / null + onInvalidated 격리 2 + unmount cleanup)
- 수정: `package.json` (+2) — `@testing-library/react` ^16.3.2 + `@testing-library/dom` ^10.4.1 (devDependencies)
- 수정: `package-lock.json` (+152)
- 수정: `vitest.config.ts` (+11/-2) — `.test.tsx` include + happy-dom matrix + esbuild jsx automatic
- 수정: `tsconfig.web.json` (+2/-1) — tests/unit/renderer .tsx include (codex F2)
- 수정: `eslint.config.mjs` (+2/-1) — tests .tsx lint override (codex F2)
- 수정: `.flowset/known-issues.md` (+10/-10) — KI-009 closed + 산식 정합화 (codex F1)

회귀 +10 / 1472 → 1482. dual review hotfix 흡수 (codex Finding 1 산식 정합화 closed 17→18 / open 10→9 / 잔여 11→10 + Finding 2 .tsx 범위 보정). 머지 commit `e9ab22a`.

## 4. T04 — KI-018/019 closed 상태 audit (본 docs PR)

Sprint 016 M5 T25 시점 status `closed` 전환된 KI-018 (top-10 hit rate ≥ 80%) + KI-019 (AI 출처 정확도 ≥ 90%) — 본 Sprint 017 M0 진입 시점 carryover regression 0 + 산식 cover audit.

재실행 결과 (2026-05-22 00:14):
- `tests/integration/scenarios/scenario-accuracy.test.ts`: **12 PASS**
- `tests/unit/scenarios/accuracyHelpers.test.ts`: **17 PASS**
- 누적 **29 PASS / 0 FAIL** (Sprint 016 M5 T23 산식 cover 완전 carryover)

결론: KI-018/019 closed 상태 정합. 회귀 0. 본 Sprint 017 진입 시점 재검증 PASS.

## 5. T05 — KI-011~016 perf bench baseline 재측정 (본 docs PR)

Sprint 016 M0 T06 시점 closed 전환된 perf bench 6종 — Sprint 017 M0 진입 시점 carryover 재측정.

재실행 결과 (2026-05-22 00:14, `tests/integration/perf-baseline.test.ts` 7 케이스):

| KI | 임계 | Sprint 016 M0 T06 baseline | Sprint 017 M0 T05 재측정 | 정합 |
|---|---|---|---|---|
| KI-011 (1K) | < 20ms | — (10K 만 측정) | **0.046ms (1K)** | PASS (신규 측정) |
| KI-011 (10K) | < 20ms | **0.447ms** | **0.453ms (10K)** | PASS (회귀 0, +0.006ms 노이즈) |
| KI-012 | < 500ms | 0.027ms | **0.027ms** | PASS (동일) |
| KI-013 | < 200ms top-10 (1K) | 1.404ms | **1.447ms** | PASS (회귀 0, +0.043ms 노이즈) |
| KI-014 | < 1초 (10ws × 10tabs) | **0.283ms** (KI-006 abort wiring 전, Sprint 016 M0 T06) → **0.581ms** (T02 abort wiring 후, Sprint 016 M0 T06 §2.4) | **0.339ms** | PASS (회귀 0, T02 baseline 대비 -0.242ms 개선 / T06 baseline 대비 +0.056ms 노이즈) |
| KI-015 | < $3/월 (1만 페이지) | $0.20/월 | **$0.20/월** | PASS (동일) |
| KI-016 | < 200MB (1만 페이지) | 90.32MB | **90.32MB** | PASS (동일) |

결론: 6종 임계 모두 PASS. carryover regression 0. KI-011~016 closed 상태 정합.

`.flowset/eval-results/sprint-016-perf-bench.md` §6 (audit append) 박힘.

## 6. 본 docs PR 검증

| 검증 | 결과 |
|---|---|
| `npm run lint` | (docs only, 무관) |
| `npm run typecheck` | (docs only, 무관) |
| `npm test` (관련 회귀) | scenario-accuracy 12 + accuracyHelpers 17 + perf-baseline 7 = **36 PASS** |
| `npm run build` | (docs only, 무관) |
| T01/T02/T03 머지 commit | `7ab9de9` / `3d77d76` / `e9ab22a` |

## 7. 자기 검증 루프

자기 검증 루프 **38회차 SUCCESS** (#181~#221 + 본 docs PR, 38 PR 누적).

## 8. 다음 진입점

**Sprint 017 M0 5/5 종결** (T04/T05 docs PR #222 머지) + **M1 T06 머지** (PR #223 `4df267b`).

## 9. Sprint 017 M1 T06 — NoteHighlight renderer UI overlay (PR #223 `4df267b`)

산출물 (commit `323929b` + hotfix `3ca600a` 합산 +2518/-1 / 14 파일):
- 신규: `src/perception/highlightInjectionScript.ts` (470) — browser-safe SHA-256 + RFC 6234 폴리필 + serialize/restore IIFE 빌더 + sanitize 헬퍼
- 신규: `src/main/highlightHandlers.ts` (287) — 4 IPC handler + composite create + noteId 무결성 검증
- 신규: `src/main/highlightRestore.ts` (104) — runHighlightRestore + insertCSS + executeJavaScript
- 신규: `src/renderer/src/note/NoteHighlight.tsx` (156) — 노트 패널 highlight 목록 UI (App.tsx mount T08 위임)
- 신규: 단위 회귀 4 파일 — handlers 24 / restore 10 / injection 20 / NoteHighlight 8 = **62 케이스**
- 수정: services.ts / index.ts / NoteService.ts / preload index.ts + d.ts / storage index.ts

scope (codex 사전 협의 정합, threadId 019e4b75): selection 저장 + 페이지 재방문 시 visual 복원 end-to-end. UX 완성도 (toast / 포커스 / z-index / App.tsx mount) T08 위임.

selection capture: WebContentsView preload 부재 → context-menu + `webContents.executeJavaScript(buildSerializeScript())` 경로 고정.
visual highlight: CSS Highlight API + `webContents.insertCSS('::highlight(<sanitized>)')` — DOM 비변경. 미지원 환경 graceful no-op.
restore trigger: `runHighlightRestore` 헬퍼 did-finish-load 옆 배치. `tabManager.snapshotAll().tabs.find` 의 `tab.workspace_id` 사용 (codex Finding 1 흡수 정합).

dual review hotfix 흡수 (codex threadId 019e4b8d — 1차 NEEDS_CHANGES 5 finding → 2차 정합 확정):
1. **BLOCKING** workspace 격리 (cross-ws visual leak) → tab.workspace_id 기반
2. **BLOCKING** crypto.subtle secure context only → RFC 6234 pure JS SHA-256 폴리필 fallthrough
3. **NEEDS_CHANGES** CSS rule / registry name 불일치 → `__fbSanitizeId` 공통화 (buildHighlightCssForIds 동일 정규식)
4. **NEEDS_CHANGES** noteId 명시 분기 무결성 → NoteService.getNote + cross-workspace 검증
5. **NOTABLE** remove 후 visual 즉시 미사라짐 → **T08 위임 유지** (NoteHighlight 미마운트, codex 2차 확정)

검증: lint 0 / typecheck PASS / test **1482 → 1544 (+62)** / build PASS / CI 6/6 PASS.

학습 #18 **16번째 확증** — T06 codex 사전 협의 + dual review 1차 NEEDS_CHANGES 5 finding 모두 본 PR 내 흡수 (4) 또는 T08 위임 명시 (1).

## 10. Sprint 017 M1 T07 — Highlight SQLite swap + V4→V5 마이그레이션 (PR #224 머지)

머지 commit: `c22b2e7` (squash, 2026-05-22 07:32:47 UTC). 2 commits 흡수 (initial `34ba208` + dual review hotfix `f97a9b1`).

산출물 (+1320 / -30 / 12 파일):
- 신규 `src/storage/schema/v05.sql` (251) — v04 canonical 사본 + `highlights` 테이블 + 6종 인덱스 (codex 019e4e82 NOTABLE #4 hotfix 후 url-time + note-time 추가)
- 신규 `src/storage/migrations/v04_to_v05.ts` (174) — 5단계 (sentinel 체크 → 판정 → 자동 백업 → dry-run 분기 → applySchema + sentinel). G-014 강제.
- 신규 `tests/unit/storage/migrations/v04_to_v05.test.ts` (286) — 8 회귀 (fresh / v04 / 데이터 보존 / dry-run / idempotent / 백업 파일 / highlights INSERT / revert 안전). v04 fixture 는 실제 `v04.sql` raw import 사용 (codex 019e4e82 NEEDS_CHANGES #2 hotfix).
- 수정 `src/storage/HighlightStore.ts` (+188 / -17) — `constructor(fb?: FlowbrowserDatabase)` optional + SQLite-backed + in-memory fallback + `inMemoryForFallback()` factory (production footgun 차단, codex 019e4e82 NOTABLE #5)
- 수정 `src/storage/Database.ts` (+11 / -1) — schema import `v04.sql` → `v05.sql` + `V05_SCHEMA_VERSION=2`
- 수정 `src/storage/index.ts` (+16 / -1) — migrateV04ToV05 + V05 상수 6종 export
- 수정 `src/main/services.ts` (+15 / -5) — bootstrap 분해 (`open` → `migrateV04ToV05` → `ensureDefaultWorkspace`, codex 019e4dd1 BLOCKING #1 정합) + `HighlightStore(flowbrowserDb)` 재할당 (영속 활성) + `HighlightStore.inMemoryForFallback()` 명시 fallback path
- 수정 `src/main/highlightHandlers.ts` (+17 / -1) — composite branch `noteService.createNote` FK error boundary 박힘 (codex 019e4e82 NEEDS_CHANGES #1 hotfix)
- 수정 단위 회귀: `HighlightStore.test.ts` (+305/-1, SQLite 8 + factory 1) / `highlightHandlers.test.ts` (+39, FK boundary 1) / `Database.test.ts` (+5/-2, V05 정합)
- 수정 `.flowset/known-issues.md` — KI-029 신규 (Phase 3 LOW, sentinel self-healing 부재)

codex 019e4dd1 사전 협의 — BLOCKING 1건 + 권고 6건 모두 흡수:
1. **BLOCKING** Database.bootstrap 자동 applySchema → G-014 순서 파괴 → 마이그레이션 함수가 schema 적용 순서 소유 (services.ts bootstrap 분해)
2. schema_meta sentinel — migrateV04ToV05 가 fresh_install 포함 모든 완료 분기에서 직접 박음
3. WAL safe snapshot — `fb.getDb().backup(destPath)` 사용 (better-sqlite3 12.x Backup API)
4. HighlightStore 생성자 `fb?` optional — DB bootstrap 성공 후 재할당
5. anchor JSON TEXT 단일 컬럼
6. FK 정책 — note_id CASCADE / workspace_id CASCADE / page_id SET NULL
7. 인덱스 — (workspace_id, page_id, created_at) + (workspace_id, url, content_hash, created_at) + (note_id) + (workspace_id, created_at) — 추후 #4 권고로 보강 2종 추가

codex 019e4e82 dual review — NEEDS_CHANGES 2건 + NOTABLE 3건 (4 본 PR hotfix 흡수 + 1 KI 위임):
1. **NEEDS_CHANGES #1** handlers composite FK error boundary 누락 → noteService.createNote try/catch + 단위 회귀 1
2. **NEEDS_CHANGES #2** v04_to_v05 fixture 가 실 v04 schema 미사용 → `v04.sql?raw` 직접 exec + highlightsTableExists helper + case 2/4 강화
3. **NOTABLE #3** sentinel-only idempotency self-healing 부재 → **KI-029 위임** (Phase 3 M3 후속 batch)
4. **NOTABLE #4** 인덱스 보강 — `idx_highlight_workspace_url_time` + `idx_highlight_note_time` 추가
5. **NOTABLE #5** production footgun — `inMemoryForFallback()` factory 추가 + services.ts 사용 변경

evaluator: **Pass 10 / Partial 1 / Fail 0** (Partial: AC-2 4항 Workspace Export/Import T09 위임 — G-013 단계별 PR 정합) → 머지 권고.

검증: lint 0 / typecheck PASS / test **1544 → 1562 (+18)** (HighlightStore SQLite 8 + v04_to_v05 8 + factory 1 + handlers FK boundary 1) / build PASS / CI 6/6 PASS.

학습 #18 **17번째 확증** — T07 codex 사전 협의 (BLOCKING 1 + 권고 6) + dual review (NEEDS_CHANGES 2 + NOTABLE 3) = 11 finding 중 10 본 PR 내 흡수 + 1 KI-029 위임. 자기 검증 루프 **40회차 SUCCESS**.

## 11. Sprint 017 M1 T08 — NoteHighlight overlay UX 머지 (PR #226 `db1d18f`)

머지 commit: `db1d18f` (squash, 2026-05-22 09:00:52 UTC). 2 commits 흡수 (initial `8d02778` + dual review hotfix `7a5d9c1`).

산출물 (+1247 / -77 / 12 파일):
- 신규 `src/renderer/src/common/ToastHost.tsx` (113) — 전역 ToastHost + `pushToast` 모듈 + `useToast` hook (module-level subscribers, AUTO_DISMISS 4s) + `__resetToastsForTest` (production 위험 docstring 강화)
- 신규 `src/perception/highlightInjectionScript.ts` 추가 (+99) — `buildRemoveVisualScript` + `buildScrollToScript` (Array.from(hl)[0] 안전 path + HIGHLIGHT_NAME_PREFIX + __fbSanitizeId 정합)
- 신규 `src/main/highlightRestore.ts` 추가 (+49) — `runHighlightRemoveVisual` + `runHighlightScrollTo` (page context inject, graceful)
- 수정 `src/renderer/src/note/NoteHighlight.tsx` (+80/-23) — useEffect race guard (cancelled flag) + selectedId workspaceId/url cleanup + 클릭 시 `highlightApi.scrollTo(id, expectedUrl)` + remove `(id, expectedUrl)` + toast (success/error/info)
- 수정 `src/renderer/src/App.tsx` (+73/-9) — Chat/Notes 탭 (`sidePanelTab` state) + `activeUrl` state + `browser:navigated` 구독 (race guard navigatedSinceMount flag) + ToastHost 전역 mount + `highlightApi.onToast` 구독
- 수정 `src/main/services.ts` (+78/-3) — `setActiveWebContentsGetter` hook + `highlight:remove` path 에 visual delete 묶음 + `highlight:scroll-to` IPC 신규 + expectedUrl mismatch graceful no-op
- 수정 `src/main/index.ts` (+24/-1) — hook 등록 (getActiveTabView WebContents) + `handleContextMenuHighlight` errorCode broadcast (`highlight:toast` → renderer)
- 수정 `src/preload/index.ts` (+26/-1) — `highlightApi.scrollTo(id, expectedUrl?)` + `remove(id, expectedUrl?)` + `onToast` 노출 + `HighlightToastPayload` 타입
- 수정 `src/renderer/src/styles.css` (+195/-2) — `side-panel__tabs` + `note-highlight` 클래스 + `toast-host` (WebContentsView native layer 정합 header 안 제한, top:6 / max-height:84 / -webkit-line-clamp:2)
- 단위 회귀 +28: `ToastHost.test.tsx` 7 신규 + `NoteHighlight.test.tsx` T08 11 신규 (click/selected/scrollTo/remove/event.stopPropagation/graceful/aria-pressed/dual review hotfix 5) + `highlightInjectionScript.test.ts` 10 신규 (RemoveVisual 5 + ScrollTo 5)

scope (T08 contract 정합):
- App.tsx mount path — Chat/Notes 탭형 우측 패널 (panelOpen 공유) — codex 019e4ec8 #1
- visual remove 강제 — store DELETE 직후 page context `CSS.highlights.delete(prefix + sanitize(id))` — codex 019e4ec8 #2
- 클릭 → scrollIntoView smooth/center + selected state (aria-pressed + selected class) — codex 019e4ec8 #3
- 전역 ToastHost — NoteHighlight 패널 mount 무관 표시 보장 — codex 019e4ec8 #4
- 다중 z-index — 단일 색상 + alpha overlap + createdAt 등록 순서 정합 유지 (schema 변경 X) — codex 019e4ec8 #5
- KI-024 graceful toast — `handleContextMenuHighlight` errorCode (no_selection / unsupported_selection / serialize_failed) 시 한글 메시지 — codex 019e4ec8 #6

codex 019e4ec8 사전 협의 — 권고 6건 모두 본 PR 흡수 (BLOCKING 0).

codex 019e4eda dual review — BLOCKING 1 + NEEDS_CHANGES 3 + NOTABLE 3 (5 본 PR hotfix 흡수 + 1 KI-030 위임):
1. **BLOCKING #1** NoteHighlight reload async race guard — useEffect cancelled flag 박음
2. **NEEDS_CHANGES #2** App.tsx getCurrentUrl race vs onNavigated — navigatedSinceMount flag
3. **NEEDS_CHANGES #3** IPC payload expectedUrl 동반 + handler 매칭 graceful no-op
4. **NEEDS_CHANGES #4** toast z-index WebContentsView 가릴 위험 — header (96px) 안 제한
5. **NOTABLE #5** selectedId workspaceId/url 변경 시 null cleanup
6. **NOTABLE #6** __resetToastsForTest production 위험 docstring 강화
7. **NOTABLE #7** App-level 회귀 cover gap — **KI-030 위임** (Phase 3, LOW)

evaluator: **Pass 11 / Partial 1 (AC-2 #4 T09 위임) / Fail 0** — 머지 권고.

검증: lint 0 / typecheck PASS / test **1562 → 1590 (+28)** / build PASS / CI 6/6 PASS.

학습 #18 **18번째 확증** — T08 codex 사전 (6 권고) + dual review (BLOCKING 1 + NEEDS_CHANGES 3 + NOTABLE 3) = 13 finding 중 12 본 PR 내 흡수 + 1 KI-030 위임. 자기 검증 루프 **41회차 SUCCESS**.

## 12. KI 변동 (Sprint 017 M1 T08 머지 시점)

- **KI-024 closed** — Shadow DOM cross-boundary graceful fallback path 박힘 (`handleContextMenuHighlight` errorCode → `highlight:toast` broadcast → 사용자 한글 안내 toast). 실제 Shadow DOM anchor 지원 (open/closed) 은 별도 후속 KI.
- **KI-030 신규** — App.tsx App-level 회귀 cover gap (Phase 3, LOW). onNavigated 구독 / Chat/Notes 탭 토글 / getCurrentUrl race guard / setActiveWebContentsGetter hook cycle — 본 PR 코드 path 박혔으나 React testing 측 회귀 누락. codex 019e4eda NOTABLE #7 정합. dependency 큰 App.tsx 분해 후 진행 권고.

## 13. 다음 진입점 (T08 머지 시점 기준)

**Sprint 017 M1 T09 진입** (Highlight Export/Import — AC-2 4항 closure):
- `WorkspaceExportImportService` 에 highlights 행 추가 — export JSON 에 `highlights[]` 박음 + import 시 contentHash conflict 정합 + FK rewrite (note_id / page_id / workspace_id 새 발급 매핑)
- 단위 회귀 — round-trip 보존 + cross-workspace 차단 + 중복 contentHash 시 skip 정합
- T06 (renderer UI) + T07 (SQLite 영속) + T08 (UX) 모두 의존 충족 — T09 는 Workspace export/import path 만 확장

후속: M2 (Sprint 016 신규 KI batch — T10 KI-020 / T11 KI-021 / T12 KI-022 / T13 KI-013 freeform wording).

**Sprint 016 M5 신규 가드레일 후보 (G-019/G-020) 정식화**: Sprint 017 종합 evaluator 시점 (T22~T24, M5).

## 14. Sprint 017 M1 T09 — Highlight Export/Import 통합 머지 (PR #228 `d710fde` — M1 closure)

머지 commit: `d710fde` (squash, 2026-05-22). 2 commits 흡수 (initial `782fc96` + dual review hotfix `7da3259`).

산출물 (+618 / -24 / 3 파일):
- 수정 `src/main/WorkspaceExportImportService.ts` (+115 / -12) — `WORKSPACE_EXPORT_SCHEMA_VERSION` 'v04'→'v05' bump + `WORKSPACE_EXPORT_SCHEMA_VERSIONS_ACCEPTED=['v04','v05']` 신규 + `ExportedHighlight` interface + `WorkspaceExportV1.highlights[]` + `ImportResultSummary.highlights:number` + exportWorkspace highlights SELECT (workspace_id 격리 + ORDER BY created_at, `idx_highlight_workspace_time` 매칭) + importWorkspace per-row 검증 (id/note_id/url/content_hash/created_at typeof) + note_id 매핑 누락 skip / page_id 누락 null / workspace_id 강제 / duplicate id 방어 / malformed anchor skip + validatePayload v04 graceful (highlights:[] normalize, v05일 때만 highlights 읽음)
- 수정 `src/preload/index.ts` (+2) — `WorkspaceImportResponsePayload.summary.highlights: number` 추가
- 수정 `tests/unit/main/WorkspaceExportImportService.test.ts` (+501 / -12) — T09 describe 13 케이스 + 정합화 2 (회귀 29 → 42)

scope (T09 contract 정합):
- AC-2 #4 closure — Workspace Export/Import 에 highlights 행 포함 round-trip 보존
- G-013 단계별 PR 전략 정합 — T06 (renderer) → T07 (SQLite) → T08 (UX) → T09 (Export/Import) 순. 본 PR 은 highlights 테이블/UI/IPC 무변경, 오직 Export/Import path 만 확장
- G-014 — 본 PR 은 schema 변경 X (T07 처리). export schemaVersion bump 만 (v04 BC graceful)

codex 019e4f02 사전 협의 — 권고 6건 모두 본 PR 흡수 (BLOCKING 0):
1. Q1 (A) — v05 bump + v04 graceful (highlights=[])
2. Q2 (A+B+C) — note_id NOT NULL → skip / page_id nullable → null / workspace_id 강제
3. Q3 — contentHash dedupe X (round-trip 보존) + duplicate highlight.id 방어 skip
4. Q4 — `WHERE workspace_id = ? ORDER BY created_at ASC` (`idx_highlight_workspace_time` 매칭)
5. Q5 — anchor TEXT 그대로 INSERT + JSON parse 가능 여부만 검증 (malformed 차단)
6. 추가 — preload import summary payload 에 `highlights: number` 추가

codex 019e4f19 dual review — BLOCKING 0 + NEEDS_CHANGES 2 + NOTABLE 1 (2 본 PR hotfix 흡수 + 1 후속 위임):
1. **NEEDS_CHANGES #1** v04 payload + highlights[] 포함 시 그대로 import (graceful BC 정책 위배) → `schemaVersion === 'v05'` 일 때만 highlights 읽기 + 회귀 1 (v04 + corrupt highlights → 무시)
2. **NEEDS_CHANGES #2** malformed highlight row 의 NOT NULL 필드 (id/url/content_hash/created_at) 미검증 → SQLite 예외가 transaction 전체 rollback 위험 → per-row typeof 검증 + skip + 회귀 1 (4 missing + 1 valid → highlights=1 + 다른 child INSERT 정상)
3. **NOTABLE** import 후 `HighlightStore.listByNote(newNoteId)` integration 검증 → T09 round-trip 회귀가 export query 로 동일 path 간접 cover, 명시 integration 검증은 **Sprint 017 M2 후반 또는 M5 위임**

evaluator (initial 평가) — **Pass 9 / Partial 1 (CI 5/6 — FlowSet Policy Check fail) / Fail 1 (PR body 양식)** → 본 hotfix `7da3259` + PR body 정정 후 모두 해소. 최종 머지 권고.

검증: lint 0 / typecheck PASS / test **1590 → 1605 (+15)** (T09 describe 11 + 정합화 2 + dual review hotfix 2) / build PASS / CI 6/6 PASS.

학습 #18 **19번째 확증** — T09 codex 사전 (6 권고) + dual review (NEEDS_CHANGES 2 + NOTABLE 1) = 9 finding 중 8 본 PR 내 흡수 + 1 후속 위임. 자기 검증 루프 **42회차 SUCCESS**.

## 15. KI 변동 (Sprint 017 M1 T09 머지 시점)

- **KI 변동 0** — 본 PR 은 Export/Import path 만 확장 (HighlightStore / NoteHighlight / schema 무변경).
  - KI-022 (Import embedding_queue re-enqueue) — Sprint 017 M2 T12 위임 정합 (본 PR 은 embedding_queue.enqueue 호출 0)
  - KI-024 (T08 closed) / KI-030 (T08 신규) — 본 PR 무영향

## 16. 다음 진입점

**Sprint 017 M2 진입** (Sprint 016 신규 KI batch):
- T10 — KI-020 SPA did-navigate-in-page hook (GitHub issue 클릭 / Notion 페이지 전환 시나리오)
- T11 — KI-021 partition cleanup reconcile path 부팅 시 1회 실행 + log
- T12 — KI-022 Import embedding_queue re-enqueue (`WorkspaceExportImportService.importWorkspace` 후 `EmbeddingQueue.enqueue` 자동 호출, pages + notes 양쪽) — T09 산출물 위에 박힘
- T13 — KI-013 freeform fallback wording 정합 + 회귀

후속 (M3~M5): 로컬 LLM spike / Notion Export / 공유 설계 시안 / 종합 PRD v0.5.0.

---

**자기 검증 루프 42회차 SUCCESS** (Sprint 015~017 누적 42 PR — 자기 검증 시스템 안정 운영). Sprint 017 M1 5종 (T06~T09 + docs) 정합 closure.
