# 2026-05-20 — Sprint 016 M0 T05 종료 + mini-milestone α 완료

## 메타 (최종 — 본 세션 종료 시점)

- **Phase**: **1 형식적 종료 + 실용 검증 진행 중** (Sprint 016 M0 잔여 T01/T02/T04/T06 + mini-milestone β 다음 세션 0순위)
- **Sprint**: **016 M0 T05 종료 + mini-milestone α 완료** (T03 분할 옵션 A 3 PR + docs 2 PR 직후 본 세션 T05 1 PR + docs 후속 1 PR + mini-milestone α 1 PR + 최종 핸드오프 1 PR 머지, PR #178 닫음)
- **PRD 버전**: v0.4.0 정식
- **본 세션 PR (머지 3건 + 닫음 1건)**: **#176** (`cae365e`, T05 KI-010 closed) + **#177** (`e68eb8a`, T05 docs 후속) + **#179** (`67f9c4d`, mini-milestone α) + #178 closed (학습 #5 정정 — mini-milestone α 흡수)
- **누적 단위 테스트**: 1089 → **1101 (+12)** (PR #176 본문 변경, #177/#179 docs only)
- **KI 잔여**: 17 → **17** (KI-010 LOW closed -1 + KI-020 LOW 신규 +1 동수, HIGH 1 / MEDIUM 3 / LOW 13)
- **PR 누적**: 75 → **79** (열린 PR 번호 카운터 기준, closed #178 포함. 머지 누적 78 + closed 1 = 79 발급)
- **가드레일**: G-011 → **G-015** (G-012/G-013/G-014/G-015 본체 정식 등록, PR #179 mini-milestone α 흡수)
- **본 세션 종료 검증**: typecheck + lint + test (1101 PASS / 67 files) PASS

---

## 1. PR #176 — IndexingService wiring + did-finish-load broadcast (KI-010 closed)

### 1.1 산출물

| 파일 | 변경 | 비고 |
|---|---|---|
| `src/main/IndexingService.ts` | +9 / -6 | `IndexingStatusPayload.workspaceId?: string` 추가. `emit()` 시그니처 확장 — indexed 시 `visit.page.workspace_id` / blocked 시 undefined |
| `src/main/services.ts` | +97 | `IndexingGate` (UserSetting.privacyExclusions getter wiring) + `IndexingService` 인스턴스화 + `createIndexingBroadcastHandler` factory 함수 export + `tryIndexPage` / `getParagraphsExtractScript` / `getIndexingServiceForTest` export |
| `src/main/index.ts` | +63 | `createTabView` did-finish-load 에 `runPageIndexing` 헬퍼 — scanWebContentsFields + ParagraphExtractor.executeJavaScript + tryIndexPage 호출. http/https allowlist 선필터, graceful try/catch |
| `tests/unit/main/IndexingService.test.ts` | +102 | workspaceId broadcast payload describe +7 케이스 (indexed default ws / indexed input ws / blocked domain / blocked password / blocked invalid url / empty_content payload / 재방문 unchanged payload) |
| `tests/unit/main/indexingBroadcast.test.ts` | +94 (신규) | createIndexingBroadcastHandler factory 단위 +5 (indexed workspaceId 명시 / 미명시 / blocked workspaceId undefined / blocked workspaceId override 방어 / 다회 호출 분기) |

### 1.2 dual review 결과

- **evaluator (사전)**: **Pass 8/8** — BLOCKING 0 / NEEDS_CHANGES 0
  - 8 항목: IndexingGate wiring / IndexingService wiring / broadcast 정합 (PRD §07.4.2) / workspaceId payload 정합 / did-finish-load hook 정합 / KI-010 closed 산식 / 단위 회귀 cover / graceful 처리 — 모두 Pass
- **codex (사전)**: **NEEDS_CHANGES** — NB 3 / NEEDS_CHANGES 1 / BLOCKING 0
  - NEEDS_CHANGES #7: services.ts broadcast closure 호출 자체 검증 미커버 — **본 PR 내 hotfix `485092e`**: `createIndexingBroadcastHandler` factory 추출 + indexingBroadcast.test.ts +5
  - NB-5: URL scheme http/https allowlist 선필터 권고 — **본 PR 내 hotfix `485092e`**: `if (!url.startsWith('http://') && !url.startsWith('https://')) return`
  - NB-1: SPA `did-navigate-in-page` 인덱싱 누락 — **후속 KI-020 LOW 신규 등록**
  - NB-6: isActiveTab stale 가능성 (acceptable, 의도된 finish-time semantics)

### 1.3 본 PR 내 hotfix 시퀀스

| commit | 흡수 | 설명 |
|---|---|---|
| `f7ca8a2` | (1차) | T05 core wiring |
| `485092e` | NEEDS_CHANGES #7 + NB-5 | `createIndexingBroadcastHandler` factory + indexingBroadcast.test.ts +5 + http/https allowlist |

### 1.4 KI 변동

- **KI-010 LOW closed** (Sprint 016 M0 T05 PR #176)
  - 산식: PRD §07.4.2 broadcast 3종 (인덱싱 / 노트 / AI 채팅) 전부 cover. 인덱싱은 본 PR 의 `onStatusChange` → `broadcastMemoryInvalidated` wiring 으로 cover.
- **KI-020 LOW 신규 등록** (SPA `did-navigate-in-page` 자동 인덱싱 누락)
  - 출처: 본 PR 사전 dual review codex NB-1
  - 권고 해소: Sprint 016 M1 (시나리오 2 PM 경쟁 — GitHub/공식 docs SPA 흐름 cover) 또는 후속 hotfix
  - 권고 구현: `did-navigate-in-page` hook + `runPageIndexing` 재호출 (debounce 500ms~1s) + iframe nav 무시 + IndexingService recordVisit UPSERT + content_hash 매칭이 중복 차단

### 1.5 본 세션 학습

1. **broadcast wiring 검증을 위한 factory 추출 패턴** — services.ts 의 onStatusChange closure 직접 검증 어려움 (BrowserWindow / IPC 의존). `createIndexingBroadcastHandler(broadcaster)` factory 로 추출 → broadcaster 를 spy/mock 가능 + 호출자 (services.ts) 는 1줄. 비슷한 wiring (note/chat broadcast) 도 향후 factory 패턴 권고.
2. **URL scheme allowlist 선필터** — IndexingGate parseUrl 이 결국 차단하더라도 본문 추출 (executeJavaScript, scanWebContentsFields) 비용 절약. http/https 외 scheme (file:, blob:, javascript:, data:, chrome-error:, about:) 일괄 skip. SPA 환경에서 빈번한 hash-only 변경 / iframe blob: 대응.
3. **codex NB 분류 능력** — 본 NB-5 는 codex 가 "결국 차단되나 비용 관점 권고" 로 명시 — BLOCKING 으로 격상 안 함. 본 PR 내 hotfix 흡수 후 NB 무력화. NB-1 은 scope 외라 후속 KI 등록 권고 — codex 가 "T05 범위로 볼지 후속 KI 로 둘지 명시" 권고 그대로 적용.
4. **dual review 사전 호출 효과** — 본 PR 은 docs PR #174 위반 학습 #8 적용 첫 코드 PR. evaluator + codex 동시 호출 → codex 가 wiring 검증 누락 + URL scheme 선필터 권고 정밀 발견 → 본 PR 내 hotfix 흡수. 사전 평가 효율 재확인.
5. **codex 호출 표준 — 실측 기반 (본 세션 G-006 위반 2회 재현)** — 본 세션 codex 사전 dual review 진행 시 (a) raw MCP tool (`mcp__codex__codex`) 직접 호출 + `gpt-5.2-codex` 모델 ID 추측 지정 → 미지원 에러, (b) 사용자 1차 지적 후 정정 시점에도 "gpt-5.5-codex" 추측 명시 → 이것도 사실 아님 (2차 지적). **실측 확인** (`~/.codex/config.toml`, `codex-cli 0.130.0`, `codex-companion.mjs`):
   - 사용자 디폴트 모델: `model = "gpt-5.5"` (`~/.codex/config.toml`), `model_reasoning_effort = "high"`
   - 모델 alias: `spark` → `gpt-5.3-codex-spark` (`codex-companion.mjs` L70 `MODEL_ALIASES`)
   - slash commands: `/codex:rescue`, `/codex:setup`, `/codex:status`, `/codex:result`, `/codex:cancel`, `/codex:review`, `/codex:adversarial-review` (`commands/*.md`)
   - 본 conversation MCP tool schema description 의 `gpt-5.2` / `gpt-5.2-codex` 는 예시일 뿐 — 사용자 환경 디폴트 아님

   **올바른 표준**:
   - 우선 `/codex:rescue` slash command (codex-rescue subagent forwarder) — model 미명시 시 config.toml 디폴트 적용
   - raw MCP 직접 호출 필요 시 model 파라미터 **생략** — config.toml `gpt-5.5` 자동 적용
   - 특정 모델 명시 시 사용자 환경에서 실측 확인 후 사용 — 임의 ID 지정 금지 (G-006)

   본 세션 학습 자체가 G-006 (추측 금지) 위반 2회로 재발. **향후 codex/CLI/MCP/tool schema 참조 시 description 예시 ≠ 환경 디폴트, 실측 확인 후 작성** — 본 mini-milestone α 흡수.

---

## 2. 다음 세션 진입 권고

### 2.-1 권고 0순위 — mini-milestone β (hooks 자동화, codex PR #179 NEEDS_CHANGES #10 정합)

본 세션 사용자 지적 ("디렉토리 구조 + 워크플로우 셋팅 + 자체 정책 빠짐") + codex 협의 옵션 D 채택 결과 — mini-milestone β 는 α (본 핸드오프 PR #179 머지) 직후 진입. T06/T04/T02 보다 **최우선**. 자동 강제 부재 (`.claude/settings.json` 의 SessionStart echo 1줄) 가 본 세션 G-006 위반 2회 + 학습 #8 위반 1회 재현의 직접 원인 — 다음 세션도 동일 패턴 재발 가능.

mini-milestone β 산출물 (codex 권고):
- `.claude/settings.json` SessionStart hook 실 주입 (state.md / 최신 handoff / guardrails.md / docs/prd README 실 출력)
- PreToolUse hook (`gh pr create/merge`, `git commit`, `git push` 전 dual review 체크파일 또는 PR body 체크박스 게이트)
- PostToolUse hook (문서 변경 감지 시 README/CLAUDE/USAGE/PRD 동기화 검사)
- Stop hook (ownership.json 소유권 / G-006 위험 표현 grep / dual review 누락 / handoff·state 갱신 여부 점검)
- CI `flowset-policy-check` job 추가 (hook 만으로 우회 가능)
- `.github/PULL_REQUEST_TEMPLATE.md` 신규 (dual review 체크박스 + 변경 영향 명시)

분량 추정: 150~400 LOC (codex 협의).

### 2.0 권고 순서 rationale 분리 (codex PR #177 NEEDS_CHANGES #6 — PR #175 §15.7.3 패턴 적용)

KI severity 우선순위 (HIGH > MEDIUM > LOW) 만 따르면 T04 (KI-004 MEDIUM) / T02 (KI-006 MEDIUM) / T01 (KI-001 MEDIUM in-progress) 가 T06 (KI-011~016 + KI-018~019 모두 LOW) 보다 앞서야 한다. 본 권고 순서는 **의존성 + 인프라 선행** 기준 적용:

| 순위 | T | severity 분포 | 선택 근거 |
|---|---|---|---|
| 1순위 | T06 | KI-011~016 + KI-018~019 8 LOW | **인프라 선행** — IndexingService wiring 완료 (T05) 직후 perf 측정 정확. 다른 T04/T02 진행 후 측정해도 결과 동일하나 baseline 측정 빠를수록 후속 hotfix 판단 빠름 |
| 2순위 | T04 | KI-004 MEDIUM | **독립 가능** — MEDIUM 우선 + ChatService 호출자 영향 작음. T06 동시 진행 가능 (다른 모듈) |
| 3순위 | T02 | KI-006 MEDIUM | **T03c stash/restore wiring 결합 우선** — activeTabByWorkspace cleanup + IndexingService.abort + EmbeddingQueue.clear + ChatService.abortStreaming 3종 callback 동시 박힘 |
| 4순위 | T01 잔여 | KI-001 MEDIUM in-progress | T05 동반 가능 / 별도 ci PR — windows-latest + macos-intel matrix + Electron 39 native ABI rebuild |

**severity vs 의존성 trade-off 시점에 의존성/인프라 선행 정합** (PR #175 학습 §15.7.3 동일).

### 2.1 권고 1순위 — T06 (perf bench infra 8종)

본 T05 종료로 T06 의존성 충족 (IndexingService wiring 완료, 인덱싱 perf 측정 가능).

- `tests/perf/*.bench.ts` 6 파일 신규:
  - `indexing.bench.ts` (KI-012 < 500ms / 페이지)
  - `search.bench.ts` (KI-013 < 200ms top-10)
  - `workspaceSwitch.bench.ts` (KI-014 < 1초 / 10 탭)
  - `embeddingCost.bench.ts` (KI-015 < $3/월)
  - `storageSize.bench.ts` (KI-016 < 200MB/만)
  - `memoryStats.bench.ts` (KI-011 < 20ms)
- `vitest.config.ts` bench include 패턴 추가 (`tests/perf/**/*.bench.ts`)
- `package.json` script `"perf": "vitest bench --run"`
- 정확도 회귀 (top-10 hit rate ≥ 80% KI-018 / AI 출처 정확도 ≥ 90% KI-019) 시나리오 30 케이스 정확도 산식 명시
- 실측 결과 보고서 `.flowset/eval-results/sprint-016-perf-bench.md`

### 2.2 권고 2순위 — T04 (KI-004 MEDIUM response_format JSON API-level)

- `ChatRequest.responseFormat?: 'text' | 'json_object'` 신규
- `OpenAIApiKeyProvider` body 에 `response_format: { type: 'json_object' }` 전달
- AutoTagger 가 `responseFormat: 'json_object'` 지정

### 2.3 권고 3순위 — T02 (KI-006 MEDIUM Workspace 전환 abort 정책)

- `WorkspaceHandlerDeps` 에 abort callback 3종 추가 (`abortIndexing` / `clearEmbeddingQueue` / `abortChatStreaming`)
- `handleWorkspaceSwitch` 진입 시 호출 — T03c stash/restore wiring 과 결합 (활성 ws 의 IndexingService 인스턴스화는 본 T05 로 완료)
- T03c evaluator NB-2 (activeTabByWorkspace stash 의 ws 삭제 cleanup 부재) 흡수 권고

### 2.4 권고 4순위 — T01 잔여 + KI-001 후속

- windows-latest + macos-intel matrix 통합 PR + required check 승격
- Electron 39 native ABI rebuild PoC (`electron-rebuild -f -w better-sqlite3 -v 39.x.x` + Electron 런타임 sqlite-vec 재load)
- KI-001 in-progress → closed 후보

### 2.5 KI-020 후속 (LOW, 즉시성 낮음)

- SPA `did-navigate-in-page` hook 추가 — Sprint 016 M1 (시나리오 2) cover 시점 또는 후속 hotfix
- debounce 500ms~1s + iframe skip
- T04/T02/T06 보다 우선순위 낮음 (시나리오 cover 시점에 자연 흡수)

---

## 3. 본 세션 컨텍스트 / 다음 세션 첫 행동 (mini-milestone β 진입 기준)

본 §3 은 PR #176 시점 (β 진입 결정 전) 작성. 본 세션 종료 시점 결정은 **§6 final + §7 final** 기준. 다음 세션 첫 행동은 아래 5단계 적용:

1. `.flowset/state.md` L8~L13 메타 + 진입점 (β 0순위 명시) 확인
2. 본 핸드오프 **§5/§6/§7 final** 정독 (§1~§4 는 PR #176 시점 기준 — 사실 보존용)
3. `.claude/settings.json` 현재 상태 (SessionStart echo 1줄) 정독 + 글로벌 CLAUDE.md v3.4 7개 시스템 통합 명세 비교
4. `.flowset/contracts/sprint-016.md` §2 (T06 잔여 대기) + `.flowset/guardrails.md` G-011~G-015 본체 (mini-milestone α 등록분) 확인
5. **mini-milestone β 진입 결정** + 브랜치 `feat/WI-S016M0-feat-flowset-hooks-automation` 생성 (또는 codex 재협의 후 분할 결정)

---

## 4. 본 세션 마무리 (PR #176 시점)

Sprint 016 M0 T05 PR #176 머지 완료 — **KI-010 LOW closed** + **KI-020 LOW 신규 등록** (KI 잔여 동수 17 유지). 단위 1089 → **1101 (+12)**. **dual review 1회 (사전, evaluator Pass 8/8 + codex NEEDS_CHANGES 1)** + 본 PR 내 hotfix 1회 (NEEDS_CHANGES #7 + NB-5 흡수). PRD §07.4.2 broadcast 3종 전부 cover.

**다음 세션 진입: T06 (권고 1순위)** — perf bench infra 8종. T05 종료로 의존성 충족.

---

## 5. 본 세션 후속 — PR #177 + #178 + #179 + 사용자 본질 지적 (mini-milestone α)

§4 작성 직후 PR #177 (T05 docs 후속) 진행 시 사용자 본질 지적 발생 — 본 세션 G-006 위반 2회 + 학습 #8 위반 시도 1회 재현 인지 후 codex 협의 → 옵션 D (mini-milestone α/β 분할) 채택 → α 진행.

### 5.1 PR 시퀀스 (§4 이후)

| PR | commit | type | 산출물 | dual review | 처리 |
|---|---|---|---|---|---|
| **#177** | `e68eb8a` | docs | T05 머지 후속 — KI-010 closed 전환 + KI-020 신규 + 핸드오프 §1~§4 (`2026-05-20.md`) + state.md 메타 갱신 | evaluator Pass 7 / NB 1 + codex PASS 5 / NEEDS_CHANGES 1 (§2 rationale) / NB 2 (모델 ID 문구 / 77 PR 후속) | 본 PR 내 hotfix 1회 — §2.0 rationale 분리 + 학습 #5 환경 한정 보강 |
| **#178** | (closed) | docs | 학습 #5 정정 (`gpt-5.5-codex` 추측) | 미실행 (학습 #8 위반 시도) | 사용자 2차 지적 후 닫음 — mini-milestone α 흡수 |
| **#179** | `67f9c4d` | docs | mini-milestone α — README/CLAUDE/USAGE/docs/prd README 정합 + .codex-tmp 정리 + handoff §1.5 학습 #5 실측 정정 + guardrails G-012~G-015 본체 등록 + state.md L13 β 우선순위 박음 | evaluator Pass 6 / Partial 2 + codex PASS 4 / NEEDS_CHANGES 6 / NB 1 | 본 PR 내 hotfix 1회 — 7건 흡수 (AI 채팅 retrieval 표기 정정 / Ctrl+Shift+N 제거 / preset 12종 실측 정정 / 파일명 정정 / KI 표 17건 정확 / state·handoff β 박음 / guardrails G-012~G-015 본체 등록) |

### 5.2 사용자 본질 지적 인지

> "지금 디렉토리 구조자체도 개판이고, 워크플로우 셋팅도 안되어있는것 같은데 자체 정책같은게 다빠져있고 그래서 그런지 계쏙 틀어지는 기분인데"

실측 결과 (codex 협의 입력):
1. 루트 README.md — Sprint 014 시점 freeze (Phase 1 페이지 번역 MVP 광고)
2. CLAUDE.md (프로젝트) — Phase 0 표기
3. `.claude/settings.json` — SessionStart echo 1줄만 작동, PreToolUse/PostToolUse/Stop hook 모두 부재
4. `.claude/agents/` — evaluator/lead-workflow 정의됐으나 hook 강제 path 0
5. `.flowset/ownership.json` — 존재하나 활용 hook 0
6. `.codex-tmp/` — 미정의, .gitignore 미등재
7. `feedback_dual_review` 표준 자동 강제 0
8. docs/USAGE.md / docs/prd/README.md — 갱신 부족

본질: 자체 정책이 문서에만 있고 자동 강제 부재. 한 사람 (Claude) 이 잊으면 그대로 틀어짐.

### 5.3 codex 협의 옵션 D 채택

A (1~2주 정지) 과함 / B-C (자동 강제 부재 Sprint 017 방치) 위반 재현 위험 → **D (2 PR mini-milestone α/β + Sprint 016 재개)** 균형.

- **α (PR #179 완료)**: README/CLAUDE/USAGE/PRD/handoff 정합 + .codex-tmp 정리 + guardrails 본체 등록 + state/handoff β 우선순위 박음
- **β (다음 세션 0순위)**: hooks 자동화 + CI `flowset-policy-check` + PR template

### 5.4 본 세션 통합 학습

1. **G-006 위반 2회 재현** — `gpt-5.2-codex` (1차) + `gpt-5.5-codex` (정정 시 2차) 모두 추측. 실측 (`~/.codex/config.toml model = "gpt-5.5"`) 후 정정. **schema description 예시 ≠ 환경 디폴트, 사용자 환경 실측 후 작성 표준**.
2. **학습 #8 위반 시도 1회** — PR #178 만들 때 "사전 dual review 필요 여부 사용자 판단 위임" 표기. 학습 #8 자체가 "강제, 누락 절대 금지" 인데 본인 위반 시도. 사용자 지적 후 PR 닫음.
3. **운영 실수 PR 사이클 비용 과잉** — 학습 #5 1줄 정정에 별도 PR + dual review + 머지 사이클은 비효율. codex 권고대로 mini-milestone α 로 흡수하는 게 표준.
4. **자동 강제 부재 = 본질 원인** — SessionStart echo 1줄만 작동. dual review 자동 게이트 / G-006 자동 grep / ownership 강제 hook 0 → Claude 단독 수동 강제 의존 → 잊으면 재현. mini-milestone β 진입 전까지 같은 패턴 재발 가능.
5. **codex 협의 효율** — 사용자 본질 지적 → codex 협의 (실측 기반 8건 진단 + 옵션 A/B/C/D 채택 권고 + hook 설계 + 우선순위 P0/P1/P2 + 분량 추정) → 옵션 D 균형 채택. 자체 결정보다 codex 협의가 빠르고 정확.
6. **사전 dual review 효과 재확인** — PR #176 codex NEEDS_CHANGES 1 + NB 3 / PR #177 codex NEEDS_CHANGES 1 + NB 2 / PR #179 codex NEEDS_CHANGES 6 + NB 1 — 매 PR 사전 평가에서 본문 hotfix 흡수. 사후 발견 시 후속 PR 비용 누적이라 사전 평가가 효율.

### 5.5 누적 변동 (본 세션 종료 시점)

- **단위 테스트**: 1089 → **1101 (+12)** (PR #176 본문 변경, #177/#179 docs only 영향 없음)
- **KI 잔여**: 17 → **17** (KI-010 LOW closed -1 + KI-020 LOW 신규 +1 동수, HIGH 1 / MEDIUM 3 / LOW 13)
- **closed 누적**: KI-007 / KI-010 / KI-017 3건 (Sprint 016 M0 T03·T05)
- **PR 누적**: 75 → **79** (#176/#177/#179 머지 + #178 닫음)
- **가드레일**: G-011 → **G-015** (G-012/G-013/G-014/G-015 본체 정식 등록, contracts §5 분리분 흡수)
- **dual review 횟수**: 본 세션 3회 (PR #176/#177/#179 모두 사전 호출) + PR 내 hotfix 3회 (각 PR 흡수)

### 5.6 본 세션 종료 검증 (2026-05-20 PR #179 머지 후)

| 검증 | 결과 |
|---|---|
| `npm run typecheck` | PASS (node + web) |
| `npm run lint` | PASS |
| `npm test` | 1101 PASS / 67 files |
| `npm run build` | 미실행 (typecheck 통과 + lint 통과 + test 통과로 충분) |
| git status main | clean (브랜치 분기 전 시점) |
| 머지 commit 검증 | PR #176 cae365e / #177 e68eb8a / #179 67f9c4d 모두 main 동기화 확인 |

---

## 6. 다음 세션 진입 권고 (final)

### 6.0 권고 0순위 — mini-milestone β (hooks 자동화)

**최우선**. 본 세션 위반 패턴 재발 차단 강제. codex 협의 결과 채택 (옵션 D).

산출물:
- `.claude/settings.json` SessionStart 실 주입 — state.md / 최신 handoff / guardrails.md / docs/prd README 실 출력
- PreToolUse hook — `gh pr create/merge` / `git commit` / `git push` 전 dual review 체크파일 또는 PR body 체크박스 게이트
- PostToolUse hook — 문서 변경 감지 시 README/CLAUDE/USAGE/PRD 동기화 검사
- Stop hook — ownership.json 소유권 / G-006 위험 표현 grep / dual review 누락 / handoff·state 갱신 여부 점검
- CI `flowset-policy-check` job (hook 만으로는 에이전트 우회 가능)
- `.github/PULL_REQUEST_TEMPLATE.md` (dual review 체크박스 + 변경 영향 명시)

분량 150~400 LOC (codex 추정).

### 6.1 권고 1순위 — T06 (perf bench infra 8종, β 의존)

본 T05 종료로 IndexingService wiring 완료 → perf 측정 baseline 정확. KI-011~016 + KI-018~019 8 임계 일괄 측정.

### 6.2 권고 2순위 — T04 (KI-004 MEDIUM response_format JSON API-level)

### 6.3 권고 3순위 — T02 (KI-006 MEDIUM Workspace 전환 abort 정책)

### 6.4 권고 4순위 — T01 잔여 + KI-001 후속

### 6.5 KI-020 후속 (LOW, 시나리오 cover 시점 자연 흡수)

---

## 7. 본 세션 종료 핸드오프 (final, §8 진입 전)

**Sprint 016 M0 T05 종료 + mini-milestone α 완료**. 사용자 본질 지적 → codex 협의 → 옵션 D 채택 → α 진행 → β 다음 세션 위임. 자동 강제 부재 본질 원인 인지 + 다음 세션 0순위 명시 박힘.

**다음 세션 진입: mini-milestone β** (권고 0순위) — hooks/CI/PR template. β 완료 후 T06 진입.

> 본 §7 은 §8 진입 전 시점 기록. §8 (PR #181 머지 후) 가 본 세션 최종.

---

## 8. mini-milestone β 완료 (PR #181 머지, 2026-05-20 후속 세션)

### 8.1 진입 결정

본 세션 시작 시 핸드오프 §3 다음 세션 첫 행동 5단계 + §6.0 권고 0순위 그대로 적용 — mini-milestone β 즉시 진입. 사용자 본질 지적 ("앞 세션에서 다 정해놨는데 왜 또 묻느냐") 후 진입 방식 추가 확인 절차 생략. 브랜치 `feat/WI-S016M0-feat-flowset-hooks-automation` 분기.

### 8.2 산출물 (PR #181 `da5041b`)

| 파일 | 변경 | 비고 |
|---|---|---|
| `.flowset/hooks/session-start.mjs` | +104 (신규) | state.md 30줄 + 최신 핸드오프 §6/§다음 세션 80줄 + guardrails [active] 헤더 + KI 잔여 통계 + 표준 환기 stdout 실주입 |
| `.flowset/hooks/pre-tool-use.mjs` | +97 (신규) | main push G-007 차단 (exit 2) / `.flowset/` 핵심 파일·디렉토리 삭제 wi-flowset §3 차단 (exit 2, forward + Windows backslash) / git commit G-009 형식 경고 / gh pr dual review 환기 |
| `.flowset/hooks/post-tool-use.mjs` | +105 (신규) | ownership.json 매핑 / PRD·v04-direction·CLAUDE.md·handoffs·guardrails 변경 시 동기화 권고 |
| `.flowset/hooks/stop.mjs` | +102 (신규) | 오늘 핸드오프 / state.md 최근 갱신 / 최근 commits G-009 / 미머지 브랜치 / dual review 환기 |
| `.flowset/hooks/README.md` | +47 (신규) | 설계 원칙 + `.ps1` → `.mjs` 전환 학습 (Windows PowerShell 5.1 cp949 한계) |
| `.claude/settings.json` | +38 / -3 | 4 hook 매처 등록 + `${CLAUDE_PROJECT_DIR}` 절대 경로 (codex review #2 흡수) |
| `.github/workflows/flowset-policy-check.yml` | +141 (신규) | PR body `## Dual Review` 섹션 + 체크박스 + 영향 영역 + 제목 G-009 + G-005 secret grep 강제 CI job |
| `.github/PULL_REQUEST_TEMPLATE.md` | +30 / -11 | Dual Review 섹션 + 가드레일 12종 + 검증 결과 + KI 변동 양식 |

총 8 files / +664 / -14.

### 8.3 dual review 결과

- **evaluator (사전)**: **Pass 7 / Partial 1 / Fail 0 / BLOCKING 0 / NEEDS_CHANGES 0 → PASS**
  - 채점 항목 8: 핸드오프 §5.3/§6.0 산출물 6종 / 글로벌 CLAUDE.md v3.4 Hook 분담 / G-007 차단 PoC / G-009 브랜치명 / G-010 UTF-8+LF+BOM 없음 / CI 게이트 5종 / `.flowset/hooks/` 자체 작업 가능성 / dual review 자동 강제 설계
  - Partial 1: G-009 커밋 메시지 (커밋 미생성 → 본 commit 시점 자동 해소)
  - 메타-검증: 평가 진행 중 본 PreToolUse hook 이 평가용 Bash 명령 자체를 exit 2 차단 → hook live 작동 추가 확증
- **codex (사전, `/codex:rescue` slash, model 생략 → config.toml `gpt-5.5` 자동 적용 — 학습 #5)**: **PASS 3 / NEEDS_CHANGES 3 / BLOCKING 1 / NB 0 → 머지 불가**
  - PASS 3: Node.js hook 4개 spec 정합 / PR template / `.ps1` → `.mjs` 전환
  - NEEDS_CHANGES #2: settings.json Stop matcher 미지원 + PATH 상대 경로
  - NEEDS_CHANGES #3: flowset-policy-check.yml `## Dual Review` 섹션 존재 미체크 + G-005 grep `.md` 제외
  - **BLOCKING #5**: pre-tool-use.mjs 정규식 — README.md 누락 / `rm -rf .flowset` 전체 삭제 미적용 / Windows backslash 경로 우회 가능
  - NEEDS_CHANGES #7: hook 우회 가능성 + README.md 무조건 표기

### 8.4 본 PR 내 hotfix 흡수 (4건)

| codex # | 파일 | 정정 내용 |
|---|---|---|
| #2 | `.claude/settings.json` | Stop hook `matcher` 필드 제거 (spec 미지원) + 모든 hook command `node "${CLAUDE_PROJECT_DIR}/.flowset/hooks/..."` 절대 경로화 |
| #3 | `.github/workflows/flowset-policy-check.yml` | `^## +Dual Review` 섹션 존재 grep 추가 (line 36-40) + G-005 grep glob `\.(ts|tsx|js|jsx|md|mjs|yml|yaml|json)$` 로 확장 (line 121) |
| **#5** | `.flowset/hooks/pre-tool-use.mjs` | (a) 정규식 2분할 — A: `.flowset/<core>` (forward/backslash 양쪽) + B: `.flowset` 전체 디렉토리 (`rm -rf .flowset`) / (b) `corePathChars` 에 `README.md` + `PROMPT.md` + `logs` 추가 / (c) 명령어 시작 anchor `^\s*` + sub-command 분할 (`;` `&&` `\|\|`) — quote 내부 string literal false-positive 회피 (14/14 case 정합) |
| #7 | `.flowset/hooks/README.md` + `.github/workflows/flowset-policy-check.yml` | required check 승격 경로 명시 — repo Settings → Branches → Branch protection rule (main) → "Require status checks" 에 `FlowSet Policy Check / policy-check` 추가. 본 설정 전 hook 우회 시 머지 차단 안 됨 (조건부 표기) |

### 8.5 본 PR 자기 검증 루프 (첫 동작)

본 PR 머지 시점에 본 CI `flowset-policy-check` 가 첫 실 게이트 동작. 본 PR body 자체가 본 PR template 양식 + Dual Review 체크박스 [x] 표시 + 영향 영역 3개 (코드 / 문서 / CI) + 제목 형식 정합 → **자기 검증 통과**.

CI 4 job 결과 (모두 SUCCESS):
- `FlowSet Policy Check / policy-check` (신규, 첫 동작)
- `Commit Check / 커밋 메시지 형식 검증`
- `CI / typecheck + test + build`
- `CI / macOS sqlite-vec PoC (KI-001)`

### 8.6 누적 변동 (PR #181 머지 후)

- **단위 테스트**: 1101 → **1101** (인프라 PR, 본문 코드 변경 0)
- **KI 잔여**: 17 → **17** (변동 없음 — 본 PR 은 KI cover 아닌 인프라 신규)
- **PR 누적**: 79 → **80** (#181 `da5041b` 머지)
- **가드레일**: G-011 → G-015 동수 (자동 강제 path 활성, 새 가드레일 등록 없음)
- **자동 강제 path**: SessionStart echo 1줄 → **4 hook (.mjs) + CI flowset-policy-check + PR template + settings.json `${CLAUDE_PROJECT_DIR}` 절대 경로 + branch protection required check 승격 권고 박음**
- **dual review 횟수**: 본 세션 누적 4회 (PR #176/#177/#179 + #181 모두 사전 호출) + PR 내 hotfix 4회 (각 PR 흡수)

### 8.7 본 세션 학습 (β 진입 후)

1. **`.ps1` → `.mjs` 전환** — Windows PowerShell 5.1 은 BOM 없는 UTF-8 `.ps1` 을 cp949 로 해석. wi-utf8.md "BOM 없음" 정책 유지 + 한글 메시지 안전 출력 위해 Node.js 채택. Electron 프로젝트라 의존 0 추가. 향후 hook / CLI 스크립트 모두 `.mjs` 표준.
2. **정규식 false-positive 회피** — `[^|;&]*` greedy 매칭이 quote 내부 string literal 까지 잡음 (본 PR 디버깅 명령 자체가 차단되는 메타 사례). 해결: (a) 명령어 시작 anchor `^\s*` (b) sub-command 분할 (`;` `&&` `\|\|`) 후 각각 검사. 14/14 case PoC 정합 후 적용.
3. **codex dual review BLOCKING 본 PR 내 흡수 효율** — codex BLOCKING 1 + NEEDS_CHANGES 3 모두 본 PR 내 hotfix 로 흡수. 사후 발견 시 후속 PR 비용 누적이라 사전 평가 효율 재확인 (PR #176/#177/#179 동일 패턴).
4. **자기 검증 루프** — 본 PR 자체가 본 CI `flowset-policy-check` + commit-check + typecheck+test+build 4 job 모두 통과. 향후 hook 정비 / CI 강화 PR 도 동일 양식 통과 강제. 우회 path 0.
5. **사용자 본질 지적 즉시 반응** — 본 세션 진입 시 "다 정해놨는데 왜 또 묻느냐" 지적 후 추가 확인 절차 생략. 핸드오프 §3/§6.0 박힌 결정사항을 그대로 적용. 다음 세션도 동일 패턴 — 핸드오프에 박힌 결정사항은 추가 협의 없이 즉시 진입.
6. **`/codex:rescue` slash 우선 학습 #5 표준** — 본 세션 1차 시도 시 raw MCP 직접 호출 (학습 #5 위반 재현) → 사용자 지적 후 `/codex:rescue` 로 정정. 학습 #5 자체가 "자동 강제 부재 → 본 세션도 재현" 사례 → 본 PR 머지 후 SessionStart hook 의 표준 환기 line 으로 차후 세션 진입 시 자동 알림 (재현 차단 강화).

### 8.8 다음 세션 진입 권고 (β 종료 후 갱신)

#### 권고 0순위 — T06 (perf bench infra 8종)

본 mini-milestone β 종료로 **다음 세션 진입은 T06 으로 승격**. 자동 강제 path 활성 + 본 PR 자기 검증 루프 첫 성공으로 G-006 / 학습 #8 재발 차단 박힘.

T06 산출물 (Sprint 016 contract §6 매트릭스 8종):
- `tests/perf/indexing.bench.ts` (KI-012 < 500ms/페이지)
- `tests/perf/search.bench.ts` (KI-013 < 200ms top-10)
- `tests/perf/workspaceSwitch.bench.ts` (KI-014 < 1초/10 탭)
- `tests/perf/embeddingCost.bench.ts` (KI-015 < $3/월/1만 페이지)
- `tests/perf/storageSize.bench.ts` (KI-016 < 200MB/1만 페이지)
- `tests/perf/memoryStats.bench.ts` (KI-011 < 20ms)
- 정확도 회귀 (KI-018 top-10 hit rate ≥ 80% / KI-019 AI 출처 정확도 ≥ 90%) — 시나리오 30 케이스 정확도 산식
- `vitest.config.ts` bench include `tests/perf/**/*.bench.ts` 추가
- `package.json` script `"perf": "vitest bench --run"`
- `.flowset/eval-results/sprint-016-perf-bench.md` 실측 보고서

#### 권고 1순위 — T04 (KI-004 MEDIUM response_format JSON API-level)

#### 권고 2순위 — T02 (KI-006 MEDIUM Workspace 전환 abort 정책)

#### 권고 3순위 — T01 잔여 (windows-latest + macos-intel matrix + Electron 39 ABI rebuild)

#### KI-020 후속 (LOW)

SPA `did-navigate-in-page` hook 추가 — Sprint 016 M1 (시나리오 2) cover 시점 자연 흡수.

#### 후속 manual 작업 (codex review #7)

**branch protection required check 승격** — repo Settings → Branches → Branch protection rule (main) → "Require status checks to pass before merging" 에 `FlowSet Policy Check / policy-check` 추가. 본 설정 없으면 hook 우회 시 PR 머지 차단 안 됨. 다음 세션 진입 시 사용자 manual 적용 권고.

### 8.9 본 세션 종료 검증 (2026-05-20 PR #181 머지 후)

| 검증 | 결과 |
|---|---|
| `npm run lint` | PASS |
| `npm run typecheck` | PASS (node + web) |
| `npm test` | 1101 PASS / 67 files (인프라 PR, 변동 0) |
| CI `FlowSet Policy Check / policy-check` | **SUCCESS (첫 동작)** |
| CI `Commit Check / 커밋 메시지 형식 검증` | SUCCESS |
| CI `CI / typecheck + test + build` | SUCCESS |
| CI `CI / macOS sqlite-vec PoC (KI-001)` | SUCCESS |
| git status main | clean (PR #181 머지 후 fast-forward) |
| 머지 commit | `da5041b4c600218bb1a87ff5e04eee3a999af6ac` |

### 8.10 본 세션 최종 종료 핸드오프 (§9 진입 전)

**Sprint 016 M0 T05 종료 + mini-milestone α 완료 + mini-milestone β 완료**. 자동 강제 path 활성 — `.claude/settings.json` SessionStart echo 1줄 → 4 hook (.mjs) + CI flowset-policy-check + PR template + settings.json `${CLAUDE_PROJECT_DIR}` 절대 경로. 본 PR 자체가 자기 검증 루프 첫 통과 케이스.

**다음 세션 진입: T06** (perf bench infra 8종) — IndexingService wiring 완료 (T05) 직후 baseline 측정 정확. β 머지로 G-006 / 학습 #8 재발 차단 박힘.

> 본 §8.10 은 §9 진입 전 시점 기록. §9 (PR #183 T06 머지 후) 가 본 세션 최종.

---

## 9. T06 perf bench infra 완료 (PR #183 머지, 2026-05-20 후속 세션)

### 9.1 진입 결정

mini-milestone β (PR #181) 머지 직후 §8.8 권고 0순위 T06 즉시 진입. β 머지로 자동 강제 path 활성 (4 hook + CI flowset-policy-check) → T06 진행 중 본 PR 도 자기 검증 루프 3회차 동작.

브랜치 `feat/WI-S016M0-feat-perf-bench-infra` 분기 → 산출물 작성 → manual measurement 실측 → dual review → 머지.

### 9.2 산출물 (PR #183 `3ee750d`, 14 files / +1087 / -1)

| 파일 | 변경 | 비고 |
|---|---|---|
| `vitest.config.ts` | +5 / -1 | `benchmark.include: ['tests/perf/**/*.bench.ts']` + reporters 추가 (test 와 분리) |
| `package.json` | +1 | `"perf": "vitest bench --run"` script |
| `tests/perf/memoryStats.bench.ts` | +73 (신규) | KI-011 — MemoryService.getStats < 20ms (1K + 10K pages) |
| `tests/perf/indexing.bench.ts` | +66 (신규) | KI-012 — IndexingService.indexPage < 500ms (1KB body) |
| `tests/perf/search.bench.ts` | +82 (신규) | KI-013 — SearchService.search < 200ms top-10 (1000 pages + 임베딩) |
| `tests/perf/workspaceSwitch.bench.ts` | +74 (신규) | KI-014 — WorkspaceService.setActive < 1초 (10 ws × 10 tabs) |
| `tests/perf/embeddingCost.bench.ts` | +39 (신규) | KI-015 — estimateMonthlyCostUsd < $3/월 |
| `tests/perf/storageSize.bench.ts` | +84 (신규) | KI-016 — SQLite 파일 크기 < 200MB (10K + 임베딩) |
| `tests/perf/embeddingCostHelpers.ts` | +14 (신규) | 비용 산식 + 임계 상수 |
| `tests/integration/scenarios/accuracyHelpers.ts` | +82 (신규) | KI-018 topKHitRate + KI-019 aiSourcesPrecision 산식 + 임계 (0.8 / 0.9) |
| `tests/unit/scenarios/accuracyHelpers.test.ts` | +95 (신규) | 12 it 산식 회귀 (topK 6 + AI 6) |
| `tests/integration/perf-baseline.test.ts` | +268 (신규) | 7 case `performance.now()` manual measurement + `expect(...).toBeLessThan(threshold)` 강제 (vitest 2.1.x bench mode 한계 회피) |
| `tests/unit/perf/embeddingCost.test.ts` | +43 (신규) | 5 it 비용 산식 회귀 |
| `.flowset/eval-results/sprint-016-perf-bench.md` | +149 (신규) | 실측 보고서 (8종 매트릭스 + 마진 + KI status 변동 권고) |

### 9.3 실측 결과 (perf-baseline.test.ts 7/7 PASS)

| KI | 임계 | 실측 mean | 마진 |
|---|---|---|---|
| KI-011 1K pages | < 20ms | **0.046ms** | 434× |
| KI-011 10K pages | < 20ms | **0.447ms** | 44.7× |
| KI-012 indexing | < 500ms | **0.027ms** | 18,500× |
| KI-013 search (1000p) | < 200ms | **1.404ms** | 142× |
| KI-014 ws switch | < 1초 | **0.283ms** | 3,500× |
| KI-015 cost | < $3/월 | **$0.20** | 15× |
| KI-016 storage (10K) | < 200MB | **90.32MB** | 2.2× |

**6/8 PASS** (perf 6 직접 측정) + **2/8 DEFERRED** (정확도 KI-018/019 산식만 박음, 실 30 케이스 cover 는 T07~T08 시점).

### 9.4 dual review 결과

- **evaluator (사전)**: **Pass 7 / Partial 2 / Fail 0 / BLOCKING 0 / NEEDS_CHANGES 0 → PASS**
  - 채점 항목 9: contract §6 8종 cover / 임계 vs 실측 정확성 / infra 박음 완성도 / 정확도 헬퍼 정합 / vitest bench mode 한계 회피 / KI status 변동 권고 / G-009 PR 제목 / G-007 main 직접 push 없음 / 자기 검증 루프
  - Partial 2: 항목 7 (PR 미생성 시점) + 항목 9 (codex 병렬 호출 누락 우려 — 본 호출로 자연 해소)
  - NB-4: 보고서 §2.7/§2.8 "6 케이스 산식 sanity PASS" 표기 → "topKHitRate 6 it + aiSourcesPrecision 6 it = 총 12 it" 정확 표기 권고
- **codex (사전, `/codex:rescue` slash, model 생략 → config.toml `gpt-5.5` 자동 적용)**: **9 axis 전체 PASS / BLOCKING 0 / NEEDS_CHANGES 0 → PASS**
  - Axis 1~9 모두 근거 파일/라인 명시 + 임계 정합 확인

### 9.5 본 PR 내 hotfix 흡수

| evaluator NB | 파일 | 정정 내용 |
|---|---|---|
| NB-4 | `.flowset/eval-results/sprint-016-perf-bench.md` §2.7/§2.8 | "6 케이스 산식 sanity PASS" → topKHitRate 6 it (빈셋/top-1/부분/any-hit/k 제한/임계 상수) + aiSourcesPrecision 6 it (빈셋/모두 일치/hallucination/citedSources 0/전체 0/임계 상수) = 총 12 it 정확 표기 |

### 9.6 본 PR 자기 검증 루프 (3회차 동작)

PR #181 (β) 머지로 활성화된 CI `flowset-policy-check` 가 본 PR 도 게이트. 본 PR body 양식 + Dual Review 체크박스 [x] + 영향 영역 2개 (코드 + 문서) + 제목 G-009 형식 정합 → **자기 검증 통과 (3회차)**.

CI 4 job 결과 (모두 SUCCESS):
- `FlowSet Policy Check / policy-check` (3회차)
- `Commit Check / 커밋 메시지 형식 검증`
- `CI / typecheck + test + build`
- `CI / macOS sqlite-vec PoC (KI-001)`

### 9.7 vitest 2.1.x bench mode 한계 학습

본 T06 진행 중 vitest bench mode 의 sqlite-vec + `beforeAll` 시드 조합에서 **samples 0 측정 한계** 확인:
- 시도: `tests/perf/memoryStats.bench.ts` `beforeAll(seedNK)` + `bench(getStats)` → ✓ 표시 + samples 0 / hz 0.0000
- 동일 패턴 다른 bench (embeddingCost — setup 없음) 는 samples 13M+ 정상 측정
- 원인 추정: bench fn 의 `fx` closure 의 native module (sqlite-vec) tinybench scheduler 호환 이슈
- 우회: vitest test 모드 + `performance.now()` manual loop (`tests/integration/perf-baseline.test.ts`) → 7/7 측정 성공 + `expect().toBeLessThan()` 강제

후속: vitest 2.2+ 또는 tinybench 직접 활용 시 bench mode 복원 가능 (KI 신규 등록 없음 — 우회 path 완료).

### 9.8 누적 변동 (PR #183 머지 후)

- **단위 테스트**: 1101 → **1125** (+24 신규 — embeddingCost 5 + accuracyHelpers 12 + perf-baseline 7)
- **KI 잔여**: 17 → **17** 동수 (closed 전환은 Sprint 016 T25 종합 evaluator 시점 — KI-011/012/013/015/016 5종 closed 후보)
- **PR 누적**: 80 → **82** (#181 β + #182 §8 docs + #183 T06 머지)
- **가드레일**: G-015 동수
- **dual review 횟수**: 본 세션 누적 5회 (PR #176/#177/#179/#181/#183 모두 사전 호출) / PR 내 hotfix 6회 흡수
- **자기 검증 루프**: 본 PR 이 3회차 동작 (PR #181/#182/#183)

### 9.9 본 세션 학습 (T06 진입 후 추가)

7. **vitest 2.1.x bench mode 한계 회피 path** — `.bench.ts` 인프라 박음 + 실측은 `.test.ts` manual measurement + `expect` 강제. 두 path 병존 — bench mode 는 vitest 2.2+ 시 복원, 현재는 baseline.test.ts 가 임계 강제 ground truth.
8. **마진 산식 — `threshold / measured`** — KI-012 18,500× / KI-014 3,500× / KI-016 2.2× 같은 마진 표기로 임계 여유 즉시 가시화. 향후 K\I closed 전환 또는 임계 재검토 판단 자료.
9. **codex 9 axis 전체 PASS** — 사전 hotfix 흡수 모범 케이스. evaluator NB-4 1건만 hotfix 흡수, codex 는 BLOCKING/NEEDS_CHANGES 0. dual review 의 두 평가가 상호 보완 + 본 PR 의 dual review 표준 정합 모범.

### 9.10 다음 세션 진입 권고 (T06 종료 후 갱신)

#### 권고 0순위 — T04 (KI-004 MEDIUM response_format JSON API-level)

T06 종료로 KI-004 (Sprint 016 contract §2 권고 1순위) 가 다음 0순위로 승격.

산출물:
- `src/ai/types.ts` — `ChatRequest.responseFormat?: 'text' | 'json_object'` 신규
- `src/ai/providers/OpenAIApiKeyProvider.ts` — body 에 `response_format: { type: 'json_object' }` 전달
- AutoTagger 가 `responseFormat: 'json_object'` 지정
- 단위 회귀 (response_format wire-up + AutoTagger 호출 정합)

#### 권고 1순위 — T02 (KI-006 MEDIUM Workspace 전환 abort 정책)

- T03c stash/restore wiring 과 결합 — activeTabByWorkspace cleanup + IndexingService.abort + EmbeddingQueue.clear + ChatService.abortStreaming 3종 callback
- T04 동시 진행 가능 (모듈 독립)
- T02 머지 후 KI-014 재측정 → closed 후보 전환

#### 권고 2순위 — T01 잔여 + KI-001 후속

- windows-latest + macos-intel matrix 통합 PR + required check 승격
- Electron 39 native ABI rebuild PoC

#### 후속 manual (β PR #181 잔여)

- **branch protection required check 승격** — repo Settings → Branches → Branch protection rule (main) → "Require status checks" 에 `FlowSet Policy Check / policy-check` 추가 (codex review #7 후속). 본 PR 까지 hook 우회 시도 0건이라 미설정 상태로도 정상 동작 중이나 정책 차원 강제 권고.

### 9.11 본 세션 종료 검증 (2026-05-20 PR #183 머지 후)

| 검증 | 결과 |
|---|---|
| `npm run lint` | PASS |
| `npm run typecheck` | PASS (node + web) |
| `npm test` | 1125 PASS / 70 files (+24 신규: 비용 5 + 정확도 12 + perf baseline 7) |
| `npm run perf` (vitest bench mode) | partial (samples 0 한계 — 보고서 §3.4 박음) |
| CI `FlowSet Policy Check / policy-check` | **SUCCESS (3회차)** |
| CI `Commit Check / 커밋 메시지 형식 검증` | SUCCESS |
| CI `CI / typecheck + test + build` | SUCCESS |
| CI `CI / macOS sqlite-vec PoC (KI-001)` | SUCCESS |
| git status main | clean (PR #183 머지 후 fast-forward) |
| 머지 commit | `3ee750d494fdaddef84f2ec3268cac49f3086cc7` |

### 9.12 본 세션 최종 종료 핸드오프

**Sprint 016 M0 T05 종료 + α 완료 + β 완료 + T06 완료**. 자동 강제 path 활성 + perf 8종 baseline 박힘 + KI closed 후보 5종 식별. 본 세션 dual review 5회 + PR 내 hotfix 6회 흡수 + 자기 검증 루프 3회차 동작.

**본 docs PR (§9 + state 갱신) 자기 검증 루프 4회차 동작** (PR #181/#182/#183 + 본 PR) — evaluator Partial-1 흡수: 본 docs PR 머지 시 CI `flowset-policy-check` 4회차 통과 강제 + 본 PR 도 PR template 양식 + Dual Review 체크박스 [x] 강제. 자동 강제 path 신뢰도 누적 자료.

**다음 세션 진입: T04** (KI-004 MEDIUM response_format JSON API-level) — KI-014 재측정 의존이 T02 후라 T04 가 다음 0순위. T02 동시 진행도 가능 (모듈 독립).

> 본 §9.12 는 §10 진입 전 시점 기록. §10 (PR #185 T04 + PR #186 T02 + PR #187 T01 잔여 + manual branch protection 승격 모두 완료 후) 가 본 세션 절대 최종.

---

## 10. T04 + T02 + T01 잔여 + manual 후속 완료 (PR #185/#186/#187 머지, 2026-05-20 후속 세션)

### 10.1 진입 결정 — 사용자 단일 요청 "0순위 1순위 2순위 manual 후속까지 진행"

§9 docs PR #184 머지 직후 사용자 요청 "0순위 T04 + 1순위 T02 + 2순위 T01 잔여 + manual 후속 (branch protection) 까지 진행" — 4 작업 순차 진행. 본 turn 에 3 코드/CI PR + 1 manual 적용 + 본 docs PR dual review 포함 8회차 기록 + hotfix 누적 11건 흡수 완료.

### 10.2 manual 후속 (branch protection required check 승격)

§9.10 후속 manual — repo Settings → Branches → main 보호 규칙 → Require status checks 에 `FlowSet Policy Check` 추가 권고. 본 세션에서 `gh api -X POST repos/.../branches/main/protection/required_status_checks/contexts --field 'contexts[]=FlowSet Policy Check'` 호출로 즉시 승격 — 현재 required checks **3종** (`typecheck + test + build` / `커밋 메시지 형식 검증` / `FlowSet Policy Check`). hook 우회 시에도 PR 머지 차단 강제 path 완성.

### 10.3 PR #185 — T04 (KI-004 closed 후보 편입)

| 파일 | 변경 | 비고 |
|---|---|---|
| `src/ai/types.ts` | +14 / -0 | `ChatRequest.responseFormat?: 'text' \| 'json_object'` optional (provider 미지원 silent ignore 주석) |
| `src/ai/providers/OpenAIApiKeyProvider.ts` | +7 / -1 | `chat()` body 에 `response_format: { type: 'json_object' }` wire-up |
| `src/ai/tagging/AutoTagger.ts` | +7 / -1 | `provider.chat(request)` 시 `responseFormat: 'json_object'` 항상 지정 |
| `tests/unit/ai/AutoTagger.test.ts` | +27 / -0 | +3 it (wire-up / 명시적 회귀 / freeform fallback path 유지) |
| `tests/unit/ai/ProviderAdapter.test.ts` | +32 / -0 | +2 it (body json_object wire-up + text 미설정) |

**dual review 결과**: evaluator Pass 6 / Partial 2 (G-013 / PR body 미작성) + codex NEEDS_CHANGES 3 (1 hotfix + 2 자연 해소) / BLOCKING 0 → 본 PR 내 hotfix 1건 흡수 (codex #3 — AutoTagger freeform fallback 명시적 it). 머지 commit `54c1e4b`.

### 10.4 PR #186 — T02 (KI-006 인터페이스, G-013 1단계)

| 파일 | 변경 | 비고 |
|---|---|---|
| `src/main/workspaceHandlers.ts` | +45 / -0 | `WorkspaceHandlerDeps` 에 abort callback 3종 optional (`abortIndexing` / `clearEmbeddingQueue` / `abortChatStreaming`) + `invokeAbortCallback` 헬퍼 (throw swallow + console.warn) + `handleWorkspaceSwitch` 에서 `setActive` 직전 `prevId !== args.id` 조건 호출 |
| `tests/unit/main/workspaceHandlers.test.ts` | +94 / -0 | KI-006 회귀 +5 it (정상 / self-switch skip / throw swallow / 미주입 no-op / invalid args) |

**dual review 결과**: evaluator **Pass 8/8** + codex 7 PASS + 1 NB (CI 미실측 자연 해소) / BLOCKING 0 → 본 PR 내 hotfix 0건. 머지 commit `0ee1b3e`.

**KI-006 status**: 본 PR 머지 후에도 `open` 유지 (또는 `in-progress` 권고) — 인터페이스만 박힘. 실 구현 (`IndexingService.abort` / `EmbeddingQueue.clear` / `ChatService.abortStreaming`) 은 후속 PR (G-013 2단계). T02-followup 머지 후 closed 전환 + KI-014 재측정.

### 10.5 PR #187 — T01 잔여 (KI-001 2차 검증 완성, hotfix 2건)

| 파일 | 변경 | 비고 |
|---|---|---|
| `.github/workflows/ci.yml` | +110 / -14 | macos-poc 확장 (Electron 39 ABI rebuild step + post-rebuild probe step) + macos-intel-poc 신규 (`runs-on: macos-15-intel`) + windows-poc 신규 (`runs-on: windows-latest`, `shell: bash`) |

**dual review 결과**: evaluator Pass 7 / Partial 2 (windows-poc 주석 / CI 미실측) + codex BLOCKING 1 (`macos-13` deprecated) + NEEDS_CHANGES 2 (자연 해소) → 본 PR 내 hotfix 2건 흡수:

- **codex BLOCKING #5**: `runs-on: macos-13` → `macos-15-intel` (commit `9a7a799`). WebFetch `actions/runner-images` README 실측 확인 후 `macos-13 deprecated` 박힘 — **G-006 추측 정정 학습 11** (본 세션 학습 #5 정합, 본인이 GitHub Actions runner 레이블을 추측 후 codex 가 BLOCKING 검출 + WebFetch 실측으로 정정)
- **evaluator Partial-5**: windows-poc 주석 정정 (PowerShell shell 명시 → bash shell 명시 + heredoc 호환 근거)

**1차 push (commit `9a7a799`) CI 결과**: macos-poc 1차 job FAILURE 발견 — 본 PR 의 `electron-rebuild` step 이 better-sqlite3 NODE_MODULE_VERSION 을 Electron 39 ABI 로 교체 → 후속 `Unit tests` (Node 20 환경) 가 ABI mismatch fail. **본 PR 내 hotfix 2건 (commit `2b6115b`)**: step 순서 정정 — Electron rebuild + post-rebuild probe 를 `Typecheck`/`Unit tests` **뒤로** 이동. Node 20 ABI 검증은 typecheck + tests 가 cover, Electron 39 ABI 검증은 마지막 step.

**branch update**: PR #186 머지로 본 PR `BEHIND` 상태 → `gh pr update-branch 187` 실행 후 CI 6 job 재실행 → **6/6 SUCCESS**. 머지 commit `f9d4ae9`.

**KI-001 status**: 3 단계 (macos-arm64 1차 + macos-intel 2차 + windows 2차) 모두 SUCCESS — Sprint 016 T25 종합 evaluator 에서 closed 판단 준비 완료. 단, 본 §10 누적 산식의 **closed 후보 6종**에는 T04/T06 확정 후보만 포함.

### 10.6 본 세션 최종 누적 변동

| 항목 | 본 세션 시작 | 종료 |
|---|---|---|
| **PR 누적** | 79 | **87** (+8: #181/#182/#183/#184/#185/#186/#187 + #178 닫음) |
| **단위 테스트** | 1101 | **1134** (+33: β 0 / T06 +24 / T04 +5 / T02 +5 / 추가 hotfix -1) |
| **KI closed 후보** | — | **6종** (KI-004 / KI-011 / KI-012 / KI-013 / KI-015 / KI-016) — Sprint 016 T25 시점 확정 |
| **KI 잔여 통계** | 17 | **17 동수** (closed 전환은 T25 시점) |
| **KI in-progress** | KI-001 / KI-003 | **KI-001 2차 검증 완료 + KI-006 인터페이스만 박힘** |
| **Required CI checks** | 2종 | **3종** (FlowSet Policy Check 승격) |
| **dual review 사전 호출** | 0 | **누적 8회차** (본 docs PR 포함, manual 1회 제외) |
| **PR 내 hotfix 흡수** | 0 | **누적 11건** (본 §10 범위: #185 1건 + #187 2건, 누적 산식 기준) |
| **자기 검증 루프 통과** | 7회차 | **8회차** (본 docs PR 포함) |
| **자동 강제 path** | echo 1줄 | 4 hook (.mjs) + CI flowset-policy-check + PR template + required check 승격 |
| **perf 8종 baseline** | 0 | **박힘** (KI-011~016 6 측정 + KI-018/019 산식) |
| **branch protection** | 2 required | **3 required (FlowSet Policy Check 승격)** |

### 10.7 본 세션 추가 학습 (10~12)

10. **G-013 단계별 PR 전략 적용 모범 — T02 인터페이스만** — T02 의 abort callback 3종을 인터페이스 (`WorkspaceHandlerDeps` optional) 만 박고 실 구현은 후속 PR 분리. evaluator + codex 모두 G-013 정합 인정. 단일 PR 에 신/구 동시 변경 피함.

11. **G-006 자동 강제 path 검증 — `macos-13` 추측 → WebFetch 실측 정정** — 본인이 GitHub Actions runner 레이블 `macos-13` 사용 시 실측 미확인. codex 가 BLOCKING #5 검출 → WebFetch `actions/runner-images` README 실측 후 `macos-13` deprecated → `macos-15-intel` 정정. dual review 가 G-006 위반을 자동 차단한 모범 사례 — mini-milestone β hooks + CI + PR template + 사전 dual review 4-layer 강제 path 효과 입증.

12. **CI step 순서 회귀 검출 + 자기 검증 루프 동작** — PR #187 1차 push 후 1차 `macos-poc` job FAILURE 발견 (`electron-rebuild` 가 better-sqlite3 NODE_MODULE_VERSION 을 교체 → 후속 Node 20 환경 `Unit tests` fail). 본 PR 내 hotfix 로 step 순서 정정 (rebuild → unit tests **뒤로**). 자기 검증 루프가 회귀를 사전 검출 + 본 PR 내 흡수 강제 — required check 승격 (FlowSet Policy Check) + macos-poc 1차 job FAILURE 가 **continue-on-error: true** 라 머지 차단은 안 되었으나, 본 세션 G-007 정합 (회귀 main 머지 회피) 으로 hotfix 흡수 결정.

### 10.8 다음 세션 진입 권고 (T01 잔여 종료 후 갱신)

#### 권고 0순위 — Sprint 016 M1 (시나리오 2·3 cover, T07~T08)

T06 baseline + T04/T02/T01 잔여 모두 종료 → Sprint 016 M1 진입. 시나리오 2 (PM 경쟁) + 시나리오 3 (학습) 회귀 셋 각 5 케이스 = 10 신규. Sprint 015 시나리오 1·4 (8 케이스) + 본 M1 (10 케이스) + Sprint 016 추가 12 케이스 = **30 케이스 정확도 회귀 셋** (KI-018 top-10 hit rate ≥ 80% + KI-019 AI 출처 정확도 ≥ 90% 산식 적용).

- `tests/integration/scenarios/scenario-2-pm.test.ts` 신규 — S2-C1~C5 5 케이스
- `tests/integration/scenarios/scenario-3-learning.test.ts` 신규 — S3-C1~C5 5 케이스
- `tests/integration/scenarios/accuracyHelpers.ts` (T06 박음) 적용 — `topKHitRate` + `aiSourcesPrecision`
- AC-2 (Sprint 016 contract) — 시나리오 회귀 18/18 (1·4 100% + 2·3 90%+)

#### 권고 1순위 — T02-followup (KI-006 실 구현, G-013 2단계)

T02 (PR #186) 가 인터페이스만 박음 → 실 구현:
- `IndexingService.abort(workspaceId)` 신규 메서드
- `EmbeddingQueue.clear(workspaceId)` 신규 메서드
- `ChatService.abortStreaming(workspaceId)` 신규 메서드
- `services.ts` 에서 callback 3종 주입 wiring (G-013 3단계)
- T02-followup 머지 후 KI-006 closed + KI-014 재측정 → closed 후보 6 → 7

#### 권고 2순위 — Sprint 016 M2 (어댑터 일괄 제거 T09~T13)

- ProviderAdapter.translate() 제거 / executeTranslateRequest → ChatService.chat() 마이그레이션 / TranslationCache + PageResultStore 어댑터 폐기 / fetchImpl 통일

#### 후속 manual (β PR #181 잔여 — 완료)

- **branch protection required check 승격 (FlowSet Policy Check)** — 본 §10.2 에서 `gh api` 호출로 완료 ✓

### 10.9 본 세션 절대 최종 검증

| 검증 | 결과 |
|---|---|
| `npm run lint` | PASS |
| `npm run typecheck` | PASS (node + web) |
| `npm test` | 1134 PASS / 70 files |
| PR #185/#186/#187 머지 commit | `54c1e4b` / `0ee1b3e` / `f9d4ae9` 모두 main fast-forward |
| CI required checks | 3종 모두 SUCCESS (typecheck+test+build / commit-check / FlowSet Policy Check) |
| CI optional PoC checks | 3종 모두 SUCCESS (macos-arm64 1차 / macos-intel 2차 / windows 2차) — KI-001 T25 closed 판단 대기 |
| dual review 표준 (학습 #8) | 본 docs PR 포함 누적 8회차 + 11건 hotfix 흡수 |
| 자기 검증 루프 | 7회차 → **8회차** (본 docs PR) |
| branch protection required | 2종 → **3종 (FlowSet Policy Check 승격)** |

### 10.10 본 세션 절대 최종 종료 핸드오프

**Sprint 016 M0 모든 작업 완료**:
- T05 + α + β + T06 + β docs + T06 docs + T04 + T02 + T01 잔여 + manual 후속 = **9 PR 머지 + 1 manual** (PR #176/#177/#179/#180/#181/#182/#183/#184/#185/#186/#187 = 11 PR 본 세션 누적 시리즈)
- 8 PR 직전 세션 시리즈 (#171~#175) + 본 세션 11 PR = Sprint 016 M0 누적 **19 PR**

**KI status 변동**:
- closed 후보 6종 (KI-004/KI-011/KI-012/KI-013/KI-015/KI-016) — Sprint 016 T25 종합 evaluator 시점 확정
- KI-001 은 macos-arm64 + macos-intel + windows 2차 검증 완료 상태로 T25 종합 evaluator 의 closed 판단 대기
- in-progress / open 유지: KI-003 / KI-006 (인터페이스만 박힘) / KI-014 (T02-followup 후 재측정) / KI-018 / KI-019 (T07~T08 30 케이스 cover 후)

**자동 강제 path 완성**:
- Hook 4 (.mjs) + CI flowset-policy-check + PR template + required check 승격 + dual review 표준 적용 + 본 docs PR 자기 검증 루프 8회차 기록

**본 docs PR dual review (evaluator 병렬, 8개 축)**:

| # | 판정 | 근거 / 정정 |
|---|---|---|
| 1. §10 본문 커버 + hotfix 시퀀스 | PASS | T04 #185 / T02 #186 / T01 #187 / manual §10.2 모두 커버. evaluator NEEDS_CHANGES였던 §10.3 제목과 §10.5 hotfix 건수 표기는 본 PR에서 정정. |
| 2. 누적 산식 | PASS | 단위 1101→1134 (+33), PR 79→87, KI closed 후보 6종, dual review 8회차, hotfix 11건, 자기 검증 7→8회차로 정합. evaluator 지적의 6종/7개 ID 충돌과 hotfix parenthetical 충돌 정정. |
| 3. PR #187 hotfix 시퀀스 | PASS | 1차 `9a7a799` = `macos-13`→`macos-15-intel` + Partial-5 windows-poc 주석 정정. 2차 `2b6115b` = Electron rebuild step 순서 회귀 fix + branch update 후 6/6 SUCCESS. |
| 4. manual §10.2 | PASS | `gh api` 호출로 `FlowSet Policy Check` required 승격 및 required checks 3종 명시. |
| 5. 다음 진입 권고 | PASS | 0순위 Sprint 016 M1, 1순위 T02-followup, 2순위 Sprint 016 M2 순서 유지. |
| 6. state.md L13 정합성 | PASS | 기존 최근 갱신 줄을 중복 유지하지 않고 L12 최종 갱신 / L13 다음 진입점 구조로 정리. |
| 7. 학습 10~12 정합성 | PASS | G-013 단계 분리, G-006 `macos-13` 추측 정정, CI step 순서 회귀 검출이 PR #185~#187 실제 흐름과 일치. |
| 8. PR 자기 검증 8회차 | PASS | 본 docs PR을 자기 검증 루프 8회차로 포함하고 §10.6/§10.9/state.md/최근 핸드오프에 반영. |

**다음 세션 진입: Sprint 016 M1 — 시나리오 2·3 cover** (T07~T08). KI-018/019 회귀 30 케이스 완성 + T02-followup 실 구현 (G-013 2단계) 동시 진행 가능.

> 본 §10 은 PR #188 머지 시점 기록. §11 (PR #188 머지 직후 dual review codex slash 분류 사건 + 자동 강제 path 정정) 이 본 세션 절대 절대 최종.

---

## 11. 학습 #13 — Dual review codex slash 분류 사건 + 자동 강제 path 정정 (2026-05-20 PR #189)

### 11.1 사건 요약

§10 docs PR (PR #188) 사전 dual review 호출 시 본인이 `/codex:rescue` slash command 를 사용. codex agent 가 **review-only 가 아닌 workspace-write 권한**으로 실행되어:
1. 백그라운드 task 로 working tree 직접 수정
2. commit `6ddf09e` (한글 mojibake 제목 "WI-S016M0-docs ?? ???? ?10 ? state ??") 생성
3. remote 에 push 까지 진행 (본인 컨텍스트 외)
4. 본인이 별도 local commit `8281679` 만들고 push 시도 → fast-forward 실패
5. force-with-lease 로 codex commit 덮어쓰기 → 본인 풍부 §10 (evaluator Partial 흡수) 적용 → PR #188 머지 (`d4640b0`)

### 11.2 사용자 본질 지적

> "코덱스가 푸쉬까지한다는건 또 뭔얘긴데 ? 우리 프로세스 자체가 듀얼검증 후에 니가 취합하는거아니었어?"

dual review 표준 절차 = **read-only 평가만 (evaluator + codex 병렬) → Claude 가 결과 취합 → hotfix 흡수 → commit / push / PR 단독 진행**. codex 가 직접 push 한 건 표준 위반.

사용자 추가 지적:
> "해당 명령어도 있잖냐 /codex:review"

본인이 학습 #5 본문에 listed slash 7종 (`/codex:rescue`, `/codex:setup`, `/codex:status`, `/codex:result`, `/codex:cancel`, **`/codex:review`**, `/codex:adversarial-review`) 박혀있었음에도 `/codex:review` 의 존재를 무시 → **G-006 위반 재현** (listed 정보를 추측 우선시).

### 11.3 codex slash 분류 (실측 확인 후 명문화)

`C:/Users/User/.claude/plugins/cache/openai-codex/codex/1.0.4/commands/review.md` 실측:

```
## /codex:review
Core constraint:
- This command is review-only.
- Do not fix issues, apply patches, or suggest that you are about to make changes.
- Your only job is to run the review and return Codex's output verbatim to the user.
```

| 작업 | 명령 | 권한 |
|---|---|---|
| **dual review (사전 read-only 평가)** | **`/codex:review`** | review-only 강제 (slash 본문에 fix/patch 차단 명시) |
| **adversarial 검토** | `/codex:adversarial-review` | review-only |
| **investigation / fix request / rescue** | `/codex:rescue` | workspace-write 허용 |
| **raw MCP 직접** (필요 시) | `mcp__codex__codex` + `sandbox: "read-only"` + `approval-policy: "never"` + model 생략 | 옵션으로 강제 |

### 11.4 사용자 본질 지적 #2

> "옵션A로 진행하되 학습 뿐만아니라 프로세스 자체에 박혀있는걸 수정해야되는거아니니?"

학습 박는 것만으로는 자동 강제 path 의 잘못된 패턴이 다음 세션에서 재현 가능. mini-milestone β 의 4-layer path (hooks + CI + PR template + memory) 전체 정정 필요.

### 11.5 본 PR (#189) 산출물 — 자동 강제 path 정정

| 파일 | 변경 | 비고 |
|---|---|---|
| `.flowset/hooks/session-start.mjs` | +2 / -1 | 표준 환기에 `/codex:review` 명시 + `/codex:rescue` 는 rescue 작업용 명시 + raw MCP read-only 강제 명시 |
| `.flowset/hooks/pre-tool-use.mjs` | +1 / -1 | dual review 환기에 `/codex:review` 명시 |
| `.flowset/hooks/stop.mjs` | +1 / -1 | 세션 종료 환기에 학습 #13 + `/codex:review` 명시 |
| `.flowset/hooks/README.md` | +12 / -0 | Dual Review 표준 표 신규 (4종 명령 분류) + 본 PR 머지 의의 명시 |
| `.github/PULL_REQUEST_TEMPLATE.md` | +6 / -2 | Dual Review 섹션에 codex 호출 표준 명시 + `/codex:rescue` 사용 금지 명시 |
| `.flowset/guardrails.md` | **+18 / -0 (G-016 신규)** | Dual review = read-only 강제 표준 가드레일 등록 (학습 #13 출처) |
| `.flowset/handoffs/2026-05-20.md` | +N (§11 신규) | 본 §11 사건 + 정정 내역 박음 |
| `~/.claude/projects/.../memory/feedback_dual_review.md` | 정정 | `/codex:review` 우선 명시 |

### 11.6 학습 #13 본문 (재명시)

1. **listed 정보 우선** — 학습 #5 본문에 박힌 slash 7종 listed 정보를 본인이 무시. G-006 자동 강제 path 가 명령어 분류 오인 자체를 검출하지 못한 한계 확인.
2. **dual review = read-only 강제** — codex 호출 시 review-only slash 또는 raw MCP `sandbox: "read-only"` 만 사용. write 권한 부여 시 codex agent 가 직접 push 가능 → dual review 본질 (Claude 가 단독 취합) 위반.
3. **자동 강제 path 정합 의무** — 학습만 박으면 재현 위험. hooks / CI / PR template / memory / guardrails 4-layer path 전체 정정 강제.
4. **G-016 신규 가드레일** — 위 3가지를 명문화. 본 PR (#189) 머지 후 활성.

### 11.7 학습 #5 본문 정정 (소급)

기존 학습 #5 (§1.5 본문) 의 "올바른 표준":
```
- 우선 `/codex:rescue` slash command (codex-rescue subagent forwarder) — model 미명시 시 config.toml 디폴트 적용
- raw MCP 직접 호출 필요 시 model 파라미터 **생략** — config.toml `gpt-5.5` 자동 적용
```

→ **학습 #13 표준으로 대체** (본 §11.3 표 참조). 학습 #5 자체는 model 생략 부분만 유효, slash 분류는 학습 #13 + G-016 가 표준.

### 11.8 dual review 사전 호출 (정정된 표준으로 첫 검증)

본 PR (#189) 자체의 dual review 는 정정된 `/codex:review` 표준으로 호출 — 본 사건 재발 방지 자기 검증.

평가 결과는 본 §11.8 작성 시점 이후에 추가 명시.

### 11.9 다음 세션 진입 권고 (PR #189 머지 후 갱신)

- **권고 0순위 Sprint 016 M1** (시나리오 2·3 cover T07~T08) — §10.8 그대로 유지
- 1순위 T02-followup / 2순위 M2 — §10.8 그대로 유지
- **신규 안내**: 본 PR (#189) 머지 후 모든 dual review 호출은 `/codex:review` 사용 강제. `/codex:rescue` 는 rescue/fix 명시적 의도 있는 경우만.

### 11.10 본 세션 최종 종료 핸드오프 (절대 절대 최종)

**Sprint 016 M0 모든 작업 완료 + dual review 자동 강제 path 정정 완료**:
- 9 PR 머지 (T05/α/β/T06/T06 docs/T04/T02/T01 잔여/§10) + 1 manual (branch protection) + 1 PR 후속 (본 §11 자동 강제 path 정정) = **10 PR + 1 manual**
- KI closed 후보 6종 (KI-004/011/012/013/015/016) + KI-001 T25 판단 준비
- 자동 강제 path 완성 + **dual review read-only 강제 표준 정합** (학습 #13 + G-016)
- 본인 학습: G-006 위반 재현 인지 → 자동 강제 path 자체 정정으로 재발 차단

---

## 12. 학습 #14 — 잔존 원격 브랜치 정리 + Stop hook G-017 강화 (PR #190)

### 12.1 사용자 본질 지적

PR #189 (학습 #13 / G-016 신규) 머지 직후 사용자 지적:
> "원격에 보면 브렌치가 여러개있는데 왜 머지후에 브렌치정리가 안된건지 검토해"

### 12.2 분석 결과

`git fetch origin --prune` 후 원격 잔존 브랜치 2종:

| 브랜치 | PR | 상태 | 원인 |
|---|---|---|---|
| `docs/WI-S016M0-docs-handoff-learning5-hotfix` | #178 | **CLOSED** (mergedAt: null) | GitHub `delete_branch_on_merge` 정책은 **MERGED 한정** — CLOSED 는 자동 삭제 안 됨. 본 세션 학습 #5 정정 시도 후 mini-milestone α 흡수 시점에 닫음. |
| `feature/WI-S015M4-feat-m4-4-indexing-gate` | #141 | MERGED (2026-05-18) | repo `delete_branch_on_merge: true` 활성됨에도 잔존. `feature/` prefix (현재 `feat/` 와 다름) Sprint 015 M4-4 시점 `--delete-branch` 옵션 누락 추정. |

본 세션 본인 PR #176~#189 (14 PR) 은 `gh pr merge --auto --squash --delete-branch` 표준 사용 → 자동 정리 정합. 위 둘은 이전 세션 흔적.

### 12.3 즉시 정리 (1차 hotfix)

```
git push origin --delete docs/WI-S016M0-docs-handoff-learning5-hotfix
git push origin --delete feature/WI-S015M4-feat-m4-4-indexing-gate
```

원격 = `origin/main` 단독 정합.

### 12.4 사용자 옵션 B/C 채택 — 향후 차단 path 박음

**옵션 B**: Stop hook 강화 — 세션 종료 시 잔존 원격 브랜치 점검 + 경고
**옵션 C**: 가드레일 G-017 신규 — PR 닫음/머지 후 원격 브랜치 즉시 정리 강제

### 12.5 본 PR (#190) 산출물

| 파일 | 변경 | 비고 |
|---|---|---|
| `.flowset/hooks/stop.mjs` | +60 / -0 | G-017 점검 step 추가 — `git for-each-ref refs/remotes/origin` + `gh pr list --head <branch>` 분류 (MERGED/CLOSED) → stderr 경고. gh CLI 미설치 시 graceful skip. |
| `.flowset/guardrails.md` | +18 / -1 | **G-017 신규** + 변경 이력 (학습 #14 출처) |
| `.flowset/handoffs/2026-05-20.md` | +60 (§12 신규) | 본 §12 — 사용자 지적 + 분석 + 정리 + 차단 path |

### 12.6 학습 #14 본문

1. **MERGED vs CLOSED 정책 비대칭** — GitHub `delete_branch_on_merge` 는 MERGED 한정. CLOSED PR 의 head 브랜치는 자동 삭제 안 됨 → 수동 cleanup 의무.
2. **세션 종료 시점 점검 자동화** — Stop hook 에 잔존 브랜치 grep + 분류 표시. 다음 세션 진입 시 SessionStart hook 의 KI 잔여 통계 + 가드레일 헤더 출력과 함께 자가 점검 가능.
3. **`--delete-branch` 표준 강제** — 모든 `gh pr merge` 호출에 `--delete-branch` 옵션 의무. 본 세션 본인 PR 14건 모두 적용 정합 — 표준 path 가 박혔으면 잔존 0.
4. **mini-milestone β 4-layer 한계** — hooks + CI + PR template + memory 가 dual review 표준만 cover. 머지 후 cleanup 은 별도 layer 필요 → G-017 박음.

### 12.7 dual review 사전 호출 (학습 #13 정합 표준 + 본 PR 실측 결과)

본 PR 자체의 dual review = **PR #189 머지 후 학습 #13 / G-016 정합 첫 정정 케이스**:
- **evaluator** (서브에이전트): Pass 7 / Partial 1 (5-layer 산식 표현) / Fail 0 / BLOCKING 0 → PASS
- **codex (raw MCP `mcp__codex__codex`, `sandbox=read-only`, `approval-policy=never`, model 생략)**: NEEDS_CHANGES 4 / BLOCKING 0 → 본 PR 내 hotfix 4건 흡수

**codex 4건 hotfix 흡수 (본 §12 작성 시점 commit)**:
1. **shell injection 회피** (`.flowset/hooks/stop.mjs` L113): `execSync(\`gh ... "${branch}"\`)` → `execFileSync('gh', ['pr','list','--head', branch, ...])` 안전 호출 전환
2. **gh pr lookup 실패 graceful 표시** (`stop.mjs` L130-149): 개별 catch 무시 → `ghLookupFailures` 카운트 + `infos.push` 로 graceful skip 가시화
3. **§12.7 raw MCP 확정 표기** (본 항목 자체): "또는" 선택지 → raw MCP 고정 호출 명시
4. **5-layer 산식 정합** (§12.8): 6개 항목 나열 모순 → `기존 4-layer (hooks/CI/PR template/memory) + Stop hook G-017 = 5-layer` + `guardrails 는 정책 출처` 분리

**자기 검증**: 본 PR 머지 시 `flowset-policy-check.yml` 의 G-016 codex 행 분류 검증 (`/codex:rescue` 금지 + `/codex:review` 또는 `sandbox=read-only` 명시 강제) 통과 — G-016 첫 실 통과 케이스.

### 12.8 본 세션 절대 절대 절대 최종 종료 핸드오프 (#190)

**Sprint 016 M0 모든 작업 완료 + dual review 표준 정합 + 잔존 브랜치 정리 + Stop hook G-017 강화**:
- 10 PR (T05/α/β/T06/T06 docs/T04/T02/T01 잔여/§10 docs) + 1 manual (branch protection) + 1 PR 후속 (§11 학습 #13 + G-016 표준 정정) + **1 PR 후속 (§12 학습 #14 + G-017 잔존 브랜치 정리)** = **12 PR + 1 manual**
- 가드레일: G-011 → G-017 (G-012/013/014/015 + G-016 + G-017 신규)
- **자동 강제 path 5-layer** (codex review #4 정합): 기존 4-layer = (1) hooks 4 mjs + (2) CI flowset-policy-check (codex 분류 + dual review 강제) + (3) PR template + (4) memory `feedback_dual_review` → **본 PR 로 (5) Stop hook G-017 점검 추가 = 5-layer 완성**. `guardrails.md` 는 layer 가 아닌 정책 출처 (G-016 / G-017 정의 위치)
- KI closed 후보 6종 + KI-001 T25 판단 준비
- 본 세션 학습 누적: #5 (model 생략, 부분 정정) / #8 (dual review 강제) / **#13 (codex slash 분류)** / **#14 (잔존 브랜치 정리)**

**다음 세션 진입**: Sprint 016 M1 (시나리오 2·3 cover T07~T08) — 정정된 dual review 표준 + G-017 강제 + 자동 강제 path 5-layer 완성 상태에서 진입.
