# 2026-05-21 — Sprint 016 M2 어댑터 일괄 제거 시퀀스 완료

## 메타 (최종 — 본 세션 종료 시점)

- **Sprint**: 016 M2 — 어댑터 일괄 제거 (T09~T13) **5종 시퀀스 완료**
- **누적 PR**: 코드 6 (#198 T12 / #199 T11 / #200 T13 / #201 T10a / #202 T10b / #203 T09) + docs 1 (예정 #204) = **7 PR**
- **단위 테스트**: 1163 PASS (T12 +0 / T11 -14 + 신규 / T13 +3 / T10a +9 / T10b +10 / T09 +0)
- **변경량 누적**: +1030 / -2119 = **-1089 net 코드 감축**
- **codex 사전 BLOCKING/NEEDS_CHANGES**: **6건 모두 본 PR 내 hotfix 흡수** (T12 #1 / T11 #1+#2 / T10a #1 / T10b #8 / T09 클린)
- **KI 변동**: KI-002 [open] → [closed] (PageCachePanel + PageResultStore 어댑터 통째 폐기)
- **학습 추가**: **#19 (codex 협의 시퀀스 = 어댑터 폐기 모범 패턴)**
- **자기 검증 루프**: 24회차 SUCCESS (#181~#204, 24 PR 누적)

---

## 1. PR #198 — T12 PageResultStore + PageCachePanel 통째 폐기 (KI-002 closed)

### 1.1 진입 결정

핸드오프 [`2026-05-20.md`](./2026-05-20.md) §16.8 권고 0순위 (Sprint 016 M2 어댑터 일괄 제거 T09~T13) 진입. 사용자 단일 요청 "핸드오프 읽고 작업 진행해" → 박힌 결정 즉시 진입 (학습 #11 정합). 5종 중 가장 독립적인 T12 부터 진입 — 다른 T 의존 0.

### 1.2 산출물 (PR #198 `7dffdde`)

| 파일 | + | - | 비고 |
|---|---|---|---|
| `src/storage/PageResultStore.ts` | 0 | 336 | 통째 삭제 |
| `src/renderer/src/settings/PageCachePanel.tsx` | 0 | 57 | 통째 삭제 (MemoryStatsPanel 흡수) |
| `tests/unit/storage/PageResultStore.adapter.test.ts` | 0 | 257 | 어댑터 모드 test 삭제 |
| `src/main/services.ts` | 2 | 16 | import + 인스턴스 + IPC handler 폐기 |
| `src/preload/index.ts` | 2 | 10 | pageResultApi + expose + type 제거 |
| `src/preload/index.d.ts` | 0 | 2 | PageResultApi 타입 제거 |
| `src/renderer/src/settings/SettingsPage.tsx` | 0 | 2 | import + 마운트 제거 |
| `src/renderer/src/styles.css` | 1 | 33 | `.page-cache-panel.*` 제거 |
| `src/storage/IndexedPageStore.ts` | 4 | 5 | normalizeIndexedUrl 단일 source 주석 |
| `src/storage/index.ts` | 3 | 7 | re-export 제거 |
| `src/storage/migrations/v03_to_v04.ts` | 16 | 7 | **codex BLOCKING #1 hotfix** |
| `tests/unit/storage/migrations/v03_to_v04.test.ts` | 40 | 0 | case 4b/4c 신규 (raw array fixture) |
| `.flowset/known-issues.md` | 6 | 4 | KI-002 → closed |

**총**: 13 files / **+74 / -736** (실측 `git show --numstat 7dffdde`).

### 1.3 codex BLOCKING #1 hotfix 흡수 (학습 #18)

**문제**: v0.3 `PageResultStore.persistOnce()` 는 `JSON.stringify(all)` 로 **raw array** 영속. 그러나 `migrations/v03_to_v04.ts:354` 는 `{ entries?: V03PageResultEntry[] }` wrapper 가정 → 실 사용자 `page-results.json` 0건 처리 → Page/Visit 영구 누락.

**정정**: `dryRunSimulate` + `migratePageResults` 양쪽 shape 허용 + case 4b/4c 테스트 추가.

### 1.4 dual review 결과

- **evaluator (사전)**: Pass 10/0/0 → PASS
- **codex (사전)**: PASS 5 / BLOCKING 1 / NEEDS_CHANGES 1 / NB 2 → 본 PR hotfix 흡수 후 PASS

---

## 2. PR #199 — T11 TranslationCache 어댑터 모드 + cache:* IPC 폐기

### 2.1 진입 결정

T12 머지 후 main 동기화. 사용자 권고 (T11 — TranslationCache 어댑터 제거). 본 PR scope = 어댑터 mode (backend 분기 path) 통째 제거, TranslationCache 클래스 자체는 T10 후 폐기.

### 2.2 산출물 (PR #199 `836700a`)

| 파일 | + | - | 비고 |
|---|---|---|---|
| `tests/unit/storage/TranslationCache.adapter.test.ts` | 0 | 420 | 어댑터 모드 test 14 케이스 삭제 |
| `src/storage/TranslationCache.ts` | 5 | 125 | backend 옵션 + AIResponseCache import + 모든 분기 path 제거 |
| `src/main/services.ts` | 3 | 12 | registerCacheIpc 폐기 (cache:* IPC 3종) |
| `src/preload/index.ts` | 2 | 8 | cacheApi + expose + type 제거 |
| `src/preload/index.d.ts` | 0 | 2 | CacheApi 타입 제거 |
| `src/storage/AIResponseCache.ts` | 4 | 5 | 어댑터 인용 주석 정리 |
| `src/storage/featureFlags.ts` | 4 | 3 | 사용처 0 주석 갱신 |
| `src/storage/migrations/v03_to_v04.ts` | 88 | 15 | **codex BLOCKING #1+#2 hotfix** |
| `tests/unit/storage/TranslationCache.legacy.test.ts` | 166 | 0 | **NEEDS_CHANGES #1 hotfix** — v0.3 single-path 회귀 8 케이스 |
| `tests/unit/storage/migrations/v03_to_v04.test.ts` | 78 | 13 | case 3 정정 + 3b 신규 |

**총**: 10 files / **+350 / -603** (실측 `git show --numstat 836700a`).

### 2.3 codex BLOCKING 2건 hotfix 흡수

- **BLOCKING #1** (T12 동일 패턴): `migrateTranslationCache` 가 wrapper shape 가정인데 v0.3 영속은 raw array.
- **BLOCKING #2** (신규): 마이그레이션 output `{entries: ..., version: 2}` wrapper + entry shape 가 AIResponseCache.parseEntry invariant 위배 → 마이그레이션 후 AIResponseCache.load() 가 모든 entry reject.

**정정**: `migrateTranslationCache` 완전 재작성. raw array `AICacheEntry[]` 영속 + composite key 5-tuple 재구성 + entry shape (id/kind/key/value/hitCount/timestamps/metadata) 정합.

### 2.4 dual review 결과

- **evaluator**: Pass 9 / NEEDS_CHANGES 1 (v0.3 single-path 회귀 0 → 본 PR 8 케이스 신규로 해소)
- **codex**: PASS 4 / BLOCKING 2 / NEEDS_CHANGES 1 / NB 2 → 본 PR 모두 흡수

---

## 3. PR #200 — T13 OpenAIApiKeyProvider fetchImpl 통일

### 3.1 진입 결정

T11 머지 후 codex 협의 (raw MCP read-only sandbox) — 다음 PR 시퀀스 결정. codex 추천: **T13 → T10a → T10b → T09**. T13 가 작고 독립적이며 T10 의 chat mock 주입성 확보.

### 3.2 산출물 (PR #200 `092cfa7`)

| 파일 | + | - | 비고 |
|---|---|---|---|
| `src/ai/providers/OpenAIApiKeyProvider.ts` | 16 | 6 | fetchImpl 옵션 + private field + 4 fetch 호출 교체 |
| `tests/unit/ai/ProviderAdapter.test.ts` | 51 | 0 | 신규 3 케이스 (default / injected / 4 endpoint 일관성) |

**총**: 2 files / **+67 / -6**.

### 3.3 dual review 결과

- **evaluator**: Pass 10/0/0 → PASS
- **codex**: PASS 5 / BLOCKING 1 (commit 전 main..HEAD empty — 자연 해소) / NB 1 → PASS

---

## 4. PR #201 — T10a executeTranslateRequest → provider.chat() (codex 옵션 C)

### 4.1 진입 결정

T13 머지 후 main 동기화. codex 협의 옵션 C — selection 번역 흐름의 provider 호출만 chat 으로 교체 (TranslationCache 유지). ChatService 직접 호출 X (retrieval / chat history persist 의미 섞임 회피).

### 4.2 산출물 (PR #201 `84f86ed`)

| 파일 | + | - | 비고 |
|---|---|---|---|
| `src/main/services.ts` | 55 | 1 | provider.chat 교체 + helper 2종 export + chat guard |
| `tests/unit/main/translateChatAdapt.test.ts` | 123 | 0 | **codex NEEDS_CHANGES #1 hotfix** — 9 케이스 (selection/explanation/summary/subtitle prompt + ChatResponse 어댑트) |

**총**: 2 files / **+178 / -1**.

### 4.3 codex NEEDS_CHANGES #1 hotfix 흡수

**문제**: executeTranslateRequest 가 provider.chat() 호출 후 ChatResponse → TranslationOutput 어댑트하는 흐름 회귀 안전망 0.

**정정**: services.ts 내부 helper 2종 export (`buildTranslationChatRequest` + `chatResponseToTranslationOutput`) + 9 케이스 단위 test 신규.

### 4.4 dual review 결과

- **evaluator**: Pass 10/0/0 → PASS
- **codex**: PASS 9 / NEEDS_CHANGES 1 / NB 2 → 본 PR hotfix 흡수 후 PASS

---

## 5. PR #202 — T10b cache backend TranslationCache → AIResponseCache

### 5.1 진입 결정

T10a 머지 후 main 동기화. codex 협의 옵션 C 두 번째 단계 — cache backend 마이그레이션. 5 사전 경계 위험 (composite key 호환 / value+metadata shape / subtitle TTL 365d / cache hit modelUsed / T11 마이그레이션 round-trip) 명시.

### 5.2 산출물 (PR #202 `84402f5`)

| 파일 | + | - | 비고 |
|---|---|---|---|
| `src/main/services.ts` | 101 | 27 | AIResponseCache wiring + 3 helper export + lookup/store/invalidate 교체 + glossary 5곳 통합 |
| `src/storage/TranslationCache.ts` | 0 | 277 | 통째 삭제 |
| `src/storage/index.ts` | 3 | 2 | re-export 제거 |
| `src/storage/migrations/v03_to_v04.ts` | 4 | 0 | **codex NEEDS_CHANGES #8 hotfix** — value providerType 추가 |
| `tests/unit/storage/TranslationCache.legacy.test.ts` | 0 | 166 | 통째 삭제 |
| `tests/unit/main/translationCacheBackend.test.ts` | 197 | 0 | 신규 10 케이스 (5 위험 cover) |
| `tests/unit/storage/migrations/v03_to_v04.test.ts` | 7 | 1 | case 3 providerType 검증 추가 |

**총**: 7 files / **+312 / -473** (실측 `git show --numstat 84402f5`).

### 5.3 codex NEEDS_CHANGES #8 hotfix 흡수

**문제**: T11 `migrateTranslationCache` value 가 `{id, sourceText, translatedText, domain, createdAt}` 인데 runtime `TranslationCacheValue` 는 `{translatedText, sourceText, providerType}` — providerType 누락. lookup 시 read-compatible 정합 위배.

**정정**: migration value 에 `providerType: e.providerType` 추가 + runtime interface 주석 read-compatible 책임 명시 + 후방호환 필드 (id/domain/createdAt) 보존.

### 5.4 dual review 결과

- **evaluator**: Pass 9 / Partial 1 (G-018 변경량) → PASS
- **codex**: PASS 8 / NEEDS_CHANGES 1 / NB 2 → 본 PR hotfix 흡수 후 PASS

---

## 6. PR #203 — T09 ProviderAdapter.translate() 일괄 제거 (M2 시퀀스 완료)

### 6.1 진입 결정

T10b 머지 후 main 동기화. codex 협의 시퀀스 마지막 단계. T10a 후 호출지점 0 + T10b 후 cache 폐기 완료 — 본 PR 은 순수 cleanup.

### 6.2 산출물 (PR #203 `ab20f97`)

| 파일 | + | - | 비고 |
|---|---|---|---|
| `src/ai/ProviderAdapter.ts` | 5 | 19 | translate() 시그니처 + import 정리 |
| `src/ai/providers/OpenAIApiKeyProvider.ts` | 5 | 90 | translate() + buildMessages + 3 private helper 제거 |
| `src/ai/providers/CodexLoginProvider.ts` | 7 | 133 | translate() + callResponses + M3-8 핫픽스 제거 |
| `tests/unit/ai/CodexLoginProvider.test.ts` | 25 | 11 | 8 케이스 chat 시그니처 마이그레이션 |
| `tests/unit/ai/ProviderAdapter.test.ts` | 4 | 11 | T13 fetchImpl 4 → 3 endpoint |
| mock stub 7 파일 (AutoTagger/EmbeddingClient/m4-pipeline/scenario-2-pm/ChatService/chatHandlers/searchHandlers) | 3 | 36 | translate property + 미사용 import 제거 |

**총**: 12 files / **+49 / -300** (실측 `git show --numstat ab20f97`).

### 6.3 dual review 결과

- **evaluator**: Pass 9 / Partial 1 (G-018 매트릭스 — 본 commit 후 정정) → PASS
- **codex**: PASS 9 / BLOCKING 0 / NEEDS_CHANGES 0 / NB 2 → PASS

---

## 7. 학습 #19 — codex 협의 시퀀스 = 어댑터 폐기 모범 패턴

본 세션 누적 6 PR 시퀀스가 향후 어댑터 폐기 사례의 모범 패턴 박음:

| 단계 | 작업 | 본 시퀀스 매핑 |
|---|---|---|
| (0) **신규 모듈 도입** (이미 완료) | chat() / embed() 메서드 추가 | Sprint 015 M2-7 |
| (1) **호환 어댑터 단계** | 어댑터로 v0.3/v0.4 동시 호환 | Sprint 015 M2 (TranslationCache.backend) |
| (2) **정리 PR (T13 패턴)** | Provider 일관성 (fetchImpl 통일) — 테스트 주입성 확보 | PR #200 |
| (3) **호출자 마이그레이션 (T10a 패턴)** | services.ts 가 신규 메서드 호출, 기존 cache 보존 | PR #201 (옵션 C) |
| (4) **cache/backend 교체 (T10b 패턴)** | 이전 cache 모듈 → 신규 cache 모듈 | PR #202 |
| (5) **호출자 0 검증 후 메서드 제거 (T09 패턴)** | 어댑터 메서드 + 구현 + 테스트 일괄 정리 | PR #203 |
| (6) **별도 독립 PR (T12 패턴)** | 의존성 없는 어댑터는 가장 먼저 진입 | PR #198 |

**적용 원칙**:
- codex 사전 협의 (raw MCP read-only) 로 시퀀스 결정 + 5 위험 사전 박음
- 매 PR 사전 dual review (evaluator + codex) — codex BLOCKING/NEEDS_CHANGES 6건 모두 본 PR 내 hotfix 흡수
- T 단위 작게 분할 + G-013 단계별 PR 전략 100% 정합

---

## 8. 누적 codex 사전 BLOCKING/NEEDS_CHANGES 6건 정리

| PR | codex 발견 | 본질 | 본 PR hotfix |
|---|---|---|---|
| #198 T12 | BLOCKING #1 | v0.3 PageResultStore raw array vs migration wrapper 가정 | migration 양쪽 shape 허용 + 회귀 케이스 2건 |
| #199 T11 | BLOCKING #1 | TranslationCache 동일 마이그레이션 패턴 | migration 양쪽 shape 허용 |
| #199 T11 | BLOCKING #2 | migration output wrapper + entry shape vs AIResponseCache.parseEntry 위배 | migration 완전 재작성 (raw array AICacheEntry[]) + round-trip 회귀 |
| #199 T11 | NEEDS_CHANGES #1 | TranslationCache v0.3 single-path 회귀 0 | legacy.test 8 케이스 신규 |
| #201 T10a | NEEDS_CHANGES #1 | ChatResponse → TranslationOutput 어댑트 회귀 0 | translateChatAdapt.test 9 케이스 신규 |
| #202 T10b | NEEDS_CHANGES #8 | migration value providerType 누락 (runtime read-compatible 위배) | migration value 에 providerType 추가 + runtime 주석 정합 |

**모두 본 PR 내 hotfix 흡수**. 후속 PR 분리 0. 학습 #18 (사전 dual review 본질적 가치) 6번째 확증.

---

## 9. 본 세션 종료 검증

| 검증 | 결과 |
|---|---|
| `npm run lint` | PASS |
| `npm run typecheck` (node + web) | PASS |
| `npm test` | 72 files / **1163 PASS** |
| `npm run build` | PASS |
| 머지 commit | `ab20f97` (PR #203 head) |
| 원격 브랜치 | `--delete-branch` 자동 정리 정합 (G-017) |
| git status main | clean (PR #203 머지 후 fast-forward) |

---

## 10. KI 변동

- **KI-002** [open] LOW PageCachePanel PARTIAL → **[closed]** (PR #198) — PageResultStore + PageCachePanel 어댑터 통째 폐기 + MemoryStatsPanel 흡수
- KI 잔여 17 → 16 (HIGH 1 / MEDIUM 3 / LOW 12 — KI-003 [open] HIGH 잔존, status `[in-progress]` 갱신은 별도 PR). 신규 KI 등록 0 (codex 사전 발견 결함은 모두 본 PR 내 흡수).

---

## 11. 다음 세션 진입 권고

### 권고 0순위 — Sprint 016 M3 (cookies partition + WorkspacePartitionManager, T14~T17)

본 M2 완료 후 자연스러운 다음 단계. contract `.flowset/contracts/sprint-016.md` §3 AC-4 + §4 M3 정합:
- `WorkspacePartitionManager` — `session.fromPartition('persist:ws-{uuid}')` 단위 격리
- 워크스페이스 전환 시 cookies / localStorage / IndexedDB 완전 격리
- 워크스페이스 삭제 시 `clearStorageData` + `clearCache` cascade
- JSON Export/Import — Workspace + Page + Visit + Note + AiChatHistory + Tag 전체 보존

리스크 (contract §7):
- session.fromPartition cookies 호환성 — Electron 38+ Partition API 안정성 사용자 사이트 의존 (Notion 등 session storage 차단 가능성)

신규 가드레일 활성:
- **G-015 [활성]** Phase 2 cookies partition 격리 — 사용자 명시 동의 후 활성화

### 권고 1순위 — G-013 3단계 후속 (T02-followup 잔여)

`ChatRequest.signal?: AbortSignal` optional + EmbeddingClient worker 의 in_progress 보호 — PRD §11.3.3 정확한 cover. PR #196 T02-followup 시점 codex NEEDS_CHANGES #1 후속.

### 권고 2순위 — KI-018 50 페어 자체 셋 + S1-C3 chat_meta schema hotfix

contract §6 정량 임계 #3 (top-10 hit rate ≥ 80%) 측정용 50 페어 자체 테스트 셋 구축. 기존 scenario-1-academic.test.ts S1-C3 의 chat_meta 구 schema → 통일 schema 정합 hotfix.

### 권고 3순위 — 잔여 docstring 정정 (evaluator NB-1)

다음 docstring 4건이 "T10 (executeTranslateRequest → ChatService.chat 마이그레이션)" 표기 잔존 (옵션 C 실제는 provider.chat 직결):
- `src/preload/index.ts:166`
- `src/storage/TranslationCache.ts` (이미 폐기 — 무관)
- `src/main/services.ts:695`
- (기타)

cleanup PR 하나로 묶음 권고.

---

## 12. M2 절대 최종 종료 핸드오프

**Sprint 016 M2 어댑터 일괄 제거 시퀀스 (T09~T13) 5종 = 코드 6 PR + 본 docs PR 1 = 7 PR 완료**:
- T12 #198: PageResultStore + PageCachePanel (KI-002 closed)
- T11 #199: TranslationCache 어댑터 모드 + cache:* IPC
- T13 #200: OpenAIApiKeyProvider fetchImpl 통일
- T10a #201: executeTranslateRequest → provider.chat() (옵션 C)
- T10b #202: cache backend → AIResponseCache
- T09 #203: ProviderAdapter.translate() 메서드 + 구현 제거
- docs PR #204: state.md + handoff 갱신

**누적**:
- 코드 PR 6 + docs PR 1 = 7 PR / 코드 +1030 / -2119 (-1089 net 감축)
- 단위 1163 PASS 유지
- codex 사전 BLOCKING/NEEDS_CHANGES 6건 모두 본 PR hotfix 흡수
- KI-002 closed
- 학습 #19 추가 (어댑터 폐기 모범 패턴)

---

## 13. Sprint 016 M3 cookies partition + Export/Import (T14~T17) 4 PR 시퀀스 완료

### 13.1 진입 결정 + codex 사전 협의 (read-only)

§12 후속. codex 협의 결과 (T14 진입 직전, read-only sandbox):
- partition 적용 = `WebContentsView` 단위 (`BrowserWindow` 아님) — `createTabView` 의 `webPreferences.partition`
- Map 캐시 X (Electron 보장 정합)
- `WorkspaceService` 직접 Electron session 결합 회피 — workspaceHandlers 가 svc.delete() 후 manager 호출
- Export/Import: page_tags/note_tags 포함 (Tag 만 export 시 연결 손실) + JSON column TEXT 그대로 + ID remap 시 retrieved_items/chat_meta page_id rewrite + vec_pages/vec_notes 제외
- 시퀀스: T14 모듈 → T15-1 createTabView 적용 → T15-2 switch 동기화 → T16 delete cascade → T17 Export/Import 독립 PR

### 13.2 PR #205 — T14 WorkspacePartitionManager 신규 (G-013 1단계)

산출물: `src/main/WorkspacePartitionManager.ts` (+127) + `tests/unit/main/WorkspacePartitionManager.test.ts` (+215, 21 케이스) = 2 files / **+342 / -0**

책임 3: `getPartitionName` / `getSession` / `clearWorkspaceData`. Map 캐시 X / SessionFactory interface 추상화 / WorkspacePartitionError 별도 클래스.

dual review:
- evaluator: Pass 7/0/0 → PASS
- codex: NEEDS_CHANGES 1 (trim alias 위험 — `'abc'` 와 `' abc '` 같은 partition alias) + NB 2 → 본 PR hotfix 흡수 (raw !== raw.trim() 거부 + 회귀 2 케이스)

검증: lint / typecheck / **1184 PASS** / build PASS. KI-002 영향 없음. G-013 1단계 정합 (호출자 0).

### 13.3 PR #206 — T15 createTabView partition wiring (G-013 2단계)

산출물: `src/main/tabViewWebPreferences.ts` (+52) + `tests/unit/main/tabViewWebPreferences.test.ts` (+68, 7 케이스) + `src/main/services.ts` (+45/-1) + `src/main/index.ts` (+45/-22) = 4 files / **+210 / -23**

`createTabView(tabId, url, workspaceId?: string|null)` 시그니처 + 호출자 10곳 정정 (initializeTabs/firstTab/tab:open/tab:close 자동 빈탭/tab:duplicate/reopenLastClosedTab/tab:show-context-menu 탭닫기·복제/menu 새탭·자동빈탭). services.ts 가 WorkspacePartitionManager 인스턴스화 + `getWorkspacePartitionName` getter (graceful null path).

dual review:
- evaluator: Pass 8/0/0 → PASS (즉시 머지 가능)
- codex: NB 2 (테스트 gap services getter / 주석 표현 정정) → 본 PR hotfix 흡수 1건 (주석 "in-memory" → "default session")

검증: lint / typecheck / **1191 PASS** / build PASS. PRD §11.2.2 정합.

### 13.4 PR #207 — T16 워크스페이스 삭제 partition cascade cleanup (G-013 3단계)

산출물: `src/main/workspaceHandlers.ts` (+58) + `src/main/services.ts` (+24/-1) + `src/main/index.ts` (+35) + `tests/unit/main/workspaceHandlers.test.ts` (+231, T16 12 케이스) = 4 files / **+348 / -1**

dual review:
- evaluator: Pass 8/0/0 → PASS
- codex: BLOCKING 1 (Active workspace 삭제 시 live WebContents/탭 정리 없이 partition cleanup 실행 → storage 재생성 + 다음 부팅 재생성 위험) + NEEDS_CHANGES 2 (invalid_input/svc.delete throw callback 미호출 회귀 누락) + NB 2

**본 PR 내 hotfix 흡수** (학습 #18 8번째 확증):
- BLOCKING #1 — `destroyWorkspaceTabs?: (wsId, newActiveId) => void` callback 신규 (svc.delete() → destroyWorkspaceTabs → clearWorkspacePartition 순서). `setWorkspaceDeleteHook` setter + main/index.ts hook 등록 (삭제 ws 모든 탭 close + destroyTabView + setActiveWorkspaceFilter + 새 빈 탭 + workspace:switched broadcast)
- NEEDS_CHANGES 2 — 4 케이스 추가 (순서 정합 / 인자 정합 / throw swallow / 미주입 no-op / invalid_input / svc.delete throw)

검증: lint / typecheck / **1203 PASS** / build PASS. G-015 완전 실현.

### 13.5 PR #208 — T17 Workspace JSON Export/Import (KI-008 closed)

산출물: `src/main/WorkspaceExportImportService.ts` (+541) + `tests/unit/main/WorkspaceExportImportService.test.ts` (+354, 19 케이스) + `src/main/workspaceHandlers.ts` (+113) + `src/main/services.ts` (+32) + `src/preload/index.ts` (+35/-1) + `tests/unit/main/workspaceHandlers.test.ts` (+104/-1, 9 케이스) + `.flowset/known-issues.md` (+30/-5) = 7 files / **+1209 / -7**

8 entity export (Workspace + Page + Visit + Note + AiChatHistory + Tag + page_tags + note_tags). 항상 새 workspace id + 모든 child id 새 발급 + retrieved_items/chat_meta + RetrievedItem.id (type 기반) 참조 rewrite + 단일 TX. vec_pages/vec_notes 제외.

dual review:
- evaluator: Pass 7/Partial 1 (KI-022 inline → 독립 헤더 부재, 본 PR hotfix 흡수) → PASS
- codex: **BLOCKING 2** + NEEDS_CHANGES 1 (summary 입력 length) + NB 4

**본 PR 내 hotfix 흡수** (학습 #18 9번째 확증):
- BLOCKING #1 — Import 후 WorkspaceService.list 캐시 미갱신. 정정: handleWorkspaceImportJson 가 svc.invalidateCache 호출 + 회귀 1
- BLOCKING #2 — dangling visit descendant 처리 깨짐 (visitIdMap 선생성 + note/chat 이 skipped visit id 를 FK 에 넣음). 정정: visitIdMap/noteIdMap 사전 생성 없이 실제 INSERT 시점에 박음 + skipped id 가 retrieved_items/chat_meta 에 등장하면 null 박음 + 회귀 1
- NEEDS_CHANGES — summary 가 입력 length → 실제 INSERT 수 카운트 + 회귀 1
- evaluator Partial — KI-022 독립 헤더 추가 + KI-021 동반 추가

검증: lint / typecheck / **1231 PASS** / build PASS.

### 13.6 학습 #18 9번째 확증 (codex 사전 dual review 본질적 가치)

본 세션 M3 시점 4 PR 내 흡수한 codex BLOCKING + NEEDS_CHANGES:
| PR | codex 발견 | 본질 | hotfix |
|---|---|---|---|
| #205 T14 | NEEDS_CHANGES #1 | workspaceId trim alias 위험 | trim 거부 + 회귀 2 |
| #205 T14 | NB #2 | "in-memory session" 주석 표현 | "default session" 정정 |
| #207 T16 | BLOCKING #1 | live WebContents destroy 없이 partition cleanup | destroyWorkspaceTabs callback + setWorkspaceDeleteHook + 회귀 4 |
| #207 T16 | NEEDS_CHANGES x2 | invalid_input + svc.delete throw 시 callback 미호출 누락 | 회귀 2 |
| #208 T17 | BLOCKING #1 | WorkspaceService 캐시 미갱신 | svc.invalidateCache + 회귀 1 |
| #208 T17 | BLOCKING #2 | dangling visit descendant 처리 | visitIdMap 실제 INSERT 시점 + null rewrite + 회귀 1 |
| #208 T17 | NEEDS_CHANGES #1 | summary 가 입력 length | 실제 INSERT count + 회귀 1 |

**모두 본 PR 내 hotfix 흡수**. 후속 분리 PR 0. 학습 #18 9번째 확증.

### 13.7 KI 변동

- KI-002 [closed] (M2 T12 PR #198) — PageCachePanel + PageResultStore 어댑터 폐기 (§10 참조)
- **KI-008 [closed]** (M3 T17 PR #208) — Workspace JSON Export/Import 신규 (8 entity + ID remap)
- **KI-021 [open] LOW 신규** (M3 T16 codex NB-1) — partition cleanup 실패 reconcile path 부재
- **KI-022 [open] LOW 신규** (M3 T17 codex NB) — Import 후 embedding_queue 재 enqueue 부재

KI 잔여 산식:
- M2 직후: 16 (HIGH 1 / MEDIUM 3 / LOW 12)
- M3 T17 직후: 16 - 1 (KI-008 closed) + 2 (KI-021 + KI-022 신규) = **17** (HIGH 1 / MEDIUM 3 / LOW 13)
- 본 M3 시퀀스 4 PR 순증: **+1** (1 closed + 2 신규)

### 13.8 본 세션 절대 최종 종료 핸드오프

**Sprint 016 M3 cookies partition + Export/Import (T14~T17) 4 PR + 본 docs PR 1 = 누적 5 PR 완료**:
- T14 #205: WorkspacePartitionManager 모듈 + 21 케이스
- T15 #206: createTabView partition wiring + 7 케이스
- T16 #207: delete cascade cleanup + destroyWorkspaceTabs (BLOCKING #1 흡수) + 12 케이스
- T17 #208: Workspace JSON Export/Import + ID remap + cache invalidation (BLOCKING 2 흡수) + 19 + 9 케이스
- docs PR #209 (예정): state.md + handoff §13 갱신

**누적**:
- 코드 PR 4 + docs PR 1 = 5 PR / 코드 +2109 / -31
- 단위 1231 PASS (1163 → 1231, +68)
- codex 사전 BLOCKING 3 (T17 #208 BLOCKING 2 + T16 #207 BLOCKING 1) + NEEDS_CHANGES 4 모두 본 PR hotfix 흡수
- KI-008 closed + KI-021 / KI-022 신규
- G-015 cookies partition 격리 완전 실효
- 학습 #18 9번째 확증

**다음 세션 진입: Sprint 016 M4 (권고 0순위)** — 백그라운드 번역 + 하이라이트 (T18~T22). BackgroundTranslationQueue + 시스템 알림 + NoteHighlight + AutoTagger.tagNote (KI-005). 권고 1순위 KI-022 후속 (embedding_queue 재 enqueue). 권고 2순위 KI-021 후속 (partition cleanup reconcile path).
