# 2026-05-21 — Sprint 016 M2 어댑터 일괄 제거 + M3 cookies partition + M4 백그라운드 번역·하이라이트 시퀀스

## 메타 (최종 — 본 세션 종료 시점)

- **Sprint**: 016 M2~M4 진행 — M2 어댑터 (T09~T13) **5종 완료** / M3 cookies partition (T14~T17) **4종 완료** / **M4 (T18~T22) 4/5 완료 (T20 다음 세션)**
- **누적 PR (본 세션)**: 코드 14 + docs 2 = **16 PR** (M2 6 PR #198~#203 / M2 docs #204 / M3 4 PR #205~#208 / M3 docs #209 / **M4 4 PR #210 T21 / #211 T22 / #212 T19 / #213 T18 + 본 docs PR (예정)**)
- **단위 테스트**: 1163 → **1329** (+166, M2 +0 / M3 +68 / M4 +98)
- **codex 사전 BLOCKING/NEEDS_CHANGES**: **17건 모두 본 PR 내 hotfix 흡수** (M2 6 + M3 7 + **M4 8** — T18 4 + T19 3 + T21 1 + T22 NB-HF 1) — 학습 #18 **10번째 확증**
- **KI 변동**: KI-002 closed (M2 T12) / **KI-005 closed (M4 T21)** / KI-008 closed (M3 T17) / KI-021 신규 (M3 T16 NB) / KI-022 신규 (M3 T17 NB)
- **학습 추가**: **#19 (codex 협의 시퀀스 = 어댑터 폐기 모범 패턴)** — M2 시점 박힘
- **자기 검증 루프**: **32회차 SUCCESS** (#181~#213, 32 PR 누적)

---

## 1~12. M2 시퀀스 (PR #198~#204) — 이전 작성분 보존

> 본 세션 §1~§12 은 이전 작성분 (M2 어댑터 일괄 제거 시퀀스) 그대로 유지. M2 PR #198~#203 + docs #204 6 PR 시퀀스 완료. KI-002 closed. 학습 #19 박음.

## 13. Sprint 016 M3 cookies partition + Export/Import (T14~T17) 4 PR 시퀀스 완료

> 본 세션 §13 도 이전 작성분 (M3 PR #205~#208 + docs #209) 그대로 유지. KI-008 closed + KI-021/KI-022 신규. G-015 cookies partition 격리 완전 실효. 학습 #18 9번째 확증.

---

## 14. Sprint 016 M4 백그라운드 번역 + 하이라이트 (T18~T22) — 4/5 PR 머지 (T20 다음 세션)

### 14.1 codex 사전 협의 시퀀스 결정

본 M4 진입 직전 codex (read-only sandbox) 협의 — 권고 시퀀스:

**T21 → T22 → T19 → T18 → T20** (가장 작고 독립적 → 위험 점증 순)

근거:
- T21 (AutoTagger.tagNote) — 가장 작은 PR + KI-005 closed 가능 (의존성 0)
- T22 (UserLevelEstimator mock) — mock 자리만 (저위험)
- T19 (NotificationService) — T18 가 쓸 알림 abstraction 먼저 (G-013 정합)
- T18 (BackgroundTranslationQueue) — 큐/비용/취소/진행률 얽힘, T19 이후 안전
- T20 (NoteHighlight) — schema + WebContents DOM + renderer overlay, M4 위험도 최고

### 14.2 PR #210 — T21 AutoTagger.tagNote (KI-005 closed)

산출물: `src/ai/tagging/AutoTagger.ts` (+68/-9) + `src/main/NoteService.ts` (+53/-11) + `tests/unit/ai/AutoTagger.test.ts` (+209) + `tests/unit/main/NoteService.test.ts` (+240/-4) + `.flowset/known-issues.md` (+8/-6) = 5 files / **+578 / -30**

설계 (codex M4 사전 협의 정합):
- public generic 화 회피 — `tagPage`/`tagNote` 분리, 호출 의도 명확
- private `tagContent(input, attach: (tagId) => void)` helper 추출 (DRY)
- `tagNote` 는 `tagStore.attachToNote` 호출 — `page_tags` FK 위반 회피
- 부가 hotfix: `TagInputBase.maxOutputTokens` → `ChatRequest.maxOutputTokens` 실 전달 (Sprint 015 M4-2 NB-1 잔존)

NoteService wiring:
- `opts.autoTagger?` optional 주입
- `createNote(enableAutoTagging=true)` + autoTagger 주입 시 `tagNote` 호출, 그 외 `'not_called'` safety
- BYOK 정합 (G-003 / KI-003) — autoTagger provider 선택은 호출자 책임

dual review:
- evaluator: Pass 7 / Partial 1 (G-018 매트릭스) / Fail 0 → PASS
- codex: BLOCKING 0 / NEEDS_CHANGES 1 (autoTagger throw 시 createNote 자체 throw 위험) / NB 다수 → 본 PR `2a2f466` hotfix 흡수 (try/catch + autoTaggingStatus='failed') + 회귀 +1

회귀 +20 / 단위 1231 → 1251. **KI-005 [open] LOW → [closed]**.

### 14.3 PR #211 — T22 UserLevelEstimator (deterministic mock, R3-B)

산출물: `src/main/UserLevelEstimator.ts` (+137 신규) + `tests/unit/main/UserLevelEstimator.test.ts` (+196 신규) = 2 files / **+333 / -0**

설계 (codex M4 사전 협의 정합):
- AI 호출 0 (provider 주입 없음, network 0)
- 저장소 mutation 0 (DB 의존성 0, type-only import)
- `workspaces.level_preference` override 0 (사용자 수동 R3-A 항상 우선)
- deterministic (동일 입력 시 동일 결과)

API: `estimate(input): { level, confidence, source: 'mock' }`. Phase 3 학습 도입 시 본 mock 교체 + `source: 'learned'` union 확장.

dual review:
- evaluator: Pass 11 / Partial 1 (CI 게이트 의존) / Fail 0 → PASS
- codex: BLOCKING 0 / NEEDS_CHANGES 0 / NB-HF 1 (estimate input null/non-string guard) → 본 PR `5af98dc` hotfix 흡수 + 회귀 +4

회귀 +27 / 단위 1251 → 1278.

### 14.4 PR #212 — T19 NotificationService (Electron Notification + fallback)

산출물: `src/main/NotificationService.ts` (+259 신규) + `tests/unit/main/NotificationService.test.ts` (+360 신규) = 2 files / **+619 / -0**

설계 (codex M4 사전 협의 정합):
- `NotificationFactory` 인터페이스 추상화 (Electron 의존 격리, 단위 테스트 stub)
- `makeRealNotificationFactory()` lazy `require('electron')`
- `Notification.isSupported()` 체크 → 미지원 시 renderer IPC `'notification:fallback'` channel
- destroyed window guard (onClick safe wrapper + IPC send 시 isDestroyed filter)
- generic trigger 수용 (translation / indexing / import 모두)

dual review:
- evaluator: Pass 12 / Partial 0 / Fail 0 → PASS
- codex: BLOCKING 0 / NEEDS_CHANGES 3 / NB 다수 → 본 PR `af79074` hotfix 흡수:
  - #1: Electron `'failed'` event 비동기 통보 → `onFailed` callback path + 회귀 3
  - #2: real factory Notification 인스턴스 module-scope `Set` 보관 (GC 방지)
  - #3: fallback display-only 명시 (IPC payload `onClick` action 미포함) + 회귀 2

회귀 +21 (16 + hotfix 5) / 단위 1278 → 1299.

### 14.5 PR #213 — T18 BackgroundTranslationQueue (queue skeleton + policy)

산출물: `src/main/BackgroundTranslationQueue.ts` (+559 신규) + `tests/unit/main/BackgroundTranslationQueue.test.ts` (+721 신규) = 2 files / **+1280 / -0**

설계 (codex T18 사전 구체 협의 옵션 D — 단일 파일에 4 구성):
- `TranslationJob` 타입 (PRD §14.3.1 schema 1:1)
- `TranslationJobStore` interface + `InMemoryTranslationJobStore` 디폴트
- `BackgroundTranslationQueue` 클래스
- `QueuePolicy` 디폴트 (minStartIntervalMs=60000 / thresholdWindowMs=5h / thresholdPages=300 / progressCoalesceMs=5000)

5 위험 사전 박음:
1. durable resume — interface 자리 + in_progress startup 리셋
2. rate limit 우회 — dispatch 루프 throttle (큐 내부 강제)
3. Codex OAuth 한도 — thresholdExceeded warn-only (차단 X)
4. cancel race — AbortController + completed 직전 status 재확인
5. progress 과다 update — coalesce 5초 또는 100% 즉시

dual review:
- evaluator: Pass 9 / Partial 0 / Fail 0 → PASS
- codex: BLOCKING 0 / NEEDS_CHANGES 4 / NB 다수 → 본 PR `a5095cc` hotfix 흡수:
  - #1: `start()` 의 `recoverRunnableJobs()` activeJob race 차단
  - #2: InMemoryStore `update()` 참조 누출 차단 (shallow copy)
  - #3: `countCompletedInWindow(providerId, windowStart)` interface 메서드 추가 (SQLite swap 시 O(N) full scan 회피)
  - #4: `thresholdExceeded` provider 별 dedupe (below→above crossing 시점에만 emit)

회귀 +30 (26 + hotfix 4) / 단위 1299 → 1329.

### 14.6 학습 #18 10번째 확증 (codex 사전 dual review 본질적 가치)

본 세션 M4 시점 4 PR 내 흡수한 codex BLOCKING + NEEDS_CHANGES:

| PR | codex 발견 | 본질 | hotfix |
|---|---|---|---|
| #210 T21 | NEEDS_CHANGES #1 | autoTagger throw → createNote 자체 throw | try/catch + autoTaggingStatus='failed' + 회귀 1 |
| #211 T22 | NB-HF | estimate input null/undefined/non-string TypeError | 'workspaceId required' throw 통일 + 회귀 4 |
| #212 T19 | NEEDS_CHANGES #1 | Electron 'failed' event 비동기 통보 미처리 | onFailed callback path + 회귀 3 |
| #212 T19 | NEEDS_CHANGES #2 | real factory Notification 인스턴스 GC 위험 | module-scope active Set + cleanup |
| #212 T19 | NEEDS_CHANGES #3 | fallback IPC payload onClick action 유실 | display-only 명시 + 회귀 2 |
| #213 T18 | NEEDS_CHANGES #1 | start() recover activeJob race | activeJob 시 recover skip + 회귀 1 |
| #213 T18 | NEEDS_CHANGES #2 | InMemoryStore.update() 참조 누출 | shallow copy + 회귀 1 |
| #213 T18 | NEEDS_CHANGES #3 | threshold O(N) full scan SQLite swap 시 고정 | countCompletedInWindow interface + 회귀 1 |
| #213 T18 | NEEDS_CHANGES #4 | thresholdExceeded 매 completed 마다 반복 emit | provider 별 dedupe + 회귀 1 |

**모두 본 PR 내 hotfix 흡수**. 후속 분리 PR 0. 학습 #18 **10번째 확증**.

### 14.7 KI 변동

- M4 closed: **KI-005 [open] LOW → [closed] (PR #210 T21)**
- M4 신규: 없음 (codex NEEDS_CHANGES 모두 본 PR 흡수)
- M4 종료 시점 통계: Phase 1 해소 4 / 잔여 16 (HIGH 1 / MEDIUM 3 / LOW 12) + Phase 2 잔여 1 (KI-021)

### 14.8 본 세션 종료 시점 검증

| 검증 | 결과 |
|---|---|
| `npm run lint` | PASS |
| `npm run typecheck` (node + web) | PASS |
| `npm test` | **1329 PASS** |
| `npm run build` | PASS |
| 머지 commit | `c28482d` (PR #213 head — T18 마지막) |
| 원격 브랜치 | `--delete-branch` 자동 정리 정합 (G-017) |
| git status main | clean |

---

## 15. M4 잔여 T20 (NoteHighlight DOM anchor) — 다음 세션 진입

### 15.1 진입 권고 (codex 사전 협의 정합)

본 M4 잔여 T20 = NoteHighlight DOM anchor. codex 권고 시 "M4 중 위험도 가장 높음 — schema + WebContents DOM + renderer overlay 종합".

### 15.2 codex 권고 설계 (이전 협의)

- 현 NoteStore schema 에 DOM highlight anchor 필드 부재 — 먼저 persistence 설계 필요
- 옵션:
  - (a) `highlight_anchor` 컬럼을 Note 에 추가
  - (b) **별도 `highlights` / `note_highlights` 테이블** (codex 권고 — 기존 deployed table ALTER 부담 축소)
- DOM 책임 분리:
  - selection serialize/replay = WebContents DOM
  - 저장/재방문 hook = main
  - 표시 컴포넌트 = renderer
- Rangy 신규 dependency 비추천 — W3C Selection/Range API 사용
- anchor 시그니처 (selector-only 금지):
  - `selector + text node path/index + start/end offset + selectedText + prefix/suffix + content_hash`

### 15.3 본 PR scope 옵션 (다음 세션 사전 협의 후 결정)

**옵션 A (가장 작게)**: HighlightStore (in-memory) + Anchor 타입 + anchor serialize/deserialize 유틸 (pure)
- 신규 파일 2~3개 + 회귀
- schema 변경 0 / renderer UI 후속 PR
- G-013 1단계 정합

**옵션 B (Store 동반)**: Highlight schema + HighlightStore (SQLite) + anchor 유틸 + 단위 회귀
- schema 변경 — `v05.sql` 또는 마이그레이션 (G-014 dry-run + 백업 동반)
- renderer UI 후속

**옵션 C (전체 박음)**: 본 PR 매우 큼, M4 일정 초과 가능

권고 — 다음 세션 codex 와 협의 후 옵션 A 가장 가능성 높음 (G-013 1단계, schema 변경 회피).

### 15.4 잠재 후속 KI 후보 (codex 사전 협의)

- DOM anchor drift — 동적 DOM / PDF viewer 에서 복원 실패 (별도 KI 등록 가능성)
- selector drift — SPA 재방문 시 DOM 변경
- WebContentsView 좌표 offset
- PDF page segmentation / extraction 품질

---

## 16. 본 세션 절대 최종 종료 핸드오프

**Sprint 016 M2~M4 (T09~T22) 中 13/14 작업 완료 (T20 다음 세션)**:

| Milestone | 작업 | 머지 PR | 단위 +Δ |
|---|---|---|---|
| M2 어댑터 일괄 제거 | T09~T13 (5종 시퀀스) | #198~#203 + docs #204 = 7 PR | +0 |
| M3 cookies partition + Export/Import | T14~T17 (4 PR) | #205~#208 + docs #209 = 5 PR | +68 |
| M4 백그라운드 번역 + 하이라이트 | T21 + T22 + T19 + T18 (4 PR) | #210~#213 + 본 docs (예정) = 5 PR | +98 |
| **누적** | **13 작업 + 3 docs = 16 PR** | | **+166** (1163 → 1329) |

**누적 통계**:
- 코드 PR 12 + docs PR 4 = 16 PR
- 단위 1163 → **1329** (+166)
- codex 사전 BLOCKING + NEEDS_CHANGES 누적 **17건 모두 본 PR hotfix 흡수**:
  - M2: 6건 (#198 T12 BLOCKING #1 / #199 T11 BLOCKING #1+#2 + NEEDS_CHANGES #1 / #201 T10a NEEDS_CHANGES #1 / #202 T10b NEEDS_CHANGES #8)
  - M3: 7건 (#205 T14 NC 1 / #207 T16 BLOCKING 1 + NC 2 / #208 T17 BLOCKING 2 + NC 1)
  - M4: 8건 (#210 T21 NC 1 / #211 T22 NB-HF 1 / #212 T19 NC 3 / #213 T18 NC 4) — 학습 #18 **10번째 확증**
- KI 변동: closed 3 (KI-002 M2 / KI-005 M4 / KI-008 M3) + 신규 2 (KI-021 + KI-022 M3 NB)
- 가드레일 G-015 cookies partition 격리 완전 실효 (M3)
- 학습 #19 신규 (codex 협의 시퀀스 모범 패턴, M2 박힘)
- 자기 검증 루프 **32회차 SUCCESS** (#181~#213)

**다음 세션 진입점**: `.flowset/handoffs/2026-05-21.md` §15 — **Sprint 016 M4 T20 (NoteHighlight DOM anchor)** / 권고 0순위. codex 사전 협의 후 옵션 A 가장 가능성 (HighlightStore in-memory + Anchor 타입 + W3C Range serialize/deserialize 유틸).

**M4 종료 후 진입 권고**:
- **0순위: T20 NoteHighlight** (M4 완료) — 본 세션 마지막 잔여
- **1순위: M5 종합** (T23~T26) — 단위 테스트 + PRD v0.4.1 + 종합 evaluator + Sprint 017 시안
- **2순위: KI-022 후속** (Import 후 embedding_queue 재 enqueue)
- **3순위: KI-021 후속** (partition cleanup reconcile path)
- **4순위: G-013 3단계 후속** (ChatRequest.signal + EmbeddingClient in_progress 보호)

---

## 17. Sprint 016 M4 T20 NoteHighlight DOM anchor 완료 + KI batch 신규 (M4 5/5 종결)

### 17.1 PR #215 머지 결과

**T20 NoteHighlight DOM anchor (G-013 1단계 — 옵션 A) PR #215 머지** `1082990` (2026-05-21T11:05:56Z UTC).

산출물 (신규 4 파일, +1505 / -0 / net = gross):

| 파일 | + | 비고 |
|---|---|---|
| `src/perception/highlightAnchor.ts` | 477 | pure W3C Range serialize/deserialize |
| `src/storage/HighlightStore.ts` | 178 | in-memory store (1:N noteId→highlight) |
| `tests/unit/perception/highlightAnchor.test.ts` | 365 | 21 회귀 (happy-dom) |
| `tests/unit/storage/HighlightStore.test.ts` | 485 | 24 회귀 |

회귀 +45 / 단위 **1329 → 1374**. PR 누적 **114** (#215).

### 17.2 설계 (codex 사전 협의 threadId 019e4a06 정합)

- text node path = root.childNodes 자식 인덱스 배열 (element-only `children` 아님)
- contentHash + contextHash 둘 다 보관 — hard gate 아니라 confidence/fallback 판단값
- prefix/suffix 32 chars (Array.from 기준 surrogate 안전)
- fallback 우선순위: contentHash 일치 시 path fast path → fuzzy fallback → ambiguous null
- iframe / Shadow DOM / PDF viewer 명시 차단 (range root 외부 시 throw)
- noteId 1:1 X — 한 노트가 여러 highlight 가능 (codex 권고 정합)
- Range 의 실제 character offset 기반 prefix/suffix 추출 (selectedText 첫 occurrence 가 아닌 정확한 위치)
- TreeWalker 대신 수동 재귀 collectTextNodes (happy-dom 호환)

### 17.3 dual review 결과 (학습 #18 11번째 확증)

**codex 사전 dual review** (threadId 019e4a16):

| 발견 | 본질 | hotfix |
|---|---|---|
| NEEDS_CHANGES #1 | `listByPage` 의 `filter.pageId !== undefined` 분기가 `pageId:null` 명시 시 cross-URL 노출 | `hasRealPageId = filter.pageId != null` + 회귀 +2 (commit `18d9466`) |
| NB-4 | `serializeRange` empty (collapsed) selection 방어 | collapsed range throw + 회귀 +1 (commit `18d9466`) |
| NB-2 | `rootSelector` docstring 명확화 (deserialize 가 사용 X) | docstring 박음 (commit `18d9466`) |
| NB-1 / NB-3 / NB-5 | fallback ordering / element container path / U+241F delimiter | KI-025 본문 sub-section 흡수 (Phase 3 후속) |

**evaluator** (Pass 7 / Partial 1 / Fail 0 → 머지 가):

- Pass: PRD §4.3.4 + §16 정합 / G-013 1단계 / codex 협의 5항 / 5 위험 / 단위 회귀 11+ 4배 초과 / 단위 누계 / 가드레일·코드 품질
- Partial 1: §6 PR body 매트릭스 net=gross=1505 박음 권고 → PR body 정정
- NB: `returns null when ownerDocument is missing` placeholder (`expect(true).toBe(true)`) 분기 cover 0 → 실 cover fakeRoot 권고 → 본 PR `564f1ee` 흡수

**codex 사후 협의** (threadId 019e4a2b — 옵션 C 권고): 18d9466 + ownerDocument hotfix 추가 commit 후 PR 생성. KI 후보 4건은 머지 후 별도 docs PR. **본 PR 정합 진행**.

본 PR 내 hotfix 흡수 누계 **4건** (codex NC 1 + NB 2 + evaluator NB 1) + PR body 매트릭스 정정 1건 (G-018 net=gross=1505) + 후속 위임 KI batch 4건 (별도 본 docs PR — codex 사후 협의 threadId 019e4a2b 옵션 C 정합).

본 docs PR 자체 dual review (codex threadId 019e4a3c — NEEDS_CHANGES 3 + NB 2 / evaluator Pass 6 Partial 2 Fail 0) 도 본 docs PR 내 hotfix 흡수 — KI 통계 산식 재집계 + KI-026 PRD 경로 정정 + state.md 하단 핸드오프 로그 갱신 + T20 hotfix 누계 일관 (4건) + KI-025 sub-section 명시 (NB-1/NB-3/NB-5). 학습 #18 11번째 확증 + 본 docs PR dual review hotfix 5건 추가 흡수.

### 17.4 KI 후보 4건 신규 등록 (본 docs PR)

T20 PR 머지 후 별도 docs PR (codex 권고 정합) 에 batch 등록:

| KI | Severity | 본문 |
|---|---|---|
| **KI-023** | LOW | HighlightStore PDF viewer 내부 selection DOM Range 캡처 미지원 — Chromium 내장 PDF plugin 별도 path 필요. Phase 3 R&D |
| **KI-024** | LOW | Shadow DOM cross-boundary range graceful fallback 부재 — renderer overlay 후속 PR 에서 try/catch + toast 안내 |
| **KI-025** | LOW | contentHash 미일치 시 path 폐기 보수성 — confidence score 노출 + Phase 3 후속 R&D. codex NB-1/NB-3/NB-5 sub-section 흡수 |
| **KI-026** | LOW | contract L62 "PRD §11.2.1 highlights" 표기 PRD 본문 불일치 — M5 T24 PRD v0.4.1 발행 시점 정정 (옵션 B PRD §11.5 신설 권장) |

KI 통계 변동: Phase 1 잔여 16 → **19** (KI-023~025 신규 3) + KI-026 (Phase 1) = LOW 12 → 15. Phase 2 잔여 1 유지. **총 잔여 17 → 20건**.

### 17.5 Sprint 016 M4 5/5 종결 (T18~T22 전체 완료)

| T | 작업 | PR | 머지 commit | 단위 +Δ |
|---|---|---|---|---|
| T21 | AutoTagger.tagNote (KI-005 closed) | #210 | `131c5c8` | +20 |
| T22 | UserLevelEstimator mock (R3-B) | #211 | `9188524` | +27 |
| T19 | NotificationService (Electron + fallback) | #212 | `3c0a6a7` | +21 |
| T18 | BackgroundTranslationQueue (queue skeleton + policy) | #213 | `c28482d` | +30 |
| T20 | NoteHighlight DOM anchor (HighlightStore + Anchor 유틸) | **#215** | **`1082990`** | **+45** |
| **누적** | **5/5** | **5 코드 PR** | | **+143** (1231 → 1374) |

docs PR: M4 4/5 시점 `docs/WI-S016M4-docs-handoff-state` #214 + T20 종결 시점 본 docs PR (분리).

### 17.6 본 세션 종료 검증

| 검증 | 결과 |
|---|---|
| `npm run lint` | PASS |
| `npm run typecheck` | PASS (node + web) |
| `npm test` | **1374 PASS** |
| `npm run build` | PASS |
| T20 PR #215 머지 commit | `1082990` |
| 원격 브랜치 자동 정리 | `--delete-branch` 정합 (G-017) |
| local stale 브랜치 | 5건 잔여 — main 정리 후 일괄 삭제 가능 |

### 17.7 학습 #18 11번째 확증

T20 PR 본 PR 내 hotfix 흡수 5건 (codex NC 1 + NB 2 + evaluator NB 1 + PR body 매트릭스 정정 1) + 후속 위임 KI 4건. codex 사전 협의 + dual review + 사후 협의 3단계 + evaluator 모두 호출 — 단일 PR 안에서 dual review 권고 모두 흡수 또는 명시 위임. **본 패턴 11번째 확증**.

추가 학습 — codex 사후 협의 (threadId 019e4a2b) 옵션 A/B/C 선택 시 외부 carry over 상황에서도 정합 path 결정 가능. 본 세션은 working tree 4 파일 carry over 가 hotfix 이전 본문이었으나 옵션 C (remote head 기준 + ownerDocument hotfix 만 추가) 로 안전 진행.

### 17.8 다음 세션 진입점

**Sprint 016 M5 종합 (T23~T26)** — 권고 0순위:

- T23 단위 테스트 — 회귀 셋 + 신규 컴포넌트 + Phase 2 진입 검증 (목표 +60~80)
- T24 PRD v0.4.1 발행 — Phase 2 진입 메타 + KI 19→ ≤6 해소 + 어댑터 정리 종결. **KI-026 권고 옵션 B (PRD §11.5 highlights 신설) 결정 시점**
- T25 종합 evaluator + 핸드오프 + state/known-issues 갱신
- T26 Sprint 017 contract 시안 (Phase 3 진입 — 로컬 LLM / Notion Export / 공유)

**M5 종료 후 진입 권고**:
- 1순위: T20 후속 renderer UI overlay (`src/renderer/src/note/NoteHighlight.tsx` + WebContentsView selection 캡처 + did-finish-load 복원 trigger) — G-013 2단계
- 2순위: 옵션 B 후속 (Highlight schema + HighlightStore SQLite swap + 마이그레이션) — G-014 dry-run + 백업
- 3순위: KI-022 후속 (Import 후 embedding_queue 재 enqueue)
- 4순위: KI-021 후속 (partition cleanup reconcile path)
- 5순위: G-013 3단계 후속 (ChatRequest.signal + EmbeddingClient in_progress 보호)

자기 검증 루프 **33회차 SUCCESS** (#181~#215, 33 PR 누적 — M4 5 PR + M3 4 PR + M2 5 PR + M0 잔여 등).
