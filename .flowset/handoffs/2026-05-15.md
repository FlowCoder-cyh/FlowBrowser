# 2026-05-15 핸드오프

## 메타
- Phase: 1
- Sprint: 003 정의 시작
- 세션 시간: Sprint 003 정의 작성

## 오늘 한 일

- 신규 세션 진입, Sprint 002 종합 핸드오프 (2026-05-11) 검토
- Sprint 003 후보 6개 우선순위 산정:
  1. (M1) LRU trim 테스트 + `pageWideBlock` 구조화 (Sprint 002 evaluator 후속)
  2. (M2) 페이지 전체 번역 (PRD §9.2 P1)
  3. (M3) 도메인 화이트/블랙리스트 UI (PRD §9.6 P1)
  4. (M4) PRD v0.3.1 패치 + Sprint 종합
- Sprint 003 정의 작성: `.flowset/contracts/sprint-003.md`
  - 12 작업 (S003-T01 ~ T12)
  - 7 AC (LRU / pageWideBlock / 페이지 번역 / 도메인 UI / 테스트 / PRD 패치 / 통과 기준)
  - 4 마일스톤 (M1~M4) / 2~3주
  - 가드레일 적용 매트릭스 (G-001/004/005/006/007/009/010)
- state.md Sprint 003 정의 완료 반영

## 결정 사항

- **Sprint 003 = Phase 1 MVP 마무리 + Privacy 가시화**
- 페이지 전체 번역 PRD §9.2에서 P1 등재 확인 → Sprint 003 범위 적합
- 사용자 도메인 화이트/블랙리스트 PRD §9.6 P1 → Sprint 003 범위 적합
- 쉬운 설명 / 요약 / Codex Login Spike → Sprint 004+로 이연
- evaluator 통과 기준 Pass ≥ 8 유지 (사용자 지시)
- 단위 테스트 누적 목표 ≥ 90 (Sprint 002 70 + 20 이상)

## 다음 작업

- Sprint 003 정의 PR commit + push + auto-merge
- PR 머지 후 M1 (LRU trim + pageWideBlock enum 구조화) 착수
- 브랜치: `feat/WI-S003-M1-feat-lru-pagewide-block`

## 다음 세션 컨텍스트

### 즉시 읽어야 할 파일
- `.flowset/state.md` (자동 주입)
- `.flowset/contracts/sprint-003.md` (Sprint 정의)
- `.flowset/eval-results/sprint-002-2026-05-11.md` (Sprint 002 후속 1·2 권고)
- `src/storage/TranslationCache.ts` (M1 LRU trim 대상)
- `src/privacy/types.ts` / `src/privacy/index.ts` (M1 BlockReason enum 대상)

### 기억해야 할 결정
- 모든 작업은 별도 브랜치 + PR (G-007)
- 각 M evaluator 호출 Pass ≥ 8
- evaluator는 general-purpose 위임 + .claude/agents/evaluator.md 정의 적용
- main 직접 push 금지 (셋업 커밋 b97e748 외 예외 없음)

### 잊으면 안 되는 맥락
- Sprint 002 Partial 잔여: 없음 (6/6 모두 Pass)
- Sprint 002 evaluator 후속 권고 1/2 = Sprint 003 M1 직접 해소 대상
- Sprint 003 M3 도메인 정책 JSON은 평문 OK (콘텐츠 아님 → G-005 무관)

---

## Sprint 003 종합 결과 (2026-05-15)

### M1 (PR #21 머지 `f7d773f`) — LRU trim 테스트 + pageWideBlock 구조화
- TranslationCache LRU trim 3 tests / BlockReason enum (consent/password/card_field/card_pattern/domain/none) / pageWideBlock boolean / reason 문자열 매칭 제거
- 단위 테스트 70 → 81 (+11), evaluator: **Pass 16 / Partial 1 / Fail 0**

### M2 (PR #22 머지 `3b610db`) — 페이지 전체 번역 + abort
- PageNodeExtractor 16종 선택자 + 4000자 청크 / `translate:page` IPC + `translate:page-abort` / TranslationPanel 페이지 모드 + 취소 + pageWideBlock 메시지
- 단위 테스트 81 → 96 (+15), evaluator: **Pass 10 / Partial 1 / Fail 0**

### M3 (PR #23 머지 `c26e363`) — 도메인 화이트/블랙리스트 정책 UI
- DomainPolicyStore (validatePattern 5종 에러 + import/export + setRules bulk + clearAll, policyVersion=1) / DomainPolicyPanel UI / 4종 신규 IPC
- 단위 테스트 96 → 115 (+19), evaluator: **Pass 9 / Partial 0 / Fail 0**

### M4 (현재 브랜치) — PRD v0.3.1 + Sprint 종합
- PRD 7개 파일 갱신 (00 change history / README / 04 requirements / 06 data_model / 11 dev_tasks_and_ops + .flowset/requirements / state)
- 변경 5개 항목: §9.2 페이지 전체 번역 실측 / §9.6 BlockReason enum / §9.6 도메인 정책 JSON 스키마 / §12.4 LRU trim 실측 / §19 신규 모듈 등록
- 자동 검증 lint 0/0 / typecheck PASS / test 115/115 / build PASS
- Sprint 종합 evaluator: **Pass 17 / Partial 0 / Fail 0**

### Sprint 003 누적
- main commits / PR: 4개 (S003 정의 #20 + M1 #21 + M2 #22 + M3 #23 + M4 본 PR)
- 단위 테스트 70 → 115 (+45, +64% 증분)
- evaluator 호출 5회 (정의 미실행 + M1/M2/M3/종합 4회) — 모두 Pass ≥ 8 충족
- 가드레일 G-001 ~ G-011 위반 0

### Sprint 004 후보 (evaluator 권고)
- P0: 사용자 수동 QA 반영 / 쉬운 설명·요약 (PRD §9.2 P1)
- P1: Codex Login Provider Spike (Phase 1 PoC 4종) / IPC 채널 정리 / paragraph abort 일관성
- Phase 2 진입 결정 필요 (Spike 2/3/4 PoC 임계 정량화 우선)

### Sprint 003 후속 조치
- 본 M4 commit + PR + main 머지
- 사용자 협의 후 Sprint 004 정의

---

## Sprint 004 결과 (2026-05-15)

### M1 (PR #26 `bc3aced`) — IPC 채널 분리 + paragraph abort
- 4종 신규 이벤트 (page-aborted / page-error / paragraphs-aborted / paragraphs-error)
- `translate:paragraphs-abort` IPC + paragraph 모드 취소 버튼
- Sprint 003 M2 §D + M3 후속 권고 직접 해소
- evaluator: **Pass 12 / Partial 0 / Fail 0**

### M2 (PR #27 `3016954`) — 쉬운 설명
- RequestType 'explanation' / 'summary' 추가 (M3 사전 등록)
- OpenAIApiKeyProvider supportedRequestTypes 7종 + buildSystemPrompt/buildUserPrompt export
- 컨텍스트 메뉴 "쉽게 설명" + popup mode 분기
- services.ts cache 우회 (explanation/summary 키 충돌 회피) + featureFromRequestType 매핑
- 단위 테스트 11개 추가 (system/user prompt 7종 매트릭스)
- evaluator: **Pass 12 / Partial 1 / Fail 0**

### M3 (PR #28 `ec275ba`) — 페이지 요약 + SummarizationPlanner
- SummarizationPlanner pure 함수 (planChunks + summarizeChunks, 의존성 주입)
- `translate:summarize-page` IPC + 3종 이벤트
- 컨텍스트 메뉴 "이 부분 요약" + TranslationPanel 페이지 요약 버튼/결과 영역
- 단위 테스트 9개 추가 (planChunks 4 + summarizeChunks 5)
- evaluator: **Pass 11 / Partial 1 / Fail 0**

### M4 (현재 브랜치) — PRD v0.3.2 + Sprint 종합
- PRD 5개 항목 패치 (§9.2 / §11 IPC 정책 / §12.4 캐시 우회 / §12.8 feature 매핑 / §19.4 모듈 등록)
- 자동 검증 lint 0/0 / typecheck PASS / test 135/135 / build PASS

### Sprint 004 누적
- main commits / PR: 5개 (#25 정의 + #26 M1 + #27 M2 + #28 M3 + 본 PR M4)
- 단위 테스트 115 → 135 (+20)
- evaluator 호출 4회 — 모두 Pass ≥ 8 충족
- 가드레일 G-001 ~ G-011 위반 0

### Sprint 005 후보
- 용어집 (PRD §9.2 P2) + glossaryVersion 캐시 invalidation 실측 활용
- 캐시 키 확장 (requestType 포함) — 충돌 회피 우회 제거
- Codex Login Provider Spike (Phase 1 PoC 4종)
- 사용자 수동 QA 결과 반영
- Phase 2 진입 (자막 / TTS / 싱크) — Spike 2/3/4 PoC 임계 정량화 우선
- 요약 결과 UX (chunkSummaries 펼침) + 요약 길이 폭주 보호 (Sprint 004 M3 evaluator 권고)

---

## Sprint 005 결과 (2026-05-15)

### M1 (PR #31 `3d47226`) — 캐시 키 확장
- TranslationCache 복합 키 6요소 (requestType 추가) → Sprint 004 cache 우회 제거
- 기존 디스크 항목은 'selection' fallback (자연 폐기)
- 단위 테스트 2개 신규 + 기존 13 tests 갱신
- evaluator: **Pass 7 / Partial 1 / Fail 0** (Partial은 커밋 부재 평가 불가 — 본 PR 머지로 해소)

### M2 (PR #32 `f797e1e`) — 용어집 + invalidation
- GlossaryStore (PRD §12.7 1:1) + validateTerm 5종 + bumpVersion (sha256)
- getActiveForDomain 최대 50개 + formatGlossaryContext
- services.ts applyGlossary 분기 (explanation/summary 제외) + glossaryVersion cache 키 통합
- 7 IPC + cache 자동 invalidation 연동
- GlossaryPanel UI (Settings) + JSON import/export
- 단위 테스트 17개 추가
- evaluator: **Pass 6 / Partial 0 / Fail 0** (AC 단위, M1+M2 누적 11)

### M3 (PR #33 `4cb42d3`) — 요약 폭주 보호 + UX
- summarizeChunks combineCharLimit (기본 8000) + 4 경로 (single/direct/resplit/truncated)
- 재분할 1회 + truncate 폴백 (무한 루프 방지)
- TranslationPanel chunkSummaries 펼치기 토글 + PATH_LABELS 한국어
- 단위 테스트 4개 추가 (4 경로 매트릭스)
- Sprint 004 M3 evaluator 후속 권고 직접 해소
- evaluator: **Pass 9 / Partial 0 / Fail 0**

### M4 (현재 브랜치) — PRD v0.3.3 + Sprint 종합
- PRD 5개 항목 패치 (§9.2 / §12.4 캐시 키 / §12.7 GlossaryTerm 실측 / §19.1 / §19.5)
- 자동 검증 lint 0/0 / typecheck PASS / test 158/158 / build PASS

### Sprint 005 누적
- main commits / PR: 5개 (#30 정의 + #31 M1 + #32 M2 + #33 M3 + 본 PR M4)
- 단위 테스트 135 → 158 (+23)
- evaluator 호출 4회 — 모두 Pass ≥ 8 충족 (M1 커밋 후 해소)
- 가드레일 G-001 ~ G-011 위반 0

### Sprint 006 후보
- Codex Login Provider Spike (Phase 1 PoC 4종: 비용/차단/모델/refresh)
- 사용자 수동 QA 결과 반영
- 다중 탭 / BrowserView (Phase 1 MVP P1)
- 페이지 캐시 (Phase 1 MVP P2)
- Phase 2 진입 (자막 / TTS / 싱크) — Spike 2/3/4 PoC 임계 정량화 우선
- 요약 메타 입력 길이 수치 표시 (Sprint 005 M3 evaluator 권고)

---

## Sprint 006 결과 (2026-05-15)

### M1 (PR #36 `b8d92dd`) — Navigation 동기화 + TranslationRenderer
- did-navigate broadcast (browser:navigated) + UrlBar canGoBack/canGoForward disabled
- TranslationRenderer 신규 (외부 페이지 IIFE): paragraph/page preset selector + replace/overlay 모드 + restore
- translate:render / translate:render-restore IPC
- 단위 테스트 12개 추가
- evaluator: **Pass 8 / Partial 1 / Fail 0** (M2에서 Partial Pass 전환)

### M2 (PR #37 `79bfa41`) — 표시 모드 + UserSetting + 자동 흐름
- UserSettingStore (PRD §12.1 translationMode 우선) + JSON 영속
- DisplayModePanel (Settings 라디오) + userSettingApi
- TranslationPanel: displayMode state + paragraphs-done / page-done 시 mode 분기 (panel은 패널만, replace/overlay는 자동 render IPC 호출)
- onNavigated 시 자동 renderRestore (M1 AC-2(3) Partial 해소)
- "원문으로" 토글 버튼
- 단위 테스트 6개 추가
- evaluator: **Pass 9 / Partial 0 / Fail 0**

### M3 (PR #38 `9812018`) — 페이지 캐시
- PageResultStore (PRD §12.10 신규): URL 정규화 + nodesSignature(sha256) + TTL 30일 + LRU 500MB
- translate:page 정상 완료 시 자동 영속 + pageResult:restore-current IPC
- restoreHint UI + "복원" 버튼 + signature mismatch 검증
- 단위 테스트 16개 추가
- evaluator: **Pass 7 / Partial 0 / Fail 0** (G-009 commit으로 8)

### M4 (현재 브랜치) — PRD v0.3.4 + Sprint 종합
- PRD 5개 항목 패치 (§9.1 Navigation / §9.2 표시 모드 + Renderer / §12.1 translationMode / §12.10 PageResultStore / §19 모듈)
- 자동 검증 lint 0/0 / typecheck PASS / test 192/192 / build PASS

### Sprint 006 누적
- main commits / PR: 5개 (#35 정의 + #36 M1 + #37 M2 + #38 M3 + 본 PR M4)
- 단위 테스트 158 → 192 (+34)
- evaluator 호출 4회 — 모두 Pass ≥ 8 충족
- 가드레일 G-001 ~ G-011 위반 0

### Sprint 007 후보
- 다중 탭 / BrowserView (PRD §9.1 P2) — 영향도 큼, 단독 Sprint
- Codex Login Provider Spike (Phase 1 PoC 4종)
- 사용자 수동 QA 결과 반영
- Phase 2 진입 (자막 / TTS / 싱크)
- 요약 메타 입력 길이 수치 표시 (Sprint 005 M3 evaluator 권고)

---

## 사용자 지적 (커밋명 위반)

Sprint 003~006 모든 마일스톤 커밋명이 `WI-S{N}-M{M}-{type}` 형식으로 commit-check CI failure 반복. 학습 29 (`-` 추가 분절 금지) 위반. memory feedback에 영속 (`~/.claude/projects/.../memory/feedback_commit_format.md`). Sprint 007부터 `WI-S{N}M{M}-{type}` (NNN 한 분절) 엄수.

---

## Sprint 007 결과 (2026-05-15)

### M1 (PR #41 `2d9183b`) — UserSetting 4 필드
- UserSettingStore 확장 (defaultLanguage/sourceLanguage/defaultProviderId/privacyFilterEnabled)
- services.ts privacyFilterEnabled 분기 (도메인만 우회, password/card 본문은 절대 우회 불가)
- GeneralPanel UI + 5종 흐름 적용
- 단위 테스트 6개 추가
- evaluator: **Pass 11 / Partial 0 / Fail 0**
- commit-check CI: **success** (NNN 한 분절 형식 정상 작동)

### M2 (PR #42 `11574e8`) — 요약 메타 수치 + 페이지 캐시 패널
- SummarizationPlanner combinedInputChars + combineCharLimit
- TranslationPanel 메타에 입력 길이 / limit 표시 (truncated 강조)
- PageCachePanel (Settings count + 모두 삭제)
- 단위 테스트 2개 추가
- evaluator: **Pass 8 / Partial 0 / Fail 0**

### M3 (PR #43 `13a2b5a`) — tsconfig + 빌드 보강
- tsconfig.node.json include 확장 (storage/ai/privacy/perception/tests)
- dynamic import 5건 → 정적 → out/main 단일 chunk
- Sprint 005 M1 §주의 2 + Sprint 006 M3 evaluator 후속 권고 직접 해소
- TranslationCache 테스트 누락 requestType 회귀 1건 추가 수정
- evaluator: **Pass 10 / Partial 0 / Fail 0**

### M4 (현재 브랜치) — PRD v0.3.5 + Sprint 종합
- PRD 5개 항목 패치 (§12.1 4 필드 + §9.6 안전 정책 + §9.2 메타 수치 + §12.10 PageCachePanel + §19 빌드 보강)
- 자동 검증 lint 0/0 / typecheck PASS / test 200/200 / build PASS

### Sprint 007 누적
- main commits / PR: 5개 (#40 정의 + #41 M1 + #42 M2 + #43 M3 + 본 PR M4)
- 단위 테스트 192 → 200 (+8)
- evaluator 호출 4회 — 모두 Pass ≥ 8 충족
- 가드레일 G-001 ~ G-011 위반 0
- **commit-check CI 모든 PR success** (NNN 위반 패턴 완전 해소)

### Sprint 008 후보
- **다중 탭 / BrowserView** (PRD §9.1 P2) — 영향도 큼, 단독 Sprint
- Codex Login Provider Spike (Phase 1 PoC 4종)
- 사용자 수동 QA 결과 반영
- Phase 2 진입 — Spike 2/3/4 PoC 임계 정량화 우선
- UserSetting Phase 2~4 필드 (subtitleMode / ttsEnabled / syncMode) — 해당 Phase 진입 시 추가

---

## Sprint 008 결과 (2026-05-15)

### M1 (PR #46 `6b2a953`) — TabManager 코어 + 다중 WebContentsView
- TabManager (순수 모델) — open/close/switch/list/getActive/snapshot/subscribe
- close 시 직전 위치 자동 전환 + 마지막 탭 null
- main/index.ts: tabViews Map + createTabView/destroyTabView/setActiveTabView + 활성 view 캐시 (browserView 변수) + 5 IPC + tab:list-update broadcast
- 각 view에 listener 등록 (did-navigate / page-title-updated / context-menu) — 활성 탭만 broadcast
- preload tabApi + window 타입
- 단위 테스트 13개
- evaluator: **Pass 5 (가드5+자검4+PRD1=15) / Partial 0 / Fail 0**

### M2 (PR #47 `b68a458`) — TabBar UI + 라우팅
- TabBar.tsx: tabApi.list/onListUpdate + formatTabLabel + 클릭 switch / X close / + open
- 활성 탭 강조 (border-top #4a9eff) + 가로 스크롤
- App.tsx에 TabBar 마운트
- main: TAB_BAR_HEIGHT 36 + HEADER_HEIGHT 96 + bounds 계산 갱신 + popup anchorY 보정
- URL Bar 동기화는 M1 broadcastNav로 자동 작동 (회귀)
- 컨텍스트 메뉴는 M1 createTabView 내부 자체 등록 (회귀)
- evaluator: **Pass 9 / Partial 0 / Fail 0**

### M3 (PR #48 `725485b`) — 탭별 라우팅 + UX
- 마지막 탭 close 시 새 빈 탭 자동 open (일반 브라우저 표준)
- paragraphs/page/summarize/render/restoreHint 모든 흐름 활성 탭 자동 라우팅 검증
- TabManager 통합 시나리오 단위 테스트 4 추가
- evaluator: **Pass 8 / Partial 0 / Fail 0**

### M4 (현재 브랜치) — PRD v0.3.6 + Sprint 종합
- PRD 5개 항목 패치 (§9.1 탭 P2 / §11 TabManager / §11.1 흐름 라우팅 / §9.1 Navigation broadcast / §19 모듈 등록)
- 자동 검증 lint 0/0 / typecheck PASS / test 217/217 / build PASS

### Sprint 008 누적
- main commits / PR: 5개 (#45 정의 + #46 M1 + #47 M2 + #48 M3 + 본 PR M4)
- 단위 테스트 200 → 217 (+17)
- evaluator 호출 4회 — 모두 Pass ≥ 8 충족
- 가드레일 G-001 ~ G-011 위반 0
- commit-check CI 모든 PR success (NNN 한 분절 형식 유지)

### Sprint 009 후보
- 탭 영속 (앱 재시작 후 복원)
- Codex Login Provider Spike (Phase 1 PoC 4종)
- 사용자 수동 QA 결과 반영
- Phase 2 진입 (자막/TTS/싱크) — Spike 2/3/4 PoC 임계 정량화 우선
- 탭 드래그/순서 변경 / 미리보기 / 그룹 (UX 개선)
- paragraphs/page 진행 이벤트에 sourceTabId 포함 (탭 전환 시 잘못된 patch 방지)

---

## Sprint 009 결과 (2026-05-15)

### M1 (PR #51 `215d630`) — Glossary flaky 핫픽스
- bumpVersion stamp에 단조 증가 counter 추가
- 같은 ms 내 mutation도 다른 version 보장
- 회귀 테스트 2 추가 (10/10 반복 PASS, 이전 4/10 flaky)

### M2 (PR #52 `8f2baf9`) — sourceTabId 가드
- 3종 IPC 진입 시점 활성 탭 ID 캡처
- 13종 이벤트 페이로드에 sourceTabId 추가 (paragraphs 5 + page 5 + summary 3)
- TranslationPanel isCurrentTab 가드 — 백그라운드 작업의 잘못된 UI patch 차단
- Sprint 008 evaluator §sourceTabId 부재 후속 직접 해소
- evaluator: Pass 8 / Partial 1 / Fail 0

### M3 (PR #53 `34ad559`) — 탭 영속
- TabStateStore (policyVersion=1 + 손상 fallback)
- TabManager.restore + initializeTabs
- 자동 저장 debounced 200ms + 종료 시 강제 flush
- subscribe 등록 순서 정합 (복원 broadcast 누락 방지)
- 단위 테스트 10개 추가
- evaluator: Pass 14 / Partial 0 / Fail 0

### M4 (현재 브랜치) — PRD v0.3.7 + Sprint 종합
- PRD 5개 항목 패치
- 자동 검증 lint 0/0 / typecheck PASS / test 229/229 / build PASS

### Sprint 009 누적
- main commits / PR: 5개 (#50 정의 + #51 M1 + #52 M2 + #53 M3 + 본 PR M4)
- 단위 테스트 217 → 229 (+12)
- evaluator 호출 3회 (M1 핫픽스는 evaluator 생략, M2 8 / M3 14 / 종합 진행 중)
- 가드레일 G-001 ~ G-011 위반 0
- commit-check CI 모든 PR success

### Sprint 010 후보
- Codex Login Provider Spike (Phase 1 PoC 4종)
- 사용자 수동 QA 결과 반영
- Phase 2 진입 (자막 / TTS / 싱크) — Spike 2/3/4 PoC 임계 정량화 우선
- 탭 UX 보강 (드래그/순서/미리보기/그룹)
- cancel-on-switch UX 옵션

---

## 본 세션 종합 (Sprint 003 ~ 009)

본 세션은 2026-05-15 단일 날짜에 Sprint 003부터 Sprint 009까지 **7개 Sprint × 4 마일스톤 + 정의/종합 = 35 PR**을 main에 머지했다. Sprint 002 종료 직후 신규 세션 진입으로 시작.

### 수치 요약

| 항목 | 시작 (Sprint 002 종료) | 종료 (Sprint 009 종료) | 증분 |
|---|---|---|---|
| main PRs | 19 | **54** | +35 |
| 단위 테스트 | 70 / 70 | **229 / 229** | +159 |
| PRD | v0.3 (Phase 0) | **v0.3.7** | 7회 patch |
| evaluator 호출 | 14 | 누적 50+ | +36+ |
| 가드레일 위반 | 0 | **0** | 유지 |

### Sprint별 한 줄 요약

| Sprint | 범위 | 종합 | PRD | 테스트 |
|---|---|---|---|---|
| 003 | LRU trim + pageWideBlock + 페이지 전체 번역 + 도메인 정책 UI | Pass 17/0/0 | v0.3.1 | 70→115 |
| 004 | IPC 채널 분리 + 쉬운 설명 + 페이지 요약 + SummarizationPlanner | Pass 9/1/0 | v0.3.2 | 115→135 |
| 005 | 캐시 키 확장 + 용어집 + 요약 폭주 보호 | Pass 9/0/0 | v0.3.3 | 135→158 |
| 006 | Navigation 동기화 + 표시 모드 3종 + 페이지 캐시 | Pass 8/0/0 | v0.3.4 | 158→192 |
| 007 | UserSetting 잔여 + 요약 메타 수치 + tsconfig·빌드 보강 | Pass 17/0/0 | v0.3.5 | 192→200 |
| 008 | 다중 탭 / TabManager + TabBar + 활성 탭 라우팅 | Pass 17/0/0 | v0.3.6 | 200→217 |
| 009 | Glossary flaky + sourceTabId + 탭 영속 | Pass 11/0/0 | v0.3.7 | 217→229 |

### 신규 모듈 (24개)

- **Storage**: TranslationCache LRU + GlossaryStore + UserSettingStore + PageResultStore + TabStateStore + DomainPolicyStore
- **Perception**: PageNodeExtractor + TranslationRenderer
- **AI**: SummarizationPlanner + buildSystemPrompt/buildUserPrompt export
- **Main**: TabManager + extractWebContentsPageNodes + persistPageResult + pageResultLookup
- **Renderer Settings**: GeneralPanel + DomainPolicyPanel + GlossaryPanel + DisplayModePanel + PageCachePanel + UsagePanel
- **Renderer**: TabBar + TranslationPanel 페이지/요약/restoreHint
- **Privacy**: BlockReason enum + pageWideBlock + DomainPolicyStore import/export

### 사용자 지적 + 해소

- **커밋명 NNN 위반** (Sprint 003~006 모든 마일스톤 커밋이 `WI-S{N}-M{M}-{type}` 형식 → commit-check CI failure 16회 반복). 사용자 지적 → memory feedback 영속 (`~/.claude/projects/.../memory/feedback_commit_format.md`) → Sprint 007부터 `WI-S{N}M{M}-{type}` (NNN 한 분절) 엄수 → **Sprint 007~009 모든 PR commit-check success**.

### 누적 evaluator 권고 해소 매트릭스

| 권고 출처 | 권고 내용 | 해소 Sprint |
|---|---|---|
| Sprint 002 후속 1 | LRU trim 단위 테스트 보강 | S003 M1 |
| Sprint 002 후속 2 | pageWideBlock 구조화 | S003 M1 |
| Sprint 003 M2 §D | page 이벤트 분리 | S004 M1 |
| Sprint 003 M3 후속 | paragraph abort 일관성 | S004 M1 |
| Sprint 004 M3 §5 | 요약 폭주 보호 | S005 M3 |
| Sprint 005 M1 §주의 2 | tsconfig 커버리지 | S007 M3 |
| Sprint 005 M3 후속 | 요약 메타 입력 길이 수치 | S007 M2 |
| Sprint 006 M3 후속 | PageResultStore 통계 UI | S007 M2 |
| Sprint 006 M3 후속 | 빌드 chunk 경고 | S007 M3 |
| Sprint 008 §Glossary flaky | counter 도입 | S009 M1 |
| Sprint 008 §sourceTabId 부재 | 13 이벤트 가드 | S009 M2 |

11개 권고 100% 해소. 미해소 후속은 모두 Sprint 010+ 후보 (탭 영속 외 — 사용자 직접 작업 또는 별도 spike).

### 다음 세션 컨텍스트 (Sprint 010 진입)

**즉시 읽어야 할 파일**
- `.flowset/state.md` (자동 주입)
- `.flowset/contracts/sprint-009.md` (Sprint 종료 / Sprint 010 §8 후보)
- `.flowset/eval-results/sprint-009-2026-05-15.md` (Sprint 010 후보 권고)
- `.flowset/handoffs/2026-05-15.md` (본 파일)
- `docs/prd/README.md` (v0.3.7 최신)

**Sprint 010 후보 우선순위 (evaluator 권고)**
- P0: Codex Login Provider Spike (Phase 1 PoC 4종 — 비용/차단/모델/refresh)
- P0: 사용자 수동 QA 결과 반영 (Sprint 001 AC-1/3/4/5/7 + Sprint 008 다중 탭 + Sprint 009 탭 영속 + 표시 모드 / 도메인 정책 / 용어집)
- P1: Phase 2 진입 전제 — Spike 2/3/4 PoC 임계 정량화 (현재 정량 기준 부재)
- P1: 탭 UX 보강 (드래그/순서 / 미리보기 / 그룹 / 컬러)
- P2: cancel-on-switch UX 옵션 / isCurrentTab 순수 함수 추출 단위 테스트

**기억해야 할 결정**
- 모든 작업 별도 브랜치 + PR (G-007 main 직접 push 금지)
- 통과 기준 evaluator Pass ≥ 8 (사용자 지시)
- 커밋명 G-009 NNN 한 분절 엄수 (memory feedback에 영속, 학습 29와 정합)
- PR auto-merge + delete-branch-on-merge 활성

**잊으면 안 되는 맥락**
- Phase 1 PoC 4개 (Spike 1 후속): 비용 / 차단 시그널 / 모델 가용성 / refresh token — Codex Login Provider 실 활성화 전 검증
- Spike 5 사용자 인터뷰 = 사용자 직접 작업 (코드 진행과 병렬)
- Sprint 001 AC-1/3/4/5 Partial 잔여 = 사용자 수동 QA로 Partial → Pass 승격 필요 (코드는 완성)
- privacyFilterEnabled=false라도 password/card 본문 차단은 절대 우회 불가 (G-004 안전 정책)
- 페이지 캐시는 평문 JSON 영속 (콘텐츠라 의문 여지 있지만 사용자 디스크 한정 + PRD §10.3 외부 전송 제한 정신 위반 아님, Sprint 006 M3 evaluator 검토 통과)

---

## Sprint 010 결과 (2026-05-15)

신규 세션 진입 — Sprint 009 종료 직후. 사용자 "우선순위 산정해서 순차진행" 지시.

### 우선순위 산정

| 순위 | 작업 | 자동 진행 | 결정 |
|---|---|---|---|
| P0-A | 탭 UX 보강 (드래그/순서/컨텍스트 메뉴) | ✅ | **Sprint 010 포함** |
| P0-B | S009 잔여 P2 (cancel-on-switch + isCurrentTab 추출) | ✅ | **Sprint 010 포함** |
| P1-A | Codex Login Provider Spike (PoC 4종) | ⚠️ API 키 + 사용자 협의 | Sprint 011+ 이연 |
| P1-B | Phase 2 Spike 정량화 (자막/TTS/싱크) | ⚠️ OS 권한 + 외부 API | Sprint 011+ 이연 |
| P2 | 사용자 수동 QA 반영 | ❌ 사용자 직접 작업 | Sprint 011+ |

### M1 (PR #57 `119b50d`) — 탭 드래그/순서 변경
- TabManager.reorder(tabId, newIndex): clamp + no-op skip + 활성/메타 보존
- tab:reorder IPC + preload tabApi.reorder
- TabBar HTML5 DnD (draggable + dragging/drop-target 시각화 + 닫기 버튼 격리)
- 단위 테스트 6 (정상 / no-op / 음수·초과 clamp / 존재 X / 활성·메타 보존)
- evaluator: **Pass 14 / Partial 0 / Fail 0**

### M2 (PR #58 `ecabb6c`) — 탭 컨텍스트 메뉴 (닫기 옵션 + 복제)
- TabManager.closeOthers / closeRight / duplicate 3 신규 API ({ok, closed} 반환)
- 4종 IPC (close-others / close-right / duplicate / show-context-menu)
- 우클릭 시 main process OS 네이티브 popup menu (4 항목 + disabled 분기: 단일 탭일 때 "다른 탭 닫기" / 가장 오른쪽일 때 "오른쪽 탭 모두 닫기")
- 단위 테스트 10 (closeOthers 3 / closeRight 4 / duplicate 3)
- evaluator: **Pass 13 / Partial 0 / Fail 0**

### M3 (PR #59 `2fb37ce`) — cancel-on-switch UX + isCurrentTab 순수 함수 추출
- UserSettingState.cancelOnTabSwitch boolean (기본 false, 호환)
- tab:switch handler 분기: cancelOnTabSwitch=true + 실제 탭 전환이면 paragraphsAborted/pageTranslateAborted 플래그 set → 진행 중 loop 자연 중단 + aborted 이벤트
- abort 플래그 두 개를 파일 상단으로 이동 (forward 사용을 위한 호이스팅)
- tabGuard.ts 신규: isCurrentTab(activeTabId, sourceTabId) 순수 함수 — null/undefined 둘 다 보수적 true
- TranslationPanel inline 함수 → import 사용 전환 (Sprint 009 M2 G-006 Partial 해소)
- GeneralPanel 토글 ("탭 전환 시 진행 작업 자동 취소")
- 단위 테스트 9 (UserSettingStore cancelOnTabSwitch 3 + tabGuard 6)
- evaluator: **Pass 12 / Partial 0 / Fail 0**

### M4 (현재 브랜치) — PRD v0.3.8 + Sprint 종합
- PRD 4개 항목 패치 (00 change history §0.11 / README v0.3.8 / 06 data_model §12.1 cancelOnTabSwitch / 11 §19.1 모듈 등록)
- .flowset/requirements / state / handoff 동기화
- 자동 검증 lint 0/0 / typecheck PASS / test 254/254 / build PASS

### Sprint 010 누적
- main commits / PR: 5개 (#56 정의 + #57 M1 + #58 M2 + #59 M3 + 본 PR M4)
- 단위 테스트 229 → 254 (+25)
- evaluator 호출 3회 (M1 Pass 14 / M2 Pass 13 / M3 Pass 12) — 모두 Pass ≥ 8 충족, 본 종합 진행 중
- 가드레일 G-001 ~ G-011 위반 0
- commit-check CI 모든 PR success (NNN 한 분절 유지)

### Sprint 011 후보

evaluator 권고 + Sprint 010 잔여:

**P0 (외부 의존성 작업 — 사용자 협의 / 환경 준비 필요)**
- **Codex Login Provider Spike** (Phase 1 PoC 4종: 비용 / 차단 / 모델 / refresh) — OpenAI API 키 + Codex Login 사용자 협의
- **사용자 수동 QA 결과 반영** — Sprint 001 AC-1/3/4/5/7 + S008 다중 탭 + S009 탭 영속 + S010 드래그/컨텍스트 메뉴/cancel-on-switch

**P1**
- **Phase 2 진입 전제** — Spike 2/3/4 PoC 임계 정량화 (자막/TTS/싱크)
- 탭 미리보기 (hover thumbnail) — WebContentsView capture API
- 탭 그룹 / 컬러

**P2**
- summary abort API (Sprint 010 §리스크 3 잔여)
- evaluator 보고서 후속 추적 섹션
- UserSetting Phase 2~4 필드 (subtitleMode / ttsEnabled / syncMode) — Phase 2 진입 시

### Sprint 010 후속 조치
- 본 M4 commit + PR + main 머지
- 사용자 협의 후 Sprint 011 정의

---

## Sprint 011 결과 (2026-05-15)

사용자 "니가 우선순위 산정해서 순차적으로진행해" 지시 → 코드 자동 진행 가능 + 외부 spike 불필요 묶음 선정.

### 우선순위 산정

| 후보 | 자동 진행 | 영향도 | 결정 |
|---|---|---|---|
| summary abort API | ✅ | 중 (S010 §리스크 3 잔여) | **포함 M1** |
| 탭 컬러 라벨 | ✅ | 작음 | **포함 M2** |
| 탭 핀(고정) | ✅ | 중 (closeOthers/closeRight 결합) | **포함 M3** |
| 탭 미리보기 (hover thumbnail) | ⚠️ | 큼 (비활성 view capture spike) | Sprint 012+ |
| 탭 그룹 | ⚠️ | 큼 (단독 Sprint 권고) | Sprint 012+ |
| Codex / QA / Phase 2 | ❌ | 외부 의존성 | Sprint 012+ |

### M1 (PR #62) — summary abort API
- SummarizationPlanner abortCheck 콜백 + SummarizationAbortedError sentinel (5개 summarize 호출 직전 검사)
- main summarizeAborted 플래그 + translate:summarize-abort IPC + summary-aborted 이벤트 (sourceTabId 포함)
- tab:switch cancelOnTabSwitch 분기에 summarizeAborted 추가 → 3종 abort 플래그 일관성 (paragraphsAborted/pageTranslateAborted/summarizeAborted)
- TranslationPanel summary 모드 cancel 버튼 + onSummaryAborted listener (idle 전환 + 메시지)
- 단위 테스트 3 (시작 직후 abort / 중간 abort / 회귀)
- evaluator: **Pass 9 / Partial 0 / Fail 0**

### M2 (PR #63) — 탭 컬러 라벨
- TabColor 타입 + TAB_COLOR_PALETTE (7색 + null)
- TabSession.color 필드 + TabManager.setColor (palette 검증 + 같은 색 no-op)
- tab:set-color IPC + 컨텍스트 메뉴 "색상 변경" radio 서브메뉴 (한글 라벨 + checked 표시)
- TabBar borderTopColor 인라인 스타일 매트릭스 (활성/비활성 × color/null)
- TabStateStore PersistedTabSession.color + 호환 fallback (옛 파일 → null)
- 단위 테스트 7 (setColor 5 + round-trip 1 + 호환 1)
- evaluator: **Pass 13 / Partial 0 / Fail 0**

### M3 (PR #64) — 탭 핀(고정)
- TabSession.pinned 필드 + TabManager.setPinned (핀↔비핀 invariant 유지)
- closeOthers/closeRight 핀 탭 자동 제외 (closed에서 빠짐)
- reorder 핀 경계 넘기는 이동 clamp
- restore 좌측 정렬 stable sort + 호환 fallback (옛 파일 → false)
- tab:set-pinned IPC + 컨텍스트 메뉴 "핀 고정 / 핀 해제" 토글
- TabBar 핀 시각화 (📌 + 좁은 셀 + 닫기 X 숨김)
- 단위 테스트 9 (setPinned 5 + closeOthers/closeRight 핀 제외 2 + 영속 2)
- evaluator: **Pass 12 / Partial 0 / Fail 0**

### M4 (현재 브랜치) — PRD v0.3.9 + Sprint 종합
- PRD 4개 항목 패치 (00 change history §0.12 / README v0.3.9 / 06 data_model §12.1 cancelOnTabSwitch 갱신 + TabSession 신규 / 11 §19.1 모듈 등록)
- .flowset 동기화 (requirements / state / handoff)
- 자동 검증 lint 0/0 / typecheck PASS / test 273/273 / build PASS

### Sprint 011 누적
- main commits / PR: 5개 (#61 정의 + #62 M1 + #63 M2 + #64 M3 + 본 PR M4)
- 단위 테스트 254 → 273 (+19, AC-4 ≥ 270 충족 +3)
- evaluator 호출 3회 (M1 Pass 9 / M2 Pass 13 / M3 Pass 12) — 모두 Pass ≥ 8 충족, 본 종합 진행 중
- 가드레일 G-001 ~ G-011 위반 0
- commit-check CI 5 PR 모두 success (NNN 한 분절 5 Sprint 연속)

### Sprint 012 후보

evaluator 권고 + Sprint 011 잔여:

**P0 (외부 의존성)**
- Codex Login Provider Spike (Phase 1 PoC 4종)
- 사용자 수동 QA 결과 반영
- Phase 2 진입 전제 (Spike 2/3/4 정량화)

**P1 (코드 자동 진행 가능)**
- 탭 미리보기 (hover thumbnail) — WebContentsView.capturePage() spike + 구현
- 탭 그룹 (단독 Sprint, 데이터 모델 + UI 영향 큼)

**P2**
- evaluator 보고서 후속 추적 섹션 (.claude/agents/evaluator.md 변경, 사용자 합의)
- UserSetting Phase 2~4 필드 (subtitleMode/ttsEnabled/syncMode) — Phase 2 진입 시
- 핀 영역 + 비핀 영역 reorder 인터페이스 명시 (현재 clamp만, drag 시 사용자에게 시각 피드백 추가 가능)

### Sprint 011 후속 조치
- 본 M4 commit + PR + main 머지
- 사용자 협의 후 Sprint 012 정의

---

## Sprint 012 결과 (2026-05-15)

사용자 "우선순위 산정해서 진행해" 지시 → capture API 우려는 활성 탭 캡처 패턴으로 우회.

### 우선순위 산정

| 후보 | 자동 진행 | 영향도 | 결정 |
|---|---|---|---|
| 탭 미리보기 (hover thumbnail) | ⚠️→✅ (활성 탭 캡처 패턴) | 중 | **포함 M1+M2** |
| 키보드 단축키 (Ctrl+T/W/Tab) | ✅ | 작음 | **포함 M3** |
| 탭 그룹 | ⚠️ | 큼 (단독 Sprint 권고) | Sprint 013+ |
| Codex / QA / Phase 2 | ❌ | 외부 의존성 | Sprint 013+ |

### M1 (PR #67) — ThumbnailStore + capture hook
- ThumbnailStore 메모리 LRU 50 항목 (true LRU set/get 모두 last-touched)
- main 활성 탭 변경 직전 captureTabThumbnail(prevTabId) fire-and-forget
- tab:get-thumbnail IPC + preload tabApi.getThumbnail
- destroyTabView → remove / closed → clear
- TabManager.cycleActiveTabId(direction) 순수 함수 (M3 사전 작업)
- 단위 테스트 14 (ThumbnailStore 8 + cycleActiveTabId 6)
- **초기 evaluator: Partial 10/1/1** — A3 capture hook dead code (setActiveTabView 진입 시점에 tabManager.getActiveId() === tabId라 호출 안 됨)
- **핫픽스 (WI-S012M1-1-fix)**: captureActiveTabThumbnail → captureTabThumbnail(prevTabId) 인자화 + setActiveTabView 진입 시점 browserView 변수 활용 + tabViews 역조회로 정확한 prevId
- **재평가: Pass 12 / Partial 0 / Fail 0**

### M2 (PR #68) — TabBar hover 미리보기 UI
- onMouseEnter 600ms setTimeout → tab:get-thumbnail 호출
- onMouseLeave 즉시 clearTimeout + null + unmount cleanup
- 미리보기 div 절대 위치 (anchorLeft = boundingClientRect.left) + fade-in 150ms
- dataUrl 있으면 img + URL/title meta, 없으면 placeholder
- 드래그 중(draggingRef) hover 시작 무시 + 콜백 이중 가드
- evaluator: **Pass 9 / Partial 0 / Fail 0**

### M3 (PR #69) — 키보드 단축키
- installApplicationMenu(): 탭 서브메뉴 4 항목
  - Ctrl+T 새 탭 / Ctrl+W 닫기 / Ctrl+Tab 다음 / Ctrl+Shift+Tab 이전
- cycleActiveTabId 단위 테스트는 M1에서 6 케이스 포함됨
- mainWindow focus 시점부터 작동
- evaluator: **Pass 13 / Partial 0 / Fail 0**

### M4 (현재 브랜치) — PRD v0.3.10 + Sprint 종합
- PRD 4개 항목 패치 (00 change history §0.13 / README v0.3.10 / 11 §19.1 모듈 등록 — TabBar hover, ThumbnailStore, Application Menu, TabManager.cycleActiveTabId)
- .flowset 동기화 (requirements / state / handoff)
- 자동 검증 lint 0/0 / typecheck PASS / test 287/287 / build PASS

### Sprint 012 누적
- main commits / PR: 5개 (#66 정의 + #67 M1 + #68 M2 + #69 M3 + 본 PR M4)
- 단위 테스트 273 → 287 (+14, AC-4 ≥ 285 충족 +2)
- evaluator 호출 4회 (M1 Pass 12 재평가 / M2 Pass 9 / M3 Pass 13 / 종합 진행 중) — 모두 Pass ≥ 8 충족
- 가드레일 G-001 ~ G-011 위반 0
- commit-check CI 5 PR 모두 success (NNN 한 분절 6 Sprint 연속)
- M1 dead code → 핫픽스 직접 해소 (evaluator 권고 환원 사이클 세 번째 사례)

### Sprint 013 후보

evaluator 권고 + Sprint 012 잔여:

**P0 (외부 의존성)**
- Codex Login Provider Spike (Phase 1 PoC 4종)
- 사용자 수동 QA 결과 반영
- Phase 2 진입 (Spike 2/3/4 정량화)

**P1 (코드 자동 진행)**
- 탭 그룹 (단독 Sprint, 데이터 모델/UI/영속/close 범위 영향 큼)
- 닫은 탭 복원 (Ctrl+Shift+T) — closed history 모델
- ThumbnailStore 디스크 영속 (메모리 안정성)
- 우측 끝 탭 미리보기 viewport 보정 (Sprint 012 M2 권고)

**P2**
- evaluator 보고서 후속 추적 섹션
- UserSetting Phase 2~4 필드 (Phase 2 진입 시)

### Sprint 012 후속 조치
- 본 M4 commit + PR + main 머지
- 사용자 협의 후 Sprint 013 정의

