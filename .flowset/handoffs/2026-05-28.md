# 2026-05-28 — Sprint 018 M2 T17b (로컬 임베딩 wiring)

## 메타

- **Sprint**: 018 M2 T17b. 진입 발화: "핸드오프 읽고 작업 진행해" (G-022 ALLOW_ENTRY) → AskUserQuestion 으로 M2 작업 선택 = **T17b 로컬 임베딩 wiring** (carryover M2 작업 T17b/T20/T21 중 사용자 명시 선택).
- 누적 main first-parent S018 PR: **11** (#246~#256) → 본 docs PR 머지 시 12 (`git log --first-parent main --grep=WI-S018` 실측 — 직전 state 의 "8/9" 표기는 M2 docs PR 머지분 누락 누적 오류, codex 019e6d06 + evaluator 발견 정정).
- 단위 회귀: 1853 → **1878** (+25; `npm test` 전체 **102 파일** = CI 정합).
- KI 변동 0 (잔여 9 유지).
- 자기 검증 루프: 58 → **59** SUCCESS (정의: dual review 통과 PR 누적 — T17a 58 → T17b 59).
- carryover 5 유지 (CO-1 T17 = T17a+T17b 머지 / T17c~e 잔여).

## 1. 설계 사전 협의 (codex 019e6898 — 후보 A)

T17b 는 폭이 큰 변경(VectorIndex 시그니처 + 5 소비처 + 13 테스트 v06 전환 + Database 접근자)이라 T17a(019e6574) 선례대로 codex 설계 사전 협의:
- **dim 해소 위치 = caller(write/query path)** (VectorIndex self-lookup 금지 — codex 019e50ec NOTABLE #3 정합).
- **test-only v06 helper** (`applyV06Schema` 를 프로덕션 `FlowbrowserDatabase` API 에 추가 금지 — applySchema 는 v05 유지가 migrateV04ToV05 정합상 강제). drift 는 v05_to_v06.test.ts drift-check 책임.
- **createWorkspace DEFAULT 의존 유지** (사용자 모델 선택 INSERT 는 T17d 개방).

## 2. 산출물 (PR #256 — +1192 / -305 / 24 파일)

| 파일 | 변경 | 내용 |
|---|---|---|
| `src/storage/embeddingModel.ts` | 신규 (+89) | 임베딩 모델 레지스트리 SSOT — parse/resolve, v06.sql CHECK ↔ vec0 테이블 ↔ UX 3자 일치 |
| `src/storage/VectorIndex.ts` | +220/-122 | `_1024`/`_768` dimension 분기 + selectVec*Table closed allowlist + cross-dim cleanup |
| `src/storage/Database.ts` | +38/-12 | WorkspaceRow.embedding_model + 접근자 SELECT * + DEFAULT 방어 (v05/v06 양립) |
| `src/main/services.ts` | +20/-2 | migrateV05ToV06 체인 + getWorkspaceEmbeddingModel wiring |
| `src/main/searchHandlers.ts` | +38/-6 | query path — embedding_model 을 임베딩 _전_ 해소 → modelHint/dimensions |
| `src/ai/embedding/EmbeddingClient.ts` | +18/-5 | ProcessJobDeps.resolveEmbeddingClient 팩토리 (임베딩 전 dim 해소) |
| `src/main/SearchService.ts` | +14/-2 | dimensions passthrough |
| `src/storage/index.ts` | +19/-1 | embeddingModel/allowlist export |
| `tests/helpers/v06Schema.ts` | 신규 (+35) | test-only v06 schema 적용 helper |
| `tests/unit/storage/migrations/v06_bootstrap_chain.test.ts` | 신규 (+184) | 부트 체인 e2e (fresh + v05 데이터 보존 + already_migrated) |
| `tests/unit/storage/VectorIndex.test.ts` | +172/-54 | 768 분기 + allowlist + cross-dim 전환 회귀 |
| `tests/unit/main/searchHandlers.test.ts` | +95/-10 | 768 query 경로 + 미지원 모델 error |
| `tests/unit/ai/EmbeddingClient.test.ts` | +77/-8 | 768 upsert positive + dimension 가드 |
| `tests/unit/storage/embeddingModel.test.ts` | 신규 (+76) | SSOT 단위 |
| 그 외 테스트 v06 전환 | — | SearchService/IndexingService/scenarios×5/perf×2 |

### 핵심 결정
- **dimension 분기 = 임베딩 _전_ 해소** (codex BLOCKING 정정) — query embedding 이 워크스페이스 모델/차원으로 생성되어야 768 테이블과 항상 일치. dim 해소를 임베딩 _후_에 하면 분기가 cosmetic (1024 생성 후 768 테이블 검색 → 항상 실패).
- **upsert/search = dim 파라미터** (caller 해소) / **has/delete/count = dimension-agnostic** (양 테이블 — IndexingService 무변경, 한 페이지는 워크스페이스 단일 dim 테이블에만 존재).
- **upsert cross-dim cleanup** — insert 전 모든 dim 테이블 DELETE (재인덱스 오염 차단).
- provider 어댑터 매핑(openai vs ollama) = **T17c 위임** — T17b 는 modelHint/dimensions threading + 768 테이블 인프라까지.

## 3. dual review (G-021 — 실 호출 증거)

- **evaluator** (`.claude/agents/evaluator.md`): round-1 `a03d9659c14bcf511` **Pass 10/0/0** → round-2 재채점 `a2de316d61c1c47f3` **Pass 9/Partial 1/Fail 0**. Partial 1 = 항목 4 cross-dim 전환 직접 회귀 부재 → **최종 HEAD 에 cross-dim 전환 회귀 추가로 해소** (`VectorIndex.test.ts` "cross-dim 전환: 1024 upsert → 768 재upsert → 1024 잔존 0").
- **codex** (raw MCP `sandbox=read-only`, threadId `019e6898-38eb-7432-ba19-d68cdef05690`): 설계 사전 협의 → 1차 리뷰 **BLOCKING 1 / NEEDS_CHANGES 2 / NOTABLE 4** → 정정 후 재리뷰 **BLOCKING 0 / NEEDS_CHANGES 0 / NOTABLE 4(해소 확인)**.
  - BLOCKING(해소): dim 해소가 임베딩 _후_라 768 경로 cosmetic → query/upsert 모두 resolve-then-embed 로 정정 (modelHint/dimensions threading).
  - NEEDS_CHANGES(해소): (1) VectorIndex upsert cross-dim cleanup (2) searchHandlers 768 query 회귀.
- 본 PR 내 hotfix 흡수: 없음 (round-2 정정은 단일 커밋 amend).

## 4. 검증

- lint PASS / typecheck PASS / `npm test` 전체 **1878** PASS (**102 파일**) / build PASS / CI **6/6 SUCCESS** (typecheck+test+build / 커밋 메시지 / FlowSet Policy Check / macOS·macOS Intel·Windows sqlite-vec PoC).
- FlowSet Policy Check 1차 실패(산출물 매트릭스 파일 그룹화 + dual review 카운트/full thread UUID 형식 미달) → verify-pr-body.mjs 규칙 정합(24 파일 개별 행 + 체크박스 행 카운트 + full UUID) 후 통과.

## 5. 다음 진입점 (본 state 동기화 docs PR 머지 후 — Sprint 018 carryover 5, M2 진행 중)

1. **T17c OllamaProvider.embed() 재구현 + provider 선택 wiring** (S018-T05, M2) — `/api/embed` + 768-dim 매핑 + supportsEmbed=true. **+ provider 선택 wiring**: 현재 searchHandlers 는 no-arg `getEmbeddingProvider()`, services.ts 는 `providers.get('openai')` 고정 / processNextEmbeddingJob 은 `resolveEmbeddingClient(workspaceId)` 팩토리만 있고 백그라운드 worker 호출자 부재 (codex 019e6d06). T17b 가 modelHint/dimensions threading + 768 vec0 테이블 인프라 완비 → T17c 에서 embedding_model.provider 별 어댑터 선택 + resolveEmbeddingClient 호출자를 붙이면 query path/processNextEmbeddingJob helper 가 Ollama-capable. **실 indexing end-to-end 활성은 worker caller wiring + T17e 검증까지 필요** (단순 OllamaProvider.embed() 만으로 부족). 이후 T17d(UX 모달 + CreateWorkspaceInput.embedding_model 개방) / T17e(E2E).
2. **T20 / T21** (M2) — R3-B 실 학습 / 워크스페이스 공유 설계.
3. **PRD v0.5.0 + Phase 3 종료 + Sprint 019** (M4~M5).
4. **후속 정정 항목**:
   - (a) `processNextEmbeddingJob` 백그라운드 worker 호출자 wiring — 현재 분기 능력만 박힘, 실 인덱싱 worker 호출자 미구현 (T17b scope 내였으나 worker 자체가 별도 미구현).
   - (b) `WorkspaceExportImportService` export schema v05 고정 + embedding_model 미export — v06 데이터 계약 정합 위해 T17d 이후 export schema bump (codex NOTABLE).

⚠️ M2 다음 작업(T17c vs T20 vs T21) 은 **사용자 명시 선택** 후 진입 (G-022). 본 handoff §5 박힘 ≠ 본 세션 진입 박음 권고.

## 6. carryover 잔여 (Sprint 018)

| # | 항목 | 상태 |
|---|---|---|
| CO-1 | T17 로컬 임베딩 통합 | **부분 — T17a(#254)+T17b(#256)+T17c(#258)+T17d(#260) 머지 / T17e + write-path wiring 잔여** (S018-T05, M2) |
| CO-3 | T20 R3-B 실 학습 | 잔여 (S018-T06, M2, 학습 데이터셋) |
| CO-4 | T21 워크스페이스 공유 설계 | 잔여 (S018-T07, M2) |
| CO-5 | T22~T26 종합 + Sprint 019 시안 | 잔여 (S018-T08~T13) |
| CO-8 | PRD v0.5.0 발행 | 잔여 (S018-T10, M4) |

작업 단위 carryover **5 유지** (CO-1 은 T17a~T17d 머지 / T17e + write-path wiring 잔여 — 본 파일 T17d 이어쓰기 2 §5 참조).

본 세션 누적: PR #256 (T17b feat) 머지 + 본 docs PR. codex thread 1개 (019e6898 설계 협의 후보 A + 2차 리뷰 BLOCKING 정정 → 재리뷰 0/0/4).

---

# 2026-05-28 (이어쓰기) — Sprint 018 M2 T17c (로컬 임베딩 provider wiring)

## 메타

- **Sprint**: 018 M2 T17c. 진입 발화: "핸드오프 읽고 작업 진행해" (G-022 ALLOW_ENTRY) → AskUserQuestion 으로 M2 작업 선택 = **T17c 로컬 임베딩 provider wiring** (carryover T17c/T20/T21 중 사용자 명시 선택).
- 누적 main first-parent S018 PR: **13** (#246~#258) → 본 docs PR 머지 시 14 (`git log --first-parent main --grep=WI-S018` 실측).
- 단위 회귀: 1878 → **1894** (+16; `npm test` 전체 **102 파일** = CI 정합).
- KI 변동 0 (잔여 9 유지).
- 자기 검증 루프: 59 → **60** SUCCESS (정의: dual review 통과 PR 누적 — T17b 59 → T17c 60).
- carryover 5 유지 (CO-1 T17 = T17a+T17b+T17c 머지 / T17d~e 잔여).

## 1. 설계 사전 협의 (codex 019e6e00 — 후보 A)

T17c 는 provider 인터페이스 시그니처 변경 + OllamaProvider.embed 재구현이라 T17a/T17b 선례대로 codex 설계 사전 협의 (+ Ollama `/api/embed` 계약은 WebFetch 로 사실 확인 — G-006):
- **scope 후보 A**: query path provider 선택 + namespace→credential 매핑 helper + OllamaProvider.embed. **write-path `resolveEmbeddingClient` 팩토리/worker 는 prod caller 부재 → defer** (dead code 회피).
- **매핑 위치 = embeddingModel.ts SSOT** (`import type CredentialProviderType`, 레이어링 무관). storage/index.ts export.
- **getEmbeddingProvider(credentialType) 시그니처** + handler 가 modelSpec 을 provider 선택 _전_ 해소 (reorder). provider-aware 에러 메시지.
- **embed dimensions 무시** (nomic 768 고정) + length 검증은 caller(EmbeddingClient.toFloat32) 위임.

## 2. 산출물 (PR #258 — +428 / -70 / 10 파일)

| 파일 | 변경 | 내용 |
|---|---|---|
| `src/ai/providers/OllamaProvider.ts` | +117/-27 | embed() 실 구현(POST /api/embed batch + nomic-embed-text 768 + error 매핑 chat 패턴) + supportsEmbed=true + 주석 갱신 |
| `src/storage/embeddingModel.ts` | +32 | embeddingProviderToCredentialProvider() — namespace→credential `as const satisfies Record` 4자 일치 |
| `src/main/searchHandlers.ts` | +37/-23 | getEmbeddingProvider(credentialType) + modelSpec 선해소 reorder + provider-aware 에러 + 능력 가드 |
| `src/main/services.ts` | +5/-3 | providers.get(providerType) wiring (이전 'openai' 고정 제거) |
| `src/main/SearchService.ts` | +2/-2 | dimension-aware 주석 |
| `src/ai/embedding/EmbeddingClient.ts` | +2/-1 | unsupported 메시지 provider-agnostic |
| `src/storage/index.ts` | +4/-2 | embeddingProviderToCredentialProvider / EmbeddingCredentialProvider export |
| `tests/unit/ai/OllamaProvider.test.ts` | +146/-10 | embed 회귀 11종 + supportsEmbed=true |
| `tests/unit/main/searchHandlers.test.ts` | +64/-1 | provider 선택(local/openai) + null 안내 + 능력 가드 회귀 |
| `tests/unit/storage/embeddingModel.test.ts` | +19/-1 | 매핑 helper 회귀 3종 |

### 핵심 결정
- **dimension/modelHint threading 임베딩 _전_ 해소** (T17b codex 019e6898 BLOCKING 선례 정합) — query embedding 이 워크스페이스 차원(768)으로 생성 + 같은 dim vec0 테이블 선택. modelSpec 을 provider 선택보다 앞에 두는 reorder 가 핵심.
- **scope 비대칭 (의도, G-013)** — query path 만 provider-aware. write-path(`processNextEmbeddingJob`)는 prod caller 부재 → 손대면 dead code. **T17e E2E 전에 worker caller + resolveEmbeddingClient provider 선택 후속 PR 필요** (그 전까지 ollama 워크스페이스는 검색만 768 가능, 실 인덱싱 적재는 prod worker 호출자 미연결이라 어느 provider 로도 안 됨 — codex 019e6e2a NC).
- **dimensions 무시** — nomic-embed-text 768 고정(OpenAI 처럼 축소 파라미터 없음). 실제 길이 강제는 EmbeddingClient.toFloat32 가 워크스페이스 dim 대조 (silent corruption 차단).

## 3. dual review (G-021 — 실 호출 증거)

- **evaluator** (`.claude/agents/evaluator.md`, agentId `aeee52271ee56b98b`): **Pass 7 / Partial 0 / Fail 0** (AC1~AC7 전부 Pass — embed/provider 선택/dim threading/provider-aware 에러/silent fallback 금지/회귀/주석 정합).
- **codex** (raw MCP `sandbox=read-only`, threadId `019e6e00-70c1-7c82-bb0d-927c8580ebf8`): 설계 협의(후보 A) → 산출물 리뷰 **BLOCKING 0 / NEEDS_CHANGES 0 / NOTABLE 5** (Q1~Q4 설계 확인 + 능력 가드 hardening 1건).
- **본 PR 내 hotfix 흡수**: `9b0d952` — codex NOTABLE(`!provider.info.supportsEmbed` 미확인) → `!provider.info.supportsEmbed || !provider.embed` 가드 + 계약 회귀 테스트.

## 4. 검증

- lint PASS / typecheck PASS / `npm test` 전체 **1894** PASS (**102 파일**) / build PASS / CI **6/6 SUCCESS** (typecheck+test+build / 커밋 메시지 / FlowSet Policy Check / macOS·macOS Intel·Windows sqlite-vec PoC).
- FlowSet Policy Check 1차부터 통과 — verify-pr-body.mjs 로컬 사전 검증(매트릭스 + dual review 카운트/full UUID 체크박스 인라인) 후 PR 생성.

## 5. 다음 진입점 (본 docs PR 머지 후 — Sprint 018 carryover 5, M2 진행 중)

1. **T17d UX 모델 선택 + T17e E2E** (S018-T05 잔여, M2). T17e 전 write-path provider 선택 비대칭 해소 필요 (worker caller + resolveEmbeddingClient).
2. **T20 / T21** (M2) — R3-B 실 학습 / 워크스페이스 공유 설계.
3. **PRD v0.5.0 + Phase 3 종료 + Sprint 019** (M4~M5).
4. **후속 정정 항목**: (a) `processNextEmbeddingJob` worker 호출자 + provider 선택 wiring (b) `WorkspaceExportImportService` export schema v05 고정 + embedding_model 미export — T17d 이후 bump.

⚠️ M2 다음 작업(T17d/e vs T20 vs T21) 은 **사용자 명시 선택** 후 진입 (G-022). 본 handoff §5 박힘 ≠ 본 세션 진입 박음 권고.

본 세션 누적(T17c): PR #258 (feat) 머지 + 본 docs PR. codex thread 1개 (019e6e00 설계 협의 후보 A + 산출물 리뷰 0/0/5).

---

# 2026-05-28 (이어쓰기 2) — Sprint 018 M2 T17d (워크스페이스 임베딩 모델 선택 UX)

## 메타

- **Sprint**: 018 M2 T17d. 진입: T17c 머지 후 사용자 "코덱스랑 협의해서 우선순위를 부여해서 순차진행해" (G-022 ALLOW_ENTRY + codex 위임) → **codex 019e6e4d 우선순위 협의**(전체 순차 [T17d → write-path → T17e → T21 → T22/T09 → PRD]) → 본 PR = 그 중 첫 단계 **T17d**.
- 누적 main first-parent S018 PR: **15** (#246~#260) → 본 docs PR 머지 시 16.
- 단위 회귀: 1894 → **1912** (+18; 102 파일 = CI 정합).
- KI 변동 0 (잔여 9 유지). 자기 검증 루프 60 → **61**.
- carryover 5 유지 (CO-1 T17 = T17a~T17d 머지 / T17e + write-path wiring 잔여).

## 1. codex 우선순위 협의 (019e6e4d)

T17c 머지 후 잔여 carryover 전체 순차 우선순위 (codex 권고):
1. **T17d** ← 본 PR (UX 메타 단계 — write-path 와 독립이라 먼저).
2. **write-path wiring** (processNextEmbeddingJob prod 호출자 + resolveEmbeddingClient provider 선택) — T17e 전제.
3. **T17e** (E2E OpenAI↔Ollama 격리).
4. **T21** (워크스페이스 공유 설계 시안 — 독립).
5. **T22/T09** → **PRD v0.5.0 / Phase 3 종료 / Sprint 019**.
- **Sprint 019 defer**: T20 (학습 데이터셋 부재, contract §155). Q4 = T17 라인 먼저 닫기(end-to-end 가치).
- **본 PR(T17d) 완료 후 잔여 = 2번 write-path wiring 부터** (state/커밋 SSOT "다음 = write-path wiring" 과 정합).

## 2. 산출물 (PR #260 — +453 / -44 / 13 파일)

| 파일 | 변경 | 내용 |
|---|---|---|
| `src/storage/Database.ts` | +40/-11 | CreateWorkspaceInput.embedding_model + 조건부 INSERT(명시 6-컬럼 v06 / 미주입 5-컬럼 DEFAULT) + 검증 |
| `src/main/WorkspaceService.ts` | +28/-2 | validateEmbeddingModel(''/null→DEFAULT, 미지원→invalid_embedding_model) + create 전달 |
| `src/main/workspaceHandlers.ts` | +8/-2 | WorkspaceCreateArgs.embeddingModel + SerializedWorkspace.embeddingModel 직렬화 |
| `src/main/WorkspaceExportImportService.ts` | +63/-19 | embedding_model export/import schema-aware 보존 + schemaVersion v06 + highlights 게이트 `!== 'v04'` 정정 |
| `src/preload/index.ts` | +4 | SerializedWorkspacePayload.embeddingModel + create args |
| `src/renderer/.../presets.ts` | +25 | EMBEDDING_MODEL_OPTIONS + DEFAULT (main SSOT 분리, 테스트 drift 차단) |
| `src/renderer/.../WorkspaceSidebar.tsx` | +26/-2 | 모델 선택 select + state + create 전달 |
| `src/renderer/src/styles.css` | +4/-2 | .workspace-modal__embedding-model 스타일 |
| 테스트 5 파일 | +255/-6 | createWorkspace(v06)/Service 검증/handler 직렬화/export-import round-trip/presets SSOT |

### 핵심 결정
- **조건부 INSERT (v05/v06 양립)** — embedding_model 명시 시만 컬럼 포함. 미주입은 기존 DEFAULT path (ensureDefaultWorkspace 무회귀). export/import 도 동일 — `PRAGMA table_info` schema-aware.
- **renderer SSOT 분리** — EMBEDDING_MODEL_OPTIONS 를 renderer 에 두되(런타임 storage import 회피, WORKSPACE_ICON_PRESETS 정책) 단위 테스트가 main EMBEDDING_MODELS 와 id/default 일치 강제.
- **export schema v05 → v06** — embedding_model round-trip 보존 (codex NC). schemaVersion bump 이 노출한 v05-한정 highlights 게이트 회귀를 round-trip 테스트로 검출 → `!== 'v04'` 정정.

## 3. dual review (G-021)

- **evaluator** (`a9c3d48adc7a61e7d`): **Pass 6 / Partial 0 / Fail 0** (AC1~AC6 — v05/v06 양립 / 미지원 throw / renderer SSOT / 전 레이어 threading / G-013 scope / 회귀).
- **codex** (raw MCP read-only, threadId `019e6e62-8753-78e1-b07d-38c71dfbafc0`): round-1 **BLOCKING 0 / NEEDS_CHANGES 2 / NOTABLE 5** → 정정 → round-2 **0 / 0 / 5**.
  - NC(해소): (1) export/import round-trip 손실 → embedding_model 보존 + schemaVersion v06 + highlights 게이트 정정 (2) 신규 select CSS 누락.
- **본 PR 내 hotfix 흡수**: `a6e9704` (NC-1 + NC-2).

## 4. 검증

- lint PASS / typecheck PASS / `npm test` 전체 **1912** PASS (**102 파일**) / build PASS / CI **6/6 SUCCESS**.

## 5. 다음 진입점 (codex 019e6e4d 순차 — 본 docs PR 머지 후)

1. **write-path wiring** — `processNextEmbeddingJob` prod worker 호출자 연결 + `resolveEmbeddingClient` provider 선택 (T17e 전제).
2. **T17e** — E2E OpenAI↔Ollama 격리 검증.
3. **T21** — 워크스페이스 공유 설계 시안. (T20 = Sprint 019 defer)
4. **T22/T09 → PRD v0.5.0 / Phase 3 종료 / Sprint 019**.

본 세션 누적(T17d): PR #260 (feat) 머지 + 본 docs PR. codex thread 2개 (019e6e4d 우선순위 협의 + 019e6e62 산출물 리뷰 2라운드 → 0/0/5).

---

# 2026-05-28 (이어쓰기 3) — Sprint 018 M2 write-path wiring (임베딩 worker drainer + provider-aware resolveEmbeddingClient)

## 메타

- **Sprint**: 018 M2 write-path wiring (S018-T05 CO-1 잔여, T17e 전제). 진입: 신규 세션 발화 "핸드오프 읽고 작업 진행해"(G-022 ALLOW_ENTRY) + 직전 세션 위임 "코덱스랑 협의해서 순차진행해" → codex 019e6e4d 순차 우선순위(write-path 가 T17d 다음) 정합 진입.
- 누적 main first-parent S018 PR: **17** (#246~#262) → 본 docs PR 머지 시 18.
- 단위 회귀: 1912 → **1942** (+30; 103 파일 = CI 정합 — 신규 EmbeddingWorker.test.ts).
- KI 변동 0 (잔여 9 유지). 자기 검증 루프 61 → **62**.
- carryover 5 유지 (CO-1 T17 = T17a~T17d + write-path wiring 머지 / **T17e 만 잔여**).

## 1. 배경 — write path 가 prod 호출자 부재였음

T17b 가 `processNextEmbeddingJob` 에 `resolveEmbeddingClient` 팩토리(임베딩 _전_ dim 해소)를 박았으나 **src 어디서도 호출 안 됨**(테스트만). enqueue 는 IndexingService/NoteService/migration 이 하는데 drain 하는 worker 가 없어 벡터가 영영 안 쌓였다. T17c 가 query path provider 선택을 박았으나 write path 는 의도적 defer(prod caller 부재 = dead code 회피, codex 019e6e2a NC). 본 PR 이 그 호출자(worker)+ prod resolveEmbeddingClient 를 박는다.

## 2. codex 설계 사전 협의 (019e6ea0 — 2라운드)

T17a/b/c 선례대로 worker lifecycle + provider 미가용 처리는 codex 사전 협의:
- **Q3 (최대 결함 지적)**: provider/API key 미가용을 `throw→markFailed` 하면 backlog 전부 영구 failed + 환경 복구 후 자동 회복 불가. **"현재 처리 불가(환경)" 와 "영구 실패(데이터/계약)" 분리 필수.**
- **C1 (실 코드 검증, G-006)**: `ProviderError.code` 확인 — `auth_invalid`(키 무효)/`network`(Ollama 미실행)/`rate_limit` 만 unavailable, `bad_request`(Ollama 모델 미설치 포함)/`server_error`/`unsupported` 는 failed 유지. (OpenAI/Ollama embed 의 실제 throw code 대조 후 확정.)
- **C2**: `release(id)` 는 `WHERE id=? AND status='in_progress'` 가드 + `boolean` 반환, `requeueInProgress` 는 `updated_at` 갱신. status union 만 확장(queue status 아님).
- **C3**: starvation 한계(claimNext provider 미인지) MVP 수용 + 문서화. backoff 30s 적절.
- **Q1/Q4/Q5**: polling MVP(wake() 후속) / worker = `src/ai/embedding/EmbeddingWorker.ts` pure + services thin wiring / hardcode(poll 3s, batch 10).
- **Q2**: boot orphan(in_progress stuck) 회복은 본 PR 필수 — `EmbeddingQueue.requeueInProgress`.

## 3. 산출물 (PR #262 — +885 / -6 / 9 파일)

| 파일 | 변경 | 내용 |
|---|---|---|
| `src/storage/EmbeddingQueue.ts` | +36 | release(in_progress→pending, attempts 불변, status 가드) + requeueInProgress(boot orphan 일괄 회복) |
| `src/ai/embedding/EmbeddingClient.ts` | +106/-3 | EmbeddingProviderUnavailableError + embedTexts ProviderError 분류 래핑 + buildEmbeddingClientForModel 순수 helper + processNextEmbeddingJob provider_unavailable 분기 + status union |
| `src/ai/embedding/EmbeddingWorker.ts` | +159 | 신규 — drainer(start/stop/isRunning, 재진입 가드, idle→poll/unavailable→backoff/maxBatch→0ms, 주입형 타이머) |
| `src/ai/index.ts` | +7/-2 | worker/helper/error export |
| `src/main/services.ts` | +74 | start/stopEmbeddingWorker + resolveEmbeddingClient deps(buildEmbeddingClientForModel + providers.get) + bootstrap catch 정지 + one-shot 계약 주석 |
| `src/main/index.ts` | +11 | rebuildAllProviders 후 startEmbeddingWorker + before-quit 정지 |
| 테스트 3 파일 | +492/-1 | release/requeueInProgress(EmbeddingQueue +88) + provider 분류/provider_unavailable/buildEmbeddingClientForModel(EmbeddingClient +211/-1) + worker drain·lifecycle 11종(EmbeddingWorker +193 신규) |

### 핵심 결정
- **provider 미가용 ≠ 영구 실패** (codex Q3): 환경 문제(키/Ollama/rate limit)는 `EmbeddingProviderUnavailableError` → `release`(→pending) + worker backoff(30s) 자동 회복. 데이터/계약 오류(미지원 모델/orphan/dim mismatch/bad_request)만 markFailed.
- **분류 SSOT** = `PROVIDER_UNAVAILABLE_CODES={auth_invalid,network,rate_limit}` (embedTexts catch 에서 ProviderError 래핑, message 보존 → query path 거동 불변).
- **one-shot lifecycle** (codex NC 해소): initServices/startEmbeddingWorker 는 app.whenReady 1회 호출이 유일 경로(src 전체 grep 검증) → module-state 캡처 stale 불가. 향후 재초기화 도입 시 restart 경계 필요를 주석 명시.
- **dim threading 정합** (codex 019e6898 선례): buildEmbeddingClientForModel 이 임베딩 _전_ 워크스페이스 차원으로 client 구성 → query path 와 동일.

## 4. dual review (G-021 — 실 호출 증거)

- **evaluator** (`.claude/agents/evaluator.md`, agentId `a0b53b81ac428e1d9`): **Pass 6 / Partial 0 / Fail 0** (AC1~AC6 — resolveEmbeddingClient 체인 / worker lifecycle / provider 미가용 회복 / boot orphan 회복 / dim threading / 회귀).
- **codex** (raw MCP sandbox=read-only, threadId `019e6ea0-d9d8-7aa0-a84e-b88b8fc71509` — 설계 협의 + 산출물 리뷰 동일 thread): round-1 **BLOCKING 0 / NEEDS_CHANGES 1 / NOTABLE 4** → 정정 → round-2 **0 / 0 / 1**.
  - NC(해소): 재초기화 stale-safety → one-shot 호출 계약 문서화(주석 변경, 거동 불변). codex round-2 "충분, 실제 restart 경계는 지금 넣지 않는 게 맞다(미사용 복잡도)" 확인.
- hotfix 흡수: 없음 (NC 해소 = 주석, 동일 커밋 반영).

## 5. 검증

- lint PASS / typecheck(node+web) PASS / `npm test` 전체 **1942** PASS (**103 파일**) / build PASS / CI **6/6 SUCCESS** (typecheck+test+build 5m55s / 커밋 메시지 / FlowSet Policy Check / macOS·macOS Intel 11m17s·Windows sqlite-vec PoC). verify-pr-body 로컬 사전 검증 후 PR.

## 6. 다음 진입점 (codex 019e6e4d 순차 — 본 docs PR 머지 후)

1. **T17e** — E2E OpenAI↔Ollama 격리 검증 (인덱싱→검색, 1024/768 격리). write-path wiring 머지로 전제 충족.
2. **T21** — 워크스페이스 공유 설계 시안. (T20 = Sprint 019 defer)
3. **T22/T09 → PRD v0.5.0 / Phase 3 종료 / Sprint 019**.
- **MVP 의도 한계 (코드 주석 명시, 후속 추적)**: (a) provider 미가용 고우선 잡 starvation (b) in-flight embed 비취소 (c) query path 인라인 체인 중복(G-013 의도적 비대칭).

⚠️ 다음 작업(T17e vs T21) 은 **사용자 명시 선택** 후 진입 (G-022). 본 §6 박힘 ≠ 본 세션 진입 박음 권고.

본 세션 누적(write-path): PR #262 (feat) 머지 + 본 docs PR. codex thread 1개 (019e6ea0 설계 협의 + 산출물 리뷰 2라운드 → 0/0/1).
