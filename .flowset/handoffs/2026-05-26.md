# 2026-05-26 — Sprint 018 M0 T02 (mini-milestone β PR C — G-022 advisory → PreToolUse blocking 전환)

## 메타

- **Sprint**: 018 M0 T02 (mini-milestone β PR C). 사용자 결정 불요 — codex 019e5161 §1 권고 정합 (T02 = G-022 blocking 전환).
- 진입 발화: "핸드오프 읽고 작업 진행해" (2026-05-26) — G-022 §허용 패턴 정합 (`작업 진행해` = ENTRY_INTENT → ALLOW_ENTRY).
- 누적 main first-parent S018 PR: 3 → 본 docs PR 머지 시 4 (#246 T01 + #247 docs + #248 T02 + 본 docs).
- 단위 회귀: 1777 → **1840** (+63 — classifier 39 + hook 19 + intent 5; `npm test` 전체 99 파일 = CI 정합). ※ PR #248 commit message 의 "1835/+58" 은 측정 오기 — dual review (evaluator MEDIUM + codex NEEDS_CHANGES) 동시 지적 후 canonical `npm test` 재측정 1840 확정. 본 docs PR 이 정정값 SSOT.
- KI 변동 0 (잔여 9 유지).
- 자기 검증 루프: 54 → **55** SUCCESS.
- **M0 종료** (T01 advisory + T02 blocking 둘 다 머지). carryover 8 → 7.

## 1. 핵심 발견 — SessionStart 는 exit 2 차단 불가 (G-006 검증)

contract `sprint-018.md` §2 의 T02 산출물 명세는 "`session-start.mjs` blocking path (exit 2 기반 차단)" 였으나, **Claude Code 공식 문서 검증 결과 SessionStart hook 은 exit 2 로 차단 불가** (SessionStart/Setup/Notification 은 exit 2 시 stderr 표시 후 진행, JSON `decision`/`permissionDecision` 미지원). 차단 가능 event 는 PreToolUse (exit 2 또는 `permissionDecision: deny`) / UserPromptSubmit (너무 coarse).

→ **contract deviation 기록** (guardrails.md G-022 §자동 강제 + 변경 이력 + 커밋 메시지 3곳): contract 의 "session-start.mjs exit 2 blocking" 를 **SessionStart advisory 유지 + PreToolUse enforcement** 로 재해석. codex 019e634d §1 P0 권고 정합 ("실제 enforcement는 pre-tool-use.mjs로 이동").

근거 = Claude Code 공식 hooks 문서 (code.claude.com/docs/en/hooks-guide + hooks): "for SessionStart, Setup, Notification, and others, exit 2 shows stderr to the user and execution continues" / PreToolUse 는 exit 2 또는 `permissionDecision: deny` 로 tool call 차단 가능. (검증 경로: claude-code-guide 서브에이전트 문서 조회 — 단, 본 결론의 증거는 위 공식 문서 자체).

## 2. 산출물

| 파일 | 변경 | 내용 |
|---|---|---|
| `.flowset/scripts/g022-tool-classifier.mjs` | 신규 (+212) | `classifyToolForG022(toolName, toolInput, intent, root, memoryRoot)` — deterministic 분류. isCloseoutPath(root/memoryRoot anchor) / shellWriteTargetsCloseout / extractCommitMessage / extractCommitType |
| `.flowset/scripts/g022-tool-classifier.d.mts` | 신규 (+24) | TS declaration |
| `.flowset/hooks/pre-tool-use.mjs` | 수정 (+155/-66) | G-022 게이트 (evaluateG022Gate / resolveLatestUtterance) + 기존 점검 분리 (evaluateShellChecks). exit 2 차단. 전부 fail-open |
| `.flowset/hooks/pre-tool-use.d.mts` | 신규 (+35) | TS declaration |
| `.flowset/hooks/session-start.mjs` | 수정 (+1) | advisory 에 PreToolUse 게이트 활성 명시 |
| `.flowset/scripts/check-finalization-intent.mjs` | 수정 (+14/-2) | entry/delegation phrase 보강 (계속 진행 / 다른 작업 / PR 생성·만들어·올려 / codex 권고대로). bare `권고대로 진행` 제거 |
| `.claude/settings.json` | 수정 (+2/-2) | PreToolUse matcher 에 Write\|Edit\|MultiEdit\|NotebookEdit 추가 |
| `.flowset/guardrails.md` | 수정 (+10) | G-022 blocking enforcement 절 + contract deviation + out-of-scope + 변경 이력 |
| `.flowset/hooks/README.md` | 수정 (+5/-2) | matcher + block 케이스 3종 갱신 |
| `tests/unit/scripts/g022-tool-classifier.test.ts` | 신규 (+263) | classifier 39 회귀 |
| `tests/unit/hooks/pre-tool-use.test.ts` | 신규 (+214) | G-022 게이트 + shell 점검 19 회귀 |
| `tests/unit/scripts/check-finalization-intent.test.ts` | 수정 (+29) | entry phrase 회귀 |

총 +964 / -72 / 12 파일.

## 3. 게이트 동작 (false positive 0)

- **활성 조건**: 최신 user 발화 advisory === `BLOCK_ENTRY` 일 때만. ALLOW_ENTRY / NEUTRAL / 미상 = inert (fail-open).
- **차단**: non-closeout Write/Edit/MultiEdit/NotebookEdit / 새 작업 커밋(feat·fix·refactor·perf) / gh pr create / git push(non-dry-run) / 새 브랜치(checkout -b·switch -c) / dependency mutation(npm install [flags] <pkg>·pnpm·yarn add) / non-closeout 셸 파일 쓰기(Set-Content·Out-File·sed -i, subcommand 단위 타겟 기반).
- **통과 (closeout)**: state.md / handoffs/ / logs/ / eval-results/ / known-issues.md / 사용자 memory + docs·chore·style·ci·test·revert 커밋 + read-only Bash + npm ci·bare install·flag-only. closeout 은 root-anchored (`<root>/.flowset/`, memory 는 `<memoryRoot>/<key>/memory/`).
- **해제**: 매 PreToolUse 호출 transcript 재독 (transcript_path 우선, stale fallback 금지). entry/delegation 발화 박히면 ALLOW_ENTRY → inert.
- **fail-open**: transcript 미발견 / 발화 추출 실패 / 검출 에러 = exit 0.
- **out-of-scope** (guardrails.md 명시): bare `>` redirect / `node -e` 파일쓰기 / cp·mv / MCP write tool.

## 4. dual review (G-021 — 실 호출 증거)

- **evaluator** (`.claude/agents/evaluator.md` 서브에이전트): **Pass 4 / Partial 0 / Fail 0** (contract AC-1 4 sub-criteria) → 머지 권고(조건 없음). 1 LOW (hooks README drift) → 본 PR `f6af913` 흡수.
- **codex** (raw MCP `sandbox=read-only`, threadId `019e634d-ca58-73b1-9789-3433d7eaa23e`): **4 라운드** adversarial review.
  - 1차: BLOCKING 2 (셸 파일 쓰기 우회 / gh pr create·push allow) + NEEDS_CHANGES 4 (transcript_path fallback / allowlist anchor / commit 추출 / NotebookEdit) + NOTABLE 2 (MCP / 권고대로 phrase)
  - 2차: BLOCKING 1 (commandTargetsCloseout substring 우회) + NEEDS_CHANGES 2 (allowlist anchor / npm ci)
  - 3차: BLOCKING 1 (npm install -D 우회) + NEEDS_CHANGES 1 (memory substring)
  - 4차: **BLOCKING 0 / NEEDS_CHANGES 0 / NOTABLE 0 → 머지 가능**
  - hotfix 흡수: `11a68b0`(1차) / `4e941bb`(3차) / `ba0543b`(4차) / `f6af913`(evaluator LOW)

## 5. 검증

- lint PASS / typecheck PASS / `npm test` 전체 **1840** PASS (99 파일) / CI 6/6 SUCCESS (typecheck+test+build / 커밋 메시지 / FlowSet Policy Check / macOS·macOS Intel·Windows sqlite-vec PoC).
- e2e 실 hook stdin → exit code: BLOCK+src write→2 / closeout→0 / feat commit→2 / docs commit→0 / gh pr create→2 / npm install -D→2 / npm ci→0 / chained shell write→2 / Set-Content src ref state.md→2 / root·memory anchor / NotebookEdit→2 / transcript missing fail-open→0.
- **G-022 자기 적용 실증**: 본 세션 모든 feat 커밋 + gh pr create + git push 가 live 게이트 통과 (진입 발화 ALLOW_ENTRY 하 inert). 게이트가 정상 작동하면서 ALLOW_ENTRY 워크플로우를 방해하지 않음 확인.

## 6. 다음 진입점 (본 docs PR 머지 후 — Sprint 018 carryover 7 작업, M0 종료)

1. **T19 사용자 결정** (S018-T03, M1) — Notion Export 옵션 A/B/C/D 명시 선택 후 진입.
2. **Schema v06 spec 재진입** (S018-T04, M1) — 복원 / 재작성 / 폐기 사용자 명시 선택 후.
3. **T17 / T20 / T21** (S018-T05~T07, M2) — dependency 승인 / 학습 데이터셋 박힌 후.
4. **단위 회귀 + 시나리오 cover** (S018-T08~T09, M3).
5. **PRD v0.5.0 + Phase 3 종료 검토 + Sprint 019 시안** (S018-T10~T13, M4~M5).

⚠️ M1 (T19/Schema v06) 은 **사용자 명시 선택** 후 진입 (G-022). handoff §6 박힘 ≠ 본 세션 진입 박음 권고.

## 7. carryover 잔여 (Sprint 017 partial closure §10.3 → CO-7 closed 로 8→7)

| # | 항목 | 상태 |
|---|---|---|
| CO-1 | T17 로컬 임베딩 통합 | 잔여 (S018-T05, M2) |
| CO-2 | T19 Notion Export | 잔여 (S018-T03, M1, 사용자 선택) |
| CO-3 | T20 R3-B 실 학습 | 잔여 (S018-T06, M2) |
| CO-4 | T21 워크스페이스 공유 설계 | 잔여 (S018-T07, M2) |
| CO-5 | T22~T26 종합 + Sprint 019 시안 | 잔여 (S018-T08~T13) |
| CO-6 | Schema v06 spec 재진입 | 잔여 (S018-T04, M1, 사용자 선택) |
| ~~CO-7~~ | ~~G-022 advisory + blocking PR~~ | **closed** (advisory #246 T01 + blocking #248 T02) |
| CO-8 | PRD v0.5.0 발행 | 잔여 (S018-T10, M4) |

작업 단위 carryover **7 미완** (CO-1~CO-6 + CO-8).

---

**Sprint 018 M0 종료 (T01 advisory `5edf048` + T02 blocking `f3f0543`).** G-022 가 SessionStart advisory + PreToolUse 실 차단 자동화로 박힘 — 학습 #8 5번째 위반 차단 path 기계화. 자기 검증 루프 55회차 SUCCESS.

---

## 8. M1 T03 — Notion Export spec (옵션 C, #250 `750da45`)

M0 종료 후 사용자 "핸드오프 읽고 작업 진행해" → AskUserQuestion 으로 다음 작업 = **T19 Notion Export** 선택 → 구현 방식 = **옵션 C (spec only)** 선택 (G-022 §허용 패턴 — 사용자 명시 선택 후 진입).

- 산출물: `.flowset/specs/sprint-018-notion-export-spec.md` (+220, 18 섹션). ExportArtifactBuilder Notion 경로 설계 — 구현은 **Sprint 020** 위임 (PRD §16 로드맵, G-013 단계별).
- 핵심: JSON(`WorkspaceExportV1` v05) = canonical round-trip / Notion = presentation projection + canonical JSON 첨부 (기존 `MarkdownExportService` 패턴 재사용). child database(+data source) 모델 / concrete property schema / query→match→upsert idempotency / file_uploads 라이프사이클 / 토큰 Keychain(G-005) / ExportPrivacyGate + filtered cascade(G-004, §13.1) / rate limit 3rps. S020-a~f 구현 분해 + AC-1~6.
- dual review: evaluator **Pass 5/0/0** (코드/PRD 인용 1:1 검증) + codex `019e63c5` 2 라운드 → 최종 **0/0/0**. codex 1차에서 **BLOCKING 2** (Notion topology — data source 는 database_id 아래 생성 / privacy-filtered payload 의미 미정의) + NEEDS_CHANGES 2 + NOTABLE 1 잡아 `d6707c5`/`3a9f44a` 흡수.
- carryover **7 → 6** (CO-2 T19 closed — 옵션 C spec, 구현 Sprint 020). 자기 검증 루프 **56회차 SUCCESS**.

### 다음 진입점 (본 state 동기화 docs PR 머지 후)
1. **Schema v06 spec 재진입** (S018-T04, M1) — 복원/재작성/폐기 **사용자 명시 선택** 후.
2. **T17 / T20 / T21** (M2) — dependency 승인 / 학습 데이터셋.
3. **PRD v0.5.0 + Phase 3 종료 + Sprint 019 시안** (M4~M5).

본 세션 누적: PR #248(T02 feat) + #249(docs) + #250(T03 spec) 3 머지 + 본 docs PR. codex 협의 thread 2개 (019e634d G-022 blocking 4라운드 / 019e63c5 Notion spec 2라운드).

---

## 9. M1 T04 — Schema v06 spec 복원 (#252 `fe60e8d`, M1 종료)

T03 머지 후 사용자 "다음 작업" = Schema v06 재진입 → 방식 = **복원** 선택 (G-022 §허용 패턴 — 사용자 명시 선택).

- 산출물: `.flowset/specs/sprint-018-schema-v06-spec.md` (+275). PR #241(`feature/WI-S017M5-feat-schema-v06-spec`, CLOSED — **내용 결함 아닌 G-022 진입 타이밍 위반**)의 +269 복원. 파일명 sprint-017→sprint-018 이동.
- 복원 = byte-copy 아님. **현재 코드 재대조**(G-006): `embedding_model` 컬럼 미존재 + `vec_pages` 단일 `float[1024]`(v05.sql) + `V05_SCHEMA_VERSION=2` → close 이후 스키마 변경 0, "v05/v06" 묘사 유효.
- **2차 codex 리뷰(019e653c) 정정 5건**: BLOCKING(1536/3072 allowlist↔테이블 불일치 → 1024/768 고정, CHECK·테이블·UX 3자 일치) + NEEDS_CHANGES 4(vec_notes→vec_notes_1024 마이그레이션 보존 / wiring 실 코드 지점 `EmbeddingClient.processNextEmbeddingJob`·`searchHandlers.ts:134` 명시 [IndexingService 직접 아님] / SearchService allowlist helper 통일 / T17a 진입 게이트 PoC 통일).
- dual review: evaluator **Pass 4/0/0** (복원 충실성 — #241 최종본 `de76ffc` byte-identical, 코드 재대조 줄번호까지 정확) + codex `019e653c` 1차 BLOCKING 1+NEEDS_CHANGES 4 → 2차 **0/0/0**. hotfix `d6ec9ef`.
- carryover **6 → 5** (CO-6 Schema v06 closed — 구현 Sprint 018 M2 T17 위임). **M1 종료** (T03 Notion + T04 Schema v06).

### 다음 진입점 (본 state 동기화 docs PR 머지 후)
1. **T17 로컬 임베딩 통합** (S018-T05, M2) — Schema v06 spec 기준, **sqlite-vec 768dim PoC 선행**(§8-2), T17a~e.
2. **T20 / T21** (M2) / **PRD v0.5.0 + Phase 3 종료 + Sprint 019** (M4~M5).

본 세션 누적: PR #248~#252 5 머지 + 본 docs PR. codex thread 3개 (019e634d G-022 4R / 019e63c5 Notion 2R / 019e653c Schema v06 2R). M0 + M1 종료, carryover 8→5. 자기 검증 루프 T04 **57회차** (정의: dual review 통과 PR 누적, T02 55 → T03 56[§8] → T04 57).
