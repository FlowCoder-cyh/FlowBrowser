# 2026-05-18 핸드오프 — Sprint 015 M2 8/8 완료 (폐기+일반화 단계 종료)

## 메타

- **Phase**: 1 (AI 콘텐츠 메모리 + 워크스페이스 베이스 인프라)
- **Sprint**: 015 M2 8/8 완료 (폐기 + 일반화 단계 100% 종료) → M3 진입 대기
- **PRD 버전**: v0.4.0 본문 완성 (정식 release tag 는 Sprint 015 M6 종료 시점)
- **단위 테스트**: 358 → **417** (M2 변화: +59 cumulative)
- **누적 PR (Sprint 015)**: 32 PR (#98 ~ #129)
- **다음 세션 작업**: M2 종합 evaluator 채점 → M3-1 SQLite v04.sql schema 진입

---

## 1. 본 세션 진행 요약 (2026-05-17 → 2026-05-18)

### 1.1 작업 흐름

**Sprint 015 M2 폐기+일반화 8 PR + M1 핫픽스 PR b10.1 진행** = 총 9 PR 머지.

| PR | M2 단계 | 핵심 | 단위 변화 |
|---|---|---|---|
| #121 | M1 핫픽스 b10.1 | A1 §B GENERALIZE 2→4 + §A1 IPC 9 + PRD §05 25 cross-link + §18 TBD 정리 | 0 |
| #122 | **M2-1** | AIResponseCache (4 kind + peek) + TranslationCache 어댑터 + UserSetting v04Enabled flag + featureFlags (env > setting) | +52 |
| #123 | **M2-2** | IndexedPageStore base (Page + Visit + recordVisit 단일 TX + workspace 격리 + content_hash dedupe + cascade DELETE) + PageResultStore 어댑터 (sideWriteFailureCount 관측) | +49 |
| #124 | **M2-3** | SummarizationPlanner DEPRECATE 마킹 (5축: 모듈 헤더 + 함수 2개 + IPC handler 2개 + preload API 2개) + warnDeprecatedOnce 모듈 flag | +4 |
| #125 | **M2-4** | summarize-* IPC 폐기 (handler 2개 + send 4종) + SummarizationPlanner 모듈 (226 lines) + 단위 테스트 22 + preload API 4종 + TranslationPanel summary 분기 + CSS dead style 13 | -22 |
| #126 | **M2-5** | paragraphs/page IPC 폐기 (handler 4개 + flag 2종 + send 10종) + preload API 4개 + listener 10개 + Payload 11종 + TranslationPanel 549→36 lines stub + CSS dead style 21 | 0 (테스트 0건) |
| #127 | **M2-6** | render/restore IPC 폐기 (handler 3개) + TranslationRenderer 모듈 + TranslationPanel 파일 자체 삭제 + App.tsx panelOpen state + CSS dead 5 | -12 |
| #128 | **M2-7** | ProviderAdapter chat/embed/chatStream optional + types 5종 (ChatMessage/ChatRequest/ChatResponse/EmbedRequest/EmbedResponse) + OpenAIApiKeyProvider chat+embed 구현 + CodexLoginProvider chat+embed throw | +20 |
| #129 | **M2-8** | v0.3 단위 테스트 3 파일 (-34) + services.ts dead export 4종 + IPC handler 2개 + storage nodesSignatureFromTexts + preload 2종 + legacy 회귀 2 케이스 | -32 |

**단위 테스트 누적 변화**: 358 (Sprint 014 종료) → 417 (M2-8 종료)
- M0~M1: 358 유지 (문서 작업)
- M2-1 ~ M2-2: +101 (어댑터 + 신규 모듈)
- M2-3 ~ M2-8: -42 (페이지 번역 use case 완전 제거)
- Net: +59

### 1.2 평가자 누적 통계 (M2 8 PR)

| PR | evaluator | codex BLOCKING | NON-BLOCKING | 핫픽스 처리 |
|---|---|---|---|---|
| M2-1 | Pass 8/0/0 | 0 | 3 | 본 PR 내 (peek + parseEntry + LRU) |
| M2-2 | Pass 8/0/0 | **1** | 3 | 본 PR 내 (recordVisit 단일 TX) |
| M2-3 | Pass 6/2/0 | 0 | 5 | 본 PR 내 (PRD §10.1 정정 + 워딩) |
| M2-4 | Pass 8/0/0 | 0 | 1 | 본 PR 내 (CSS dead style 13) |
| M2-5 | Pass 7/1/0 | 0 | 4 | 본 PR 내 (CSS dead style 21 + 카운트 9→10) |
| M2-6 | Pass 7/1/0 | 0 | 3 | 본 PR 내 (CSS dead 5 + PRD M2-8 contract) |
| M2-7 | Pass 8/0/0 | 0 | 4 | 본 PR 내 (Codex 401 refresh test + PRD M5 contract) |
| M2-8 | Pass 7/1/0 | **1** (자체 적용) | 2 | 본 PR 내 (PRD 카운트 정정 + legacy 회귀 2) |

**합계**: evaluator Partial 4건 / codex BLOCKING 2건 / NON-BLOCKING 25건 → **본 PR 내 즉시 핫픽스 패턴 (KI 누적 0건 유지)**.

### 1.3 PRD §19.5.1 M2 진행률 매트릭스

```
M2-1 ✅ AIResponseCache 신규 + TranslationCache 어댑터 + feature flag (#122)
M2-2 ✅ IndexedPageStore base + PageResultStore 어댑터 + recordVisit 단일 TX (#123)
M2-3 ✅ SummarizationPlanner DEPRECATE 마킹 (#124)
M2-4 ✅ translate:summarize-* IPC 폐기 + SummarizationPlanner 제거 (#125)
M2-5 ✅ translate:paragraphs/page IPC 폐기 + TranslationPanel 이행 stub (#126)
M2-6 ✅ translate:render/restore IPC 폐기 + TranslationRenderer/Panel 완전 제거 (#127)
M2-7 ✅ ProviderAdapter chat/embed PARTIAL (#128)
M2-8 ✅ v0.3 테스트 재작성 + services.ts dead cleanup (#129)
```

**M2 = 8/8 완료. v0.3 페이지 번역/요약 use case 100% 폐기 + v0.4 chat/embed 인터페이스 도입 완성**.

### 1.4 신규 모듈 (M2 도입)

| 모듈 | 위치 | 용도 |
|---|---|---|
| `AIResponseCache` | `src/storage/AIResponseCache.ts` | 4 kind (translation/embedding/ai_response/tag) × LRU + TTL + peek/lookup/store/invalidate/clearKind/stats |
| `featureFlags` | `src/storage/featureFlags.ts` | `isV04Enabled` helper (env `FLOWBROWSER_V04` > UserSetting.v04Enabled) |
| `IndexedPageStore` | `src/storage/IndexedPageStore.ts` | Page + Visit + recordVisit (단일 TX) + workspace 격리 + content_hash dedupe + cascade DELETE + JSON 영속 (M3 SQLite 흡수 예정) |
| 신규 타입 | `src/ai/types.ts` | ChatMessage / ChatRequest / ChatResponse / EmbedRequest / EmbedResponse + ProviderInfo.supportsChat/Embed flag |
| ProviderAdapter chat/embed | `src/ai/providers/{OpenAI,Codex}*` | OpenAI: chat completions + embeddings ($0.02/M, 1024 차원) / Codex: chat Responses API + embed throw |

### 1.5 폐기 코드 (M2 제거)

| 카테고리 | 항목 | 라인 (대략) |
|---|---|---|
| 모듈 | SummarizationPlanner.ts + TranslationRenderer.ts + TranslationPanel.tsx | ~480 |
| IPC handler (main) | summarize 2 + paragraphs/page 4 + render/restore 3 + pageResult lookup/store 2 = **11** | ~600 |
| send 채널 (main) | summary 4 + paragraphs 5 + page 5 = **14** | (handler 내포) |
| preload API | summarizePage/abortSummarize + paragraphs/page 4 + render/restore/restoreCurrent 3 + pageResultApi.lookup/store 2 = **11** | ~100 |
| preload listener | summary 4 + paragraphs 5 + page 5 = **14** | (API 내포) |
| preload Payload 타입 | summary 4 + paragraphs+page 11 + PageResultLookupArgs/Entry 2 = **17** | ~150 |
| services.ts export | persistPageResult / pageResultLookup / extractWebContentsPageNodes / extractWebContentsParagraphs = **4** | ~80 |
| storage helper | nodesSignatureFromTexts | 5 |
| 단위 테스트 | SummarizationPlanner.test 18 + .deprecate 4 + TranslationCache.test 15 + .lru 3 + PageResultStore.test 16 + TranslationRenderer.test 12 = **68 케이스** | ~700 |
| 통합 CSS | .panel-summary* / .panel-chunk* / .panel-toolbar* / .panel-btn* / .panel-rows* / .panel-progress* / .panel-restore* / .translation-panel / .panel-header* / .panel-empty = **39 스타일** | ~250 |

**Net 코드 감소**: 약 -2400 lines (모듈 + IPC + preload + UI + CSS + 테스트).

### 1.6 Bundle 크기 변화

| 단계 | JS bundle | CSS bundle |
|---|---|---|
| M2-1 시작 | 329.26 kB | 26.49 kB |
| M2-4 종료 | (변화 없음) | 25.36 kB (-1.13) |
| M2-5 종료 | 307.30 kB (-16.37 / -5%) | 23.02 kB (-2.34) |
| M2-6 종료 | 305.80 kB (-1.5) | 22.27 kB (-0.75) |
| **M2-8 종료** | **305.80 kB** | **22.27 kB** |

**Net**: JS -23.46 kB (-7.1%) / CSS -4.22 kB (-15.9%).

### 1.7 보존 결정 (M5 어댑터 제거 시 함께 폐기)

| 항목 | 보존 이유 | M5 폐기 트리거 |
|---|---|---|
| `pageResultStore` 인스턴스 + `pageResult:stats/clear` IPC + PageCachePanel | PageCachePanel renderer 사용 중 (캐시 통계 표시) | M5-8 어댑터 제거 시 동반 |
| `pageResultStore` 어댑터 모드 (PageResultStore 클래스 spec) | `.adapter.test` (11 케이스) 에서 IndexedPageStore side-write 검증 | M5-8 동반 |
| `translateApi.request` IPC | selection 번역 popup (handleContextMenuTranslate) 사용 중 | M5-5 ChatService 도입 시 마이그레이션 |
| `translate()` 메서드 (ProviderAdapter) | services.ts executeTranslateRequest 가 selection 번역에 사용 | M5-8 ChatService.chat() 마이그레이션 후 제거 |
| `panel:set-open` IPC + `browserApi.setPanelOpen` API | WebContentsView 가시성 토글 (M5 ChatPanel 재사용) | M5-6 ChatPanel 도입 시 재활성 |
| `tabGuard.ts` / `tabLabel.ts` 모듈 + 단위 테스트 | M5 ChatPanel 재사용 결정 위임 | M5 결정 시점 |
| `perception/ParagraphExtractor` + `PageNodeExtractor` 모듈 | M3 IndexingService 가 직접 import 활용 예정 | (유지) |
| `scanWebContentsFields` (services.ts) | selection 번역 popup 사용 중 | M5-5 시점 결정 |
| `UrlBar onTogglePanel/panelOpen` optional props | M5 ChatPanel 도입 시 재활성 | M5-6 |

### 1.8 SSOT 갱신 (Sprint 015 M2 진행 중)

| 갱신 | 위치 | 내용 |
|---|---|---|
| §17 S3 feature flag | v04-direction.md | PR #122 시점 박힘 + UserSetting + env 우선순위 |
| §B GENERALIZE 4종 | v04-migration-matrix.md | b7.1 ProviderAdapter/UsageLog 추가 + 분류 요약 표 |
| §A 매트릭스 카운트 | v04-dependency-graph.md | 19→21 폐기 / §A1 10→9 / §A2 9→12 정정 |
| §05.3.1 IPC 25개 | 05_crud_matrix.md | chat:abort 포함 + cross-link 5 파일 동반 |
| §19.5.1 M2-1~M2-8 ✅ | 19_migration_v03_v04.md | 모든 M2 PR 완료 표기 + PR # + 핫픽스 명시 |
| §19.5.4 M5-5/M5-8 | 19_migration_v03_v04.md | M2-7 codex E/F → executeTranslateRequest 마이그레이션 + fetchImpl 통일 contract |

### 1.9 G-NNN 가드레일 활성 (M2 시점)

본 Sprint 진입 시 활성화된 G-012/G-013/G-014 모두 정합:

| G | 상태 | M2 내 적용 |
|---|---|---|
| G-012 v0.4 SSOT 우선 | ✅ | 모든 PR이 v04-direction.md / PRD §19.5.1 동기 갱신 (역방향 0) |
| G-013 단계별 PR 전략 | ✅ | M2-1~M2-2 신규+어댑터 → M2-3 deprecate 마킹 → M2-4~M2-6 사용처+IPC 제거 → M2-7 신규 인터페이스 → M2-8 cleanup |
| G-014 데이터 마이그레이션 | (대기) | M3 IndexedPageStore SQLite 진입 시점 활성 (M3-1) |

---

## 2. M2 핵심 학습 (M3 진입 적용 필수)

### 2.1 본 PR 내 즉시 핫픽스 패턴 (KI 누적 0 유지)

evaluator + codex 병렬 평가 후 발견 약점은 머지 전 본 PR 내 정정:
- **즉시 정정 권고**: BLOCKING 1건 (M2-2 recordVisit 단일 TX) 머지 전 차단 → 본 PR 내 신규 메서드 추가로 해결
- **즉시 정정 NON-BLOCKING**: CSS dead style / SSOT 카운트 / PRD cross-link / 주석 잔여 식별자
- **위임 결정**: 일정 외 작업 (services.ts cleanup → M2-8 / TranslationPanel 자체 폐기 → M2-6) 명시적으로 cross-link

**누적 결과**: Sprint 015 KI 0건 유지 (10 PR 연속).

### 2.2 evaluator 우선순위

- **F 추측 금지**: 실제 코드 grep + 외부 사실 공식 인용 + 기준일 → 모든 식별자 / API spec / 가격 검증
- **D SSOT 인용**: PRD §NNN 정확 인용 (M2-3 §10.6 → §10.1 정정 사례)
- **H 일관성**: 카운트 정합 (헤더 vs 본문 vs 합산, M2-5 send 9→10 정정 사례)

### 2.3 codex 정밀 검토 5 관점

본 Sprint M2 8 PR 모두 동일 5 관점 적용:
1. 실제 코드 grep 사실
2. 외부 사실 정합 (OpenAI API spec / Electron / Node.js)
3. 카운트 정확
4. 시제 분리 (M2-N 현재 vs 미래 contract)
5. schema 다층 정합 (Page/Visit/Embedding ↔ §04 ↔ §05 ↔ §10)

### 2.4 단계별 PR 전략 (G-013) 패턴

**M2 8 PR이 검증한 패턴**:
1. 신규 모듈 + 어댑터 도입 (M2-1/M2-2)
2. deprecate 마킹 단계 (M2-3) — 코드 동작 보존, 호출 시 dev console.warn
3. 실제 제거 단계 (M2-4/M2-5/M2-6) — 모듈 + IPC + UI 동시 제거
4. 신규 인터페이스 도입 (M2-7) — PARTIAL "시작" 단계
5. cleanup 단계 (M2-8) — dead export + 테스트 재작성

**M3+ 적용**: 동일 패턴 — 신규 (M3-1/M3-2 SQLite/sqlite-vec) → 확장 (M3-3 IndexedPageStore SQLite) → 마이그레이션 (M3-6/M3-7).

### 2.5 어댑터 모드 패턴 (M5 종료 시 일괄 제거)

본 Sprint 도입된 어댑터 모드 (TranslationCache → AIResponseCache, PageResultStore → IndexedPageStore, ProviderAdapter translate → chat/embed):
- backend 주입 시 위임 / 미주입 시 v0.3 동작 100% 보존
- 디폴트 flag false → 안전한 점진 전환
- M5-8 시점에 어댑터 + v0.3 인스턴스 일괄 제거

**M3 진입 시 주의**: IndexedPageStore SQLite 전환은 어댑터 패턴 X (M3-3에서 직접 schema 마이그레이션). PageResultStore 어댑터는 M5-8까지 보존.

---

## 3. M3 진입 권장 (다음 세션)

### 3.1 M3 contract (PRD §19.5.2)

| PR | 작업 |
|---|---|
| M3-1 | SQLite v04.sql schema + 통합 DB 진입점 |
| M3-2 | sqlite-vec native module 빌드 검증 + VectorIndex (대안: in-memory fallback) |
| M3-3 | IndexedPageStore SQLite 확장 (Page + Visit + 본문 캐시 content_hash dedupe) — 현재 JSON 영속 → SQLite 흡수 |
| M3-4 | NoteStore / AiChatHistoryStore / TagStore / EmbeddingQueue |
| M3-5 | EmbeddingClient (text-embedding-3-small 1024 차원, BYOK 디폴트) — M2-7 OpenAI embed 활용 |
| M3-6 | migrations/v03_to_v04.ts + dry-run + revert + 8 회귀 케이스 (G-014 활성) |
| M3-7 | UsageLog GENERALIZE schema 마이그레이션 + PageCachePanel PARTIAL |

### 3.2 M3 진입 전 권고 작업

1. **M2 종합 evaluator 채점** — Sprint 015 M2 8/8 + 통합 정합 (D/F/H 카테고리 가중)
2. **state.md M3 진입 갱신** — Sprint 015 진행 통계 갱신 (M0~M2 완료, M3 진입 명시)
3. **`.flowset/specs/m3-spike-decisions.md` 신규** — M3-2 sqlite-vec native build PoC 결과 + 대안 (in-memory fallback) 결정 박힘

### 3.3 M3 핵심 리스크

| # | 리스크 | 완화 |
|---|---|---|
| 1 | sqlite-vec Windows/macOS native build (R1) | M3-2 30분 PoC 우선, 실패 시 in-memory cosine fallback (PRD §15 정합) |
| 2 | IndexedPageStore SQLite 전환 시 v0.3 데이터 손실 | G-014 dry-run + 자동 백업 + revert 절차 (M3-6 활성) |
| 3 | 임베딩 비용 측정 ($3/월 / 1만 페이지 임계) | M3-5 시작 시 토큰 수 측정 → 초과 시 본문 청크 길이 제한 (예: 2000 토큰) 적용 |
| 4 | UsageLog schema 마이그레이션 (M3-7) | v0.3 enum (translation/summary/tts/stt) → v0.4 (chat/embed/tag/background_translation) 매핑 정합 |

### 3.4 신규 가드레일 활성 (M3)

- **G-014 데이터 마이그레이션 dry-run + 자동 백업** — M3-1 시점 활성. 백업 위치: `<userDataDir>/backup/v03/<ISO_timestamp>/`. 로그: `<userDataDir>/migration-v04.log`. revert 절차 spec.

---

## 4. 다음 세션 첫 행동 권장

### 4.1 즉시 (M2 종료 마무리)

1. **M2 종합 evaluator 호출** — 8 PR 통합 정합 + KI 0건 유지 + 단위 +59 / Bundle -23.46 kB 검증
2. **state.md 갱신** — Sprint 015 M0~M2 완료 + M3 진입 명시 + 다음 작업 M3-1 명시
3. **`.flowset/known-issues.md` 확인** — KI 0건 유지 검증

### 4.2 M3 진입

4. **M3-2 sqlite-vec native build 30분 PoC** — Windows + macOS 빌드 가능성 확인 (실패 시 in-memory fallback)
5. **M3-1 PR — SQLite v04.sql schema + 통합 DB 진입점** — 신규 모듈 도입 (G-013 단계별 PR 1단계)

### 4.3 M3 진입 시 주의 (M2 학습 적용)

- **codex + evaluator 병렬 평가 패턴 유지** — 매 PR 적용
- **본 PR 내 즉시 핫픽스 패턴 유지** — KI 누적 0 유지
- **G-014 dry-run + 자동 백업 활성** — M3-1 시점 활성
- **실제 코드 grep + 외부 사실 검증** — M3 외부 의존 (sqlite-vec / OpenAI 임베딩) 증가 → 검증 부담 증가
- **schema 다층 정합** — §04 Page/Visit/Embedding 컬럼 spec ↔ schema/v04.sql ↔ IndexedPageStore 구현 동기

---

## 5. 기억해야 할 결정 (M2 학습 누적)

### 5.1 어댑터 패턴 (M5-8 일괄 제거)

- TranslationCache → AIResponseCache (kind='translation')
- PageResultStore → IndexedPageStore (side-write)
- ProviderAdapter.translate → chat/embed (M5-8 final 제거)

### 5.2 신규 v0.4 모듈 (M2 완성)

- `AIResponseCache` (4 kind LRU+TTL JSON)
- `IndexedPageStore` (Page+Visit+recordVisit 단일 TX, JSON → M3 SQLite)
- `featureFlags` (env > UserSetting)

### 5.3 신규 ProviderAdapter 메서드 (M2-7)

- `chat?(request: ChatRequest): Promise<ChatResponse>` — multi-turn 지원
- `embed?(request: EmbedRequest): Promise<EmbedResponse>` — 1024 차원 디폴트
- `chatStream?(request)` — optional (M5 ChatService 구체 spec)
- `info.supportsChat / supportsEmbed` — 능력 표기

### 5.4 fetch 주입 패턴 (M5-8 통일 contract)

- 현재: OpenAIApiKeyProvider `globalThis.fetch` 직접 / CodexLoginProvider `fetchImpl` 주입
- M5-8: 통일 (PRD §19.5.4 M2-7 codex F 권고 contract)

### 5.5 가드레일 G-001 ~ G-014 (14종 활성, M3에서 G-014 본격 활성)

---

## 6. 본 세션 작업 산출물 (참조)

### 6.1 코드 변경 (M2 8 PR)

신규 모듈 + 어댑터 (M2-1 ~ M2-2):
- `src/storage/AIResponseCache.ts` (~300 lines)
- `src/storage/featureFlags.ts` (~30 lines)
- `src/storage/IndexedPageStore.ts` (~500 lines)
- `src/ai/types.ts` (+72 lines, 5 신규 타입)
- `src/ai/ProviderAdapter.ts` (chat/embed/chatStream optional + translate @deprecated)
- `src/ai/providers/OpenAIApiKeyProvider.ts` (chat + embed + throwForHttpStatus + estimateEmbedCost)
- `src/ai/providers/CodexLoginProvider.ts` (chat + embed throw + splitMessagesForResponsesApi)

폐기 (M2-3 ~ M2-6, M2-8):
- `src/ai/SummarizationPlanner.ts` (226 lines) — 삭제
- `src/perception/TranslationRenderer.ts` (137 lines) — 삭제
- `src/renderer/src/translation/TranslationPanel.tsx` (549 lines → 36 stub → 삭제) — 삭제
- `src/main/index.ts` (~600 lines IPC handler + flag + send) — 제거
- `src/preload/index.ts` (~250 lines API + listener + Payload) — 제거
- `src/main/services.ts` (~80 lines dead export + IPC handler) — 제거
- `src/storage/PageResultStore.ts` `nodesSignatureFromTexts` (5 lines) — 제거

### 6.2 단위 테스트 변화

신규 (M2-1 ~ M2-2, M2-7):
- `tests/unit/storage/AIResponseCache.test.ts` (29)
- `tests/unit/storage/TranslationCache.adapter.test.ts` (14, 본 PR +2 legacy 회귀)
- `tests/unit/storage/featureFlags.test.ts` (7)
- `tests/unit/storage/UserSettingStore.test.ts` (+4 v04Enabled)
- `tests/unit/storage/IndexedPageStore.test.ts` (38)
- `tests/unit/storage/PageResultStore.adapter.test.ts` (11)
- `tests/unit/ai/ProviderAdapter.test.ts` (20)

삭제 (M2-4, M2-6, M2-8):
- `tests/unit/ai/SummarizationPlanner.test.ts` (18)
- `tests/unit/ai/SummarizationPlanner.deprecate.test.ts` (4)
- `tests/unit/perception/TranslationRenderer.test.ts` (12)
- `tests/unit/storage/TranslationCache.test.ts` (15)
- `tests/unit/storage/TranslationCache.lru.test.ts` (3)
- `tests/unit/storage/PageResultStore.test.ts` (16)

**Net**: 신규 +123 / 삭제 -68 = +59 (358 → 417).
**모듈**: 신규 +6 / 삭제 -6 = 0 (~33 → 30 — 신규 7 - 폐기 -10이 합치면서 일부 어댑터 변환).

### 6.3 SSOT 갱신 (.flowset/specs/ + docs/prd/)

- `v04-direction.md` §17 S3 PR #122 박힘
- `v04-migration-matrix.md` §B GENERALIZE 4종 (b7.1 정정 포함)
- `v04-dependency-graph.md` §A 매트릭스 21/9/12 정정
- `docs/prd/19_migration_v03_v04.md` §19.5.1 M2-1~M2-8 ✅ + §19.5.4 M5-5/M5-8 contract
- `docs/prd/05_crud_matrix.md` §5.3.1 IPC 25개 + cross-link 5 파일

### 6.4 운영 인프라

- `.flowset/known-issues.md` — KI 누적 0건 유지 (Sprint 015 진행 중 핫픽스 패턴으로 즉시 해소, 10 PR 연속)
- `.flowset/contracts/sprint-015.md` — M2 8 PR 종료 표기 (다음 세션에서 갱신)
- `.flowset/guardrails.md` — G-013 단계별 PR 패턴 8 PR 검증 (M2 학습)

---

## 7. Sprint 015 진행 통계 (M0~M2 종료 시점)

| 항목 | 수치 |
|---|---|
| **Sprint 015 PR** | 32개 (#98 ~ #129) |
| 누적 PR (M0~M2) | 32개 |
| PRD v0.4 본문 lines | 약 5,200+ |
| `.flowset/specs/` 누적 lines | 약 1,800+ |
| codex blocking 핫픽스 (M1) | 127건 |
| codex BLOCKING (M2) | 2건 (M2-2 / M2-8 자체 적용) |
| codex NON-BLOCKING (M2) | 25건 (모두 본 PR 내 정정) |
| evaluator Partial/Fail (M2) | 4 Partial / 0 Fail (모두 본 PR 내 정정) |
| SSOT 갱신 (M0~M2 누적) | 약 30+ |
| 신규 가드레일 활성 | G-012 (M1) / G-013 (M2) / G-014 (M3 대기) |
| Known Issue 누적 | **0건** (32 PR 연속 즉시 핫픽스로 해소) |
| 단위 테스트 | 358 → 417 (+59) |
| commit-check CI | 32 PR 모두 success (G-009 NNN 한 분절 11 Sprint 연속) |
| JS bundle | 329.26 → 305.80 kB (-23.46 / -7.1%) |
| CSS bundle | 26.49 → 22.27 kB (-4.22 / -15.9%) |

---

## 8. 다음 세션 컨텍스트 (M3 진입)

### 8.1 즉시 읽어야 할 파일

- `.flowset/state.md` (자동 주입)
- **본 핸드오프** (`.flowset/handoffs/2026-05-18.md`)
- `.flowset/contracts/sprint-015.md` (M3 milestone 진입 spec, §2 T11~T15)
- `.flowset/specs/v04-direction.md` (방향 SSOT, 결정 박힘)
- `.flowset/specs/v04-migration-matrix.md` (A1 GENERALIZE 4종)
- `.flowset/specs/v04-data-migration.md` (A3 데이터 마이그레이션 5단계)
- `docs/prd/04_data_model.md` (Page/Visit/Embedding 컬럼 spec)
- `docs/prd/19_migration_v03_v04.md` §19.5.2 (M3 7 PR spec)
- `.flowset/guardrails.md` (G-014 활성 검증)

### 8.2 잊으면 안 되는 맥락

- **PRD v0.4 정식 발행 (release tag)** 은 Sprint 015 M6 종료 시점
- **단위 테스트 417 유지** — M3 진입 시 IndexedPageStore SQLite 전환 회귀 0 강제
- **G-014 dry-run + 자동 백업** — M3-1/M3-6 활성. revert 절차 spec 박힘 (PRD §19.3)
- **sqlite-vec native build 리스크** — M3-2 PoC 우선, 실패 시 in-memory cosine fallback
- **PageResultStore 어댑터 모드 보존** — M5-8 일괄 제거. M3 IndexedPageStore SQLite 전환과 별개
- **fetchImpl 통일 contract** — M5-8 (M2-7 codex F 권고)

### 8.3 본 세션 학습 (M3+ 진입 시 핵심)

1. **본 PR 내 즉시 핫픽스 패턴** — codex + evaluator 발견 약점 머지 전 정정 (KI 누적 0 유지)
2. **단계별 PR 전략 (G-013)** — 신규+어댑터 → deprecate 마킹 → 실제 제거 → cleanup 4 단계
3. **어댑터 모드 보존 명시** — M5-8 일괄 제거 contract 정합
4. **schema 다층 정합** — §04 ↔ §05 ↔ §10 ↔ 구현 동기
5. **외부 사실 공식 인용 + 기준일** — OpenAI 가격 / Electron API / sqlite-vec spec
6. **카운트 정확** — 헤더 vs 본문 vs 합산 (M2-5 send 9→10 / M2-8 PRD 카운트 사례)
7. **선택 보존 명시** — 페이지 번역 폐기 vs selection 번역 보존 / panel:set-open 보존 / perception 모듈 보존
8. **codex BLOCKING 자체 적용 가능** — codex 검토 중 자체적으로 코드 수정 (M2-8 사례)

---

## 9. 의사 결정 기록

### 9.1 본 세션 진행 흐름 (사용자 명시 결정)

- "진행해" (8회) → M1 종합 evaluator + M2-1~M2-8 전체 자율 진행. 각 PR 본 PR 내 핫픽스 패턴 적용.
- "일단 핸드오프 작성하고 검증받아 신규세션에서 진행하게" → M2 8/8 종료 시점 핸드오프 작성 + state.md 갱신 + M2 종합 evaluator 호출

### 9.2 이전 세션 결정 유지

- 한국어 문서 우선 (G-008)
- 추측 금지 (G-006)
- main 직접 push 금지 + PR + 오토머지 (G-007)
- 커밋명 NNN 한 분절 (G-009, 학습 29·30 적용)
- BYOK 디폴트 (G-003 강화)
- 위장 X / 자체 정체성 (Brave/Edge UA)
- 결격사유 0 — 헤드리스 / 봇 우회 / 자동 prefetch X
- Chrome 확장 못 하는 영역에 집중 (워크스페이스 + 대용량 인덱스 + 시스템 통합)

---

## 10. M3 후보 우선순위 (다음 세션)

**P0 (M3 진입 필수)**:
- M2 종합 evaluator 채점 통과 (Pass ≥ 8)
- state.md / handoff M3 진입 갱신
- M3-2 sqlite-vec native build PoC (30분, 실패 시 fallback 결정)
- M3-1 SQLite v04.sql schema + 통합 DB 진입점

**P1 (M3 진행)**:
- M3-3 ~ M3-7 (5 PR 순차)
- 회귀 셋 18 케이스 자동화 셋 구축 (A2 §E1)
- M3 종료 evaluator + Known Issue 등록 (예상 HIGH 0)

**P2 (M4 진입 준비)**:
- 페이지 인덱싱 hook (did-finish-load → IndexingService)
- AI 자동 태깅 (AutoTagger 6 kind)
- dwell 측정 + Privacy IndexingGate

---

## 11. M2 종합 evaluator 결과 (2026-05-18)

### 11.1 8 카테고리 채점

| 카테고리 | 판정 | 핵심 |
|---|---|---|
| **C 구조** | Pass | 8 PR 모두 PRD §19.5.1 M2-N ✅ + 머지 commit hash 정합. 신규 모듈 위치 / 폐기 모듈 완전 삭제 / 보존 결정 모두 정합 |
| **D SSOT 인용** | Pass | 단위 417 실측 = 핸드오프 일치 / Bundle JS 305.80 + CSS 22.27 kB 실측 일치 / M5-5/M5-8 contract 정합 |
| **E 완전무결** | Pass | **KI 누적 0건 (32 PR 연속)** / TBD 0 / IPC 폐기 잔존 0 |
| **F 추측 금지** | Pass | 본 PR 내 즉시 핫픽스 패턴 효과성 입증 — BLOCKING 2 + NB 25 + Partial 4 모두 머지 전 정정 |
| **G 한국어** | Pass | 커밋명 + 주석 + 코드 식별자 정합 |
| **H 일관성** | Pass | 어댑터 모드 3종 M5-8 일괄 제거 contract 정합 / G-013 4 단계 패턴 검증 / 단위 +59 정확 |
| **I 기술 정확성** | Pass | recordVisit 단일 TX / dimensions=1024 / $0.02/M / Codex output_text 변환 / sideWriteFailureCount / G-014 M3-1 활성 명시 |
| **J 외부 사실** | Pass | OpenAI 가격 2026-05-16 기준일 / Electron / Node.js 표준 |

### 11.2 종합 판정

- Pass: **8** / Partial: 0 / Fail: 0
- 종합 판정: **Pass**
- **M3 진입 가능 여부: YES (핫픽스 불필요)**

### 11.3 다음 세션 첫 행동 (evaluator 권고)

1. state.md M0~M2 완료 + M3 진입 명시로 갱신 ✅ (본 PR 포함)
2. M3-2 sqlite-vec native build 30분 PoC (Windows + macOS) — 실패 시 in-memory cosine fallback 결정 박힘
3. M3-1 PR (SQLite v04.sql schema + 통합 DB 진입점) 진입 — G-013 1단계 + G-014 활성

---

## 12. 마무리

Sprint 015 M2 8/8 완료. v0.3 페이지 번역/요약 use case 100% 폐기. v0.4 ProviderAdapter chat/embed 인터페이스 도입 완성. **KI 누적 0건 유지 (32 PR 연속)**. **M2 종합 evaluator: Pass 8/0/0 → M3 진입 가능**.

다음 세션에서 **M2 종합 evaluator 채점 + state.md M3 진입 갱신 후 M3-2 sqlite-vec PoC → M3-1 진입** 권장.

본 세션의 8 PR 진행 + 본 PR 내 즉시 핫픽스 패턴 + codex/evaluator 병렬 평가 패턴 + 어댑터 모드 보존 contract 4 패턴이 M3~M6 진입 시 표준 — 단계별 PR (G-013) + 데이터 마이그레이션 안전망 (G-014) + 회귀 0 강제.

수고하셨습니다.

---

## 13. M3 종료 (2026-05-18 추가 세션, 8 PR 일괄 진행)

**M3 spike + 7 PR 모두 완료** — Sprint 015 M0~M3 종료. 단위 417 → 567 (+150 본 M3, 누적 358 → 567 본 Sprint 209). KI 누적 0건 유지 (40 PR 연속).

### 13.1 M3 진행 매트릭스

| PR | M3 단계 | 핵심 | 단위 변화 |
|---|---|---|---|
| #131 | M3-spike | sqlite-vec 30분 PoC (Windows Electron 39 6/6 PASS) — better-sqlite3 12.10.0 / sqlite-vec 0.1.9 / float[1024] partition 격리 검증 / in-memory fallback 미발동 / macOS 미검증 KI 후보 | 0 |
| #132 | **M3-1** | SQLite v04.sql schema + FlowbrowserDatabase 진입점 — 9 entity + 9 인덱스 + 2 vec0 + PRAGMA foreign_keys/WAL + ensureDefaultWorkspace + CHECK 제약 + CASCADE | +25 |
| #133 | **M3-2** | VectorIndex 모듈 + vec_pages/vec_notes cascade trigger 2종 — UPSERT (DELETE+INSERT atomic) + workspace partition 격리 + 차원 검증 + pages/notes DELETE 시 자동 정리 | +19 |
| #134 | **M3-3** | IndexedPageStoreSqlite 신규 (JSON IndexedPageStore 보존) — FlowbrowserDatabase pages/visits 활용 + 단일 TX recordVisit + prepared statement 캐시 + 'default' string → UUID 매핑 | +25 |
| #135 | **M3-4** | NoteStore + AiChatHistoryStore + TagStore + EmbeddingQueue — 4 storage 모듈 + embedding_queue 테이블 schema 추가 + 인덱스 2종 (status+priority/target) | +46 |
| #136 | **M3-5** | EmbeddingClient + processNextEmbeddingJob 파이프라인 — ProviderAdapter.embed wrapper + text-embedding-3-small 1024 + BYOK + Queue→Provider→VectorIndex 단일 잡 처리 + orphan/failed/idle 상태 | +18 |
| #137 | **M3-6** | v03→v04 마이그레이션 + 8 회귀 케이스 — 5단계 (백업/dry-run/매핑 5종/.deprecated/log) + revert (idempotent 재시도) + G-014 본격 활성 | +10 |
| #138 | **M3-7** | UsageLog GENERALIZE — V04Feature 4종 (chat/embed/tag/background_translation) + 옵션 컬럼 3 (workspace/model/duration) + summarize byWorkspace+byModel + migrateLegacyEntries idempotent + PageCachePanel PARTIAL copy 갱신 | +7 |

### 13.2 신규 모듈 (M3 도입 11개)

| 모듈 | 위치 | 용도 |
|---|---|---|
| `FlowbrowserDatabase` | `src/storage/Database.ts` | better-sqlite3 + sqlite-vec 통합 진입점 + schema 적용 + Workspace CRUD + schema_meta |
| `schema/v04.sql` | `src/storage/schema/v04.sql` | DDL 10 테이블 + 11 인덱스 + 2 vec0 virtual + 2 trigger |
| `VectorIndex` | `src/storage/VectorIndex.ts` | sqlite-vec wrapper + UPSERT TX + workspace 격리 검색 + 차원 검증 |
| `IndexedPageStoreSqlite` | `src/storage/IndexedPageStoreSqlite.ts` | SQLite backend Page+Visit CRUD + recordVisit 단일 TX |
| `NoteStore` | `src/storage/NoteStore.ts` | Note CRUD + 3중 anchor + ai_tags JSON |
| `AiChatHistoryStore` | `src/storage/AiChatHistoryStore.ts` | AI 메모 CRUD + role/status + retrieved_items/chat_meta JSON |
| `TagStore` | `src/storage/TagStore.ts` | Tag + PageTag/NoteTag M:N + 6 kind UNIQUE |
| `EmbeddingQueue` | `src/storage/EmbeddingQueue.ts` | 백그라운드 큐 + priority FIFO + claim TX + markSucceeded/Failed |
| `EmbeddingClient` | `src/ai/embedding/EmbeddingClient.ts` | provider.embed wrapper + text-embedding-3-small + BYOK + processNextJob |
| `migrations/v03_to_v04` | `src/storage/migrations/v03_to_v04.ts` | A3 §B 5단계 + 매핑 5종 + revert |
| `src/types/sql-modules.d.ts` | TS 선언 | `.sql?raw` vite import 지원 |

### 13.3 신규 의존성 (M3-1)

- `better-sqlite3@^12.10.0` (11.x 차단 — V8 ABI 비호환)
- `sqlite-vec@^0.1.9` (windows-x64 prebuilt)
- `@electron/rebuild@^4.0.4` (devDep — Electron 39 ABI rebuild)
- `@types/better-sqlite3@^7.6.13` (devDep)
- `npm run rebuild` script 추가

### 13.4 schema 갱신 누적 (M3 종료 시)

| 갱신 | 위치 | 내용 |
|---|---|---|
| §04.3.8 partition_key → partition key | docs/prd/04_data_model.md | M3-spike 정정 (sqlite-vec 0.1.9 실측) |
| §19.5.2 M3-spike + M3-1~M3-7 ✅ | docs/prd/19_migration_v03_v04.md | 모든 M3 PR 완료 entry |
| `.gitignore` `spike/` | 본 디렉토리 | P-002 PoC 산출물 제외 |
| `eslint.config.mjs` `spike/` ignore | 본 디렉토리 | spike 코드 lint 제외 |
| tsconfig.node.json `src/types/**/*.d.ts` | 본 파일 | `.sql?raw` 선언 인식 |

### 13.5 G-NNN 가드레일 활성 (M3 종료 시점)

| G | 상태 | M3 내 적용 |
|---|---|---|
| G-012 v0.4 SSOT | ✅ | 모든 PR 이 PRD §19.5.2 + §04 동기 갱신 |
| G-013 단계별 PR | ✅ | spike → 스키마 → 모듈 → 어댑터 → 마이그레이션 → 일반화 7+1 단계 |
| **G-014** 데이터 마이그레이션 dry-run + 자동 백업 | **✅ (M3-6 본격 활성)** | 백업 위치 / 로그 / revert 단위 테스트 검증 |

### 13.6 M3 핵심 학습 (M4 진입 적용)

1. **본 PR 내 즉시 핫픽스 패턴 유지** — 모든 PR 머지 전 lint/typecheck/test/build 통과 강제, KI 0건 유지
2. **단계별 PR 전략 (G-013) 8단계 검증** — spike (선택) + 스키마/모듈/어댑터/마이그레이션 (필수)
3. **schema 다층 정합** — §04.3 (PRD) ↔ schema/v04.sql ↔ Database.test EXPECTED_TABLES/INDEXES 동기
4. **외부 사실 spec 검증** — sqlite-vec 0.1.9 `partition key` (space) 실측 후 PRD 정정 / OpenAI text-embedding-3-small dimensions=1024
5. **어댑터 모드 보존 명시** — IndexedPageStore (JSON) 보존, M5-8 일괄 제거 contract
6. **CHECK 제약 + CASCADE + 트리거 다층 정합** — workspace DELETE → pages CASCADE → trigger → vec_pages 정리 (단위 테스트 다층 검증)
7. **PRD §19.5.2 entry 즉시 박힘** — 매 PR 머지 시점에 진행률 정합
8. **단위 테스트 회귀 가드** — 매 PR 직전 `npm test` 전체 실행 (회귀 0 강제)

### 13.7 M4 진입 권장 (다음 세션)

| # | 작업 |
|---|---|
| 1 | M3 종합 evaluator 채점 — 8 PR + spike 통합 정합 (D/F/H 카테고리 가중) |
| 2 | M4-4 IndexingGate Privacy 11 패턴 + 비밀번호 필드 — G-004 강화 (사이트 시작 전 필수) |
| 3 | M4-1 IndexingService — did-finish-load hook + recordVisit + EmbeddingQueue.enqueue (M3 자산 활용) |
| 4 | M4-3 DwellTracker — 탭 활성 + focus 시간 누적 |
| 5 | M4-2 AutoTagger — Tag.kind 6종 JSON schema + ProviderAdapter.chat 호출 + TagStore.ensureTag + attachToPage |
| 6 | M4-5 회귀 테스트 — 시나리오 1 자동 인덱싱 + 시나리오 3 Visit 누적 |

### 13.8 잊으면 안 되는 맥락

- **better-sqlite3 11.x 차단**: Electron 39 V8 142 `Context::GetIsolate` 제거 → 12.x 강제 (m3-spike §3.1)
- **sqlite-vec macOS 미검증**: KI 후보 (Sprint 016 또는 macOS CI 추가 시 재검증)
- **JSON IndexedPageStore 보존**: M5-8 일괄 제거 — 어댑터 모드 + 본 모듈 동반 폐기
- **PageResultStore 어댑터 보존**: 동일 (M5-8)
- **PageCachePanel PARTIAL**: M5-8 MemoryStatsPanel 흡수
- **fetch 주입 통일 contract**: M5-8 (M2-7 codex F)
- **UsageLog feature 보존성**: V03Feature 값 마이그레이션 시 매핑 X (감사 로그 보존)
- **G-014 본격 활성**: M3-6 이후 모든 데이터 변경 마이그레이션은 dry-run + 자동 백업 + revert 강제

### 13.9 Sprint 015 진행 통계 (M3 종료 시점)

| 항목 | 수치 |
|---|---|
| Sprint 015 누적 PR (#98~#138) | 40 |
| M0~M2 PR (#98~#129) | 32 |
| M3 PR (#131~#138, spike 포함) | 8 |
| 단위 테스트 (Sprint 014 종료 시점 358 → 본 시점) | 567 (+209) |
| KI 누적 | 0건 (40 PR 연속 즉시 핫픽스로 해소) |
| commit-check CI | 40 PR 모두 success (G-009 NNN 한 분절 11 Sprint 연속) |
| 신규 가드레일 활성 (Sprint 015) | G-012 (M1) / G-013 (M2) / G-014 (M3-6 본격) |
| JS bundle | 305.80 → 305.84 kB (+0.04 / PageCachePanel copy 갱신만) |
| schema/v04.sql | 10 테이블 + 11 인덱스 + 2 vec0 virtual + 2 trigger |
| 신규 모듈 | 11 (Database/VectorIndex/IndexedPageStoreSqlite/NoteStore/AiChatHistoryStore/TagStore/EmbeddingQueue/EmbeddingClient/migrations/schema.sql/sql-modules.d.ts) |
| 신규 의존성 | 4 (better-sqlite3 12.10 / sqlite-vec 0.1.9 / @electron/rebuild 4.0.4 / @types/better-sqlite3 7.6) |

### 13.10 본 세션 진행 흐름 (사용자 명시 결정)

- "핸드오프 읽고 작업 진행해" → M3-spike 진입 결정
- "진행해" (3회) → M3-1 ~ M3-7 자율 진행. 매 PR 본 PR 내 핫픽스 패턴 적용. 모든 PR 머지 → 본 핸드오프 §M3 추가 작성.

### 13.11 마무리 (M3)

Sprint 015 M3 7/7 + spike 완료. v0.4 베이스 인프라 SQLite + sqlite-vec + 8 storage 모듈 + 마이그레이션 + 임베딩 파이프라인 완성. **KI 누적 0건 유지 (40 PR 연속)**. **M4 진입 가능 (M3 종합 evaluator 채점 + 핸드오프 검토 권장)**.

다음 세션에서 **M3 종합 evaluator → M4-4 IndexingGate (Privacy 우선) → M4-1 IndexingService → M4-2/3/5 순차 진입** 권장.
