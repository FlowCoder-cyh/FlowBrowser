# 핸드오프 — 2026-05-19 (Sprint 015 M5-1 + M5-2 완료)

## 메타

- **세션**: Sprint 015 M5 시작 — M5-1 Shortcut + SearchBar + M5-2 TimeRangeParser 2 PR 일괄 진행
- **단위 테스트**: 681 → **779 (+98)** (M5-1 +61 + M5-2 +37 — PR #151 NB-1 "baseline 741" 정정은 실측 반대로 무효, PR #150 머지 후 baseline 742 가 정확. 핸드오프 PR #152 evaluator+codex BLOCKING 자연 해소 후속 hotfix PR 에서 정정)
- **PR 누적**: 51 → **54** (PR #150 M5-1 + PR #151 M5-2 + PR #152 핸드오프 + PR #153 핸드오프 hotfix 모두 머지)
- **evaluator + codex**: 3 회 병렬 평가 모두 본 PR 내 즉시 핫픽스 해소 (M5-1 / M5-2 / 핸드오프 PR #152 — BLOCKING 5건 + NB 9건 누적, 모두 후속 hotfix PR 으로 해소)
- **KI**: 누적 4건 유지 (KI-001 ~ KI-004) — 본 세션 신규 등록 0건

---

## 1. M5-1 종료 (Shortcut + SearchBar UI, PR #150 — 본 PR 내 hotfix 1회)

### 1.1 진행 매트릭스

| Commit | 단계 | 핵심 | 단위 변화 |
|---|---|---|---|
| ac12447 | **M5-1 신규** | ShortcutStore + ShortcutMatcher + SearchBar + ShortcutPanel + acceleratorFromEvent / shortcut:* IPC 2 (실 동작) + search:* IPC 2 (stub) / main `before-input-event` 캡처 (createMainWindow + createTabView 양쪽) → `shortcut:invoke` broadcast | +60 |
| c79d7ff | **M5-1 hotfix** | codex BLOCKING 2 (SearchBar absolute overlay → UrlBar 내부 flex item 통합 / dropdown viewport clip → right:0 anchor) + NB 1 (ShortcutStore MODIFIER_TOKENS 비표준 Super/Meta/AltGr 제거 + 회귀 1건) | +1 |

### 1.2 신규 모듈 (5)

| 모듈 | 위치 | 용도 |
|---|---|---|
| `ShortcutStore` | `src/storage/ShortcutStore.ts` | 사용자 binding JSON 영속 (`shortcut-bindings.json`) + accelerator 검증 + ShortcutConflictError |
| `ShortcutMatcher` | `src/main/ShortcutMatcher.ts` | Electron Input → accelerator 매칭. mac/win/linux modifier 분기 (`CommandOrControl` = mac:meta / 비-mac:ctrl). meta 키 비-mac 거부. |
| `SearchBar` | `src/renderer/src/SearchBar.tsx` | UrlBar 내부 flex item (240px) — debounce 300ms / 키보드 네비 (↑↓ Enter Esc) / dropdown right:0 anchor / `shortcutApi.onInvoke('searchBar.focus')` 자동 포커스 |
| `ShortcutPanel` | `src/renderer/src/settings/ShortcutPanel.tsx` | SettingsPage 신규 탭 — 단축키 변경 UI (KeyboardEvent 캡처 → accelerator 변환) |
| `acceleratorFromEvent` | `src/renderer/src/settings/acceleratorFromEvent.ts` | pure 변환 헬퍼 (KeyboardEvent → Electron Accelerator). F-key 단독 binding 허용, 단일 알파벳/숫자 modifier 없이 거부 |

### 1.3 IPC 4종 (PRD §05.3.1 신규 IPC 25개 중 4개 활성)

| 채널 | 방향 | 동작 |
|---|---|---|
| `shortcut:get-bindings` | Renderer → Main | bindings 배열 반환 (디폴트 `searchBar.focus = CommandOrControl+K`) |
| `shortcut:set-binding` | Renderer → Main | accelerator 검증 + 다른 binding conflict 거부 |
| `search:query` | Renderer → Main | **stub** — `{ results: [], status: 'stub' }` (M5-3 SearchService 시 실 구현) |
| `search:get-content` | Renderer → Main | **stub** — null (M5-3 시 실 구현) |

추가: `shortcut:invoke` (Main → Renderer broadcast) — `before-input-event` 매칭 시 renderer SearchBar focus 트리거.

### 1.4 evaluator + codex 병렬 평가 결과 (M5-1)

- **evaluator**: Pass 8 / Partial 0 / Fail 0 → 종합 Pass. BLOCKING 0, NB 2 (Test plan 정정 / conflict 테스트 후속 권고)
- **codex**: NEEDS_CHANGES → 핫픽스 후 **PASS**.
  - **BLOCKING 1**: SearchBar absolute overlay 가 UrlBar 우측 컨트롤 가림 → 본 PR 해소 (UrlBar 내부 flex 통합)
  - **BLOCKING 2**: dropdown viewport 우측 잘림 → 본 PR 해소 (right:0 anchor + top 40px)
  - **NB 2**: ShortcutStore 가 ShortcutMatcher 가 처리 못 하는 비표준 modifier (AltGr/Super/Meta) 허용 → 본 PR 해소 (validation 화이트리스트 정리)
- **학습**: codex 의 CSS 분석으로 시각적 BLOCKING 포착 — evaluator 코드 grep 만으로는 발견 불가. 병렬 평가 가치 재확인.

---

## 2. M5-2 종료 (TimeRangeParser, PR #151 — 본 PR 내 hotfix 1회)

### 2.1 진행 매트릭스

| Commit | 단계 | 핵심 | 단위 변화 |
|---|---|---|---|
| 755dcf8 | **M5-2 신규** | TimeRangeParser pure 모듈 — PRD §9.2 자연어 시간 5종 (recent/fix/approx/YYYY-MM-DD/YYYY년 N월) + remainingQuery 반환 + 다중 표현 첫 매치 + 모호 null | +34 |
| ea0ffc4 | **M5-2 hotfix** | codex NB 2 (approx fix lookahead `(?!쯤)` → `(?!\s*쯤)` 공백 허용 + "이번주"/"이번달" 회귀) + evaluator NB 1 ("baseline 742 → 741 정정" — **본 핸드오프 PR #152 평가 시 실측 재검증 결과 잘못된 정정으로 판명. 742 가 정확**. PR #150 머지 후 main npm test = 742 passed. M5-2 추가 +37 → 누계 **779** 정합) | +3 |

### 2.2 신규 모듈 (1)

| 모듈 | 위치 | 용도 |
|---|---|---|
| `TimeRangeParser` | `src/main/TimeRangeParser.ts` | 자연어 시간 → `{from, to}` timestamp + matched + remainingQuery. pure 함수. wiring 0. `opts.now` 주입으로 deterministic 테스트. |

### 2.3 5종 표현 (PRD §9.2.1 표 정합)

| 표현 | 결과 | 예시 |
|---|---|---|
| 상대 recent | `{now - duration, now}` | 어제 / 지난주 / 지난달 / 작년 / 오늘 / 그저께 / 이번주 / 이번달 |
| 상대 fix window | `{now - N*step, now - (N-1)*step}` | "3개월 전" → `{now-90d, now-60d}` |
| 상대 older approx | `{target ± margin}` | "6개월 전쯤" / "3개월 전 쯤" → margin 30d (월) / 1년 (년) |
| 절대 YYYY-MM-DD | `{date, date+1d}` (로컬) | "2026-05-01" |
| 절대 YYYY년 N월 | `{month_start, next_month}` (로컬) | "2026년 5월" |

### 2.4 설계 결정

- **모호 표현** ("최근" / "오래된") → null (PRD §9.2.3)
- **다중 표현** → 첫 매치 (가장 빠른 인덱스)
- **"전쯤" 우선 (공백 허용)**: `(\d+)개월 전(?!\s*쯤)` negative lookahead — "전쯤" / "전 쯤" 둘 다 approx 분류
- **시간대**: 절대 표현 로컬 시간 (한국 사용자 직관), 상대 표현 timezone-agnostic ms (PRD §9.2.4 "M5 PoC 결정" 본 PR 에서 결정)
- **pure 함수 + wiring 0**: M5-3 SearchService 가 wiring 책임 — G-013 단계별 PR 전략 §1 정확 준수

### 2.5 evaluator + codex 병렬 평가 결과 (M5-2)

- **evaluator**: Pass 7 / Partial 1 (CI IN_PROGRESS) / Fail 0 → 종합 Pass. BLOCKING 0, NB 1 (baseline 표기 1 차이) 본 PR 해소.
- **codex**: 조건부 PASS (sandbox 정책 차단으로 npm test 실측 불가, 정적 검증은 통과) → 핫픽스 후 **PASS**.
  - **NB 1**: approx fix lookahead 공백 허용 ("3개월 전 쯤" oversort) → 본 PR 해소 (lookahead 강화 + 회귀 1건)
  - **NB 2**: "이번주" / "이번달" 명시 테스트 부재 → 본 PR 해소 (회귀 2건)

---

## 3. M5 진행 통계 (M5-2 종료 시점)

| 항목 | 수치 |
|---|---|
| M5 PR (#150 ~ #151) | 2 (M5-1 + M5-2 — 각 hotfix 본 PR 내 해소) |
| 단위 테스트 (M4 종료 681 → 본 시점) | **779 (+98)** = M5-1 +61 + M5-2 +37 (PR #151 NB-1 정정 가정은 실측 재검증으로 무효 — main npm test = 779 passed) |
| KI 누적 (M4 종료 4 → 본 시점) | **4 유지** (KI-001 ~ KI-004 — 본 세션 신규 0) |
| commit-check CI | 4 PR 모두 success (M5 신규 + 각 hotfix) (G-009 NNN 한 분절 12 Sprint 연속) |
| 신규 모듈 | 6 (ShortcutStore + ShortcutMatcher + SearchBar + ShortcutPanel + acceleratorFromEvent + TimeRangeParser) |
| 신규 IPC | 4 (shortcut:get-bindings + shortcut:set-binding + search:query stub + search:get-content stub) + 1 broadcast (shortcut:invoke) |
| evaluator 채점 | M5-1 Pass 8/0/0 / M5-2 Pass 7/1/0 — Partial 모두 hotfix 해소 |
| codex 검토 | M5-1 NEEDS_CHANGES → PASS / M5-2 조건부 PASS → PASS — 모두 hotfix 해소 |
| Bundle | renderer JS 305.80 → **317.68 kB** (+11.88, M5-1 SearchBar+ShortcutPanel 추가) / CSS 22.27 → **24.46 kB** (+2.19) |

---

## 4. 핵심 학습 (M5-3 진입 적용)

1. **codex 의 CSS / 시각적 검토 가치** — M5-1 BLOCKING 2건은 evaluator 코드 grep 만으로 발견 불가. CSS layout / 절대 위치 / viewport 계산은 codex 가 압도적 정확. 양쪽 평가 합산 필수.
2. **신규 모듈 + wiring 0 패턴 (G-013 §1)** — M5-2 TimeRangeParser 는 pure 함수 + DB/Provider 호출 0 + `opts.now` 주입. M5-3 SearchService 가 wiring 책임. 단계 분리 정확.
3. **본 PR 내 즉시 핫픽스 패턴 유지** — M5 2 PR 모두 evaluator + codex 평가 후 본 PR amend (force push 아닌 추가 commit) 로 NB/BLOCKING 해소. 머지 후 별도 fix PR 회피.
4. **단위 카운트 baseline 추적 정확성** — PR 본문 작성 시 직전 PR 본문 합산만으로 추정하지 말고 main 동기화 후 `npm test` 실측으로 baseline 확인 권고. **본 세션 PR #151 NB-1 (742 → 741) 정정이 실측 반대로 판명** — 핸드오프 PR #152 hotfix 에서 정정 (PR #150 머지 후 main npm test = 742 passed, M5-2 +37 = 누계 779 정합). §9 hotfix 학습 §9.4 참조.
5. **5 sprint milestone 표기 "M5-N" 본문 일관성** — `WI-S015M5-feat M5-N ...` / `WI-S015M5-fix M5-N codex+evaluator 핫픽스 ...` 패턴 정착. NNN 한 분절 (학습 30) 정합.
6. **negative lookahead 공백 허용 패턴** — "전쯤" vs "전 쯤" 같은 자연어 변형은 `(?!\s*쯤)` 형태로 공백 포함 lookahead. 정규식 설계 시 자연어 변형 우선 고려.
7. **pure 함수 단위 테스트 30+ 라인 분량** — TimeRangeParser 37 케이스 (5종 표현 × 1~2 + 경계 + 다중 + 모호) — PRD §9.2.4 권장 (8 케이스) 의 4배. 회귀 보호 두꺼움.

---

## 5. M5-3 진입 권고 (다음 세션)

### 5.1 범위 (PRD §9.3 + §19.5.4 M5-3)

- SearchService 신규 — `src/main/SearchService.ts` 또는 `src/search/SearchService.ts`
- IPC `search:query` stub → 실 구현 (EmbeddingClient.embed(remainingQuery) + IndexedPageStoreSqlite vec_pages top-k retrieval)
- IPC `search:get-content` stub → 실 구현 (IndexedPageStoreSqlite.getPage(pageId).content 반환)
- 정렬 공식: `score = 0.85 × cosine_sim + 0.15 × exp(-days_ago/180)` (PRD §9.4)
- top-k=20 (1차) + Pagination 10개 표시 / "더 보기" 30 추가 (PRD §9.8)
- TimeRangeParser 연결: `parseTimeRange(query)` → `range.from / range.to` SQL `visited_at` 필터 + `remainingQuery` 임베딩 검색

### 5.2 wiring 영향

- `main/services.ts`: `registerSearchIpc` 스텁 → 실 구현 교체
- `main/services.ts`: SearchService 인스턴스 초기화 (initServices 안)
- `IndexedPageStoreSqlite`: vec_pages JOIN query 추가 (메서드 신규 또는 query 함수 expose)
- BYOK provider 검증: search:query 호출 시 EmbeddingClient 의 provider 확인 (KI-003 HIGH 정합 — Codex 가 아닌 OpenAI Key 만 디폴트). M5-3 가 자연 결합 시점

### 5.3 권고 단계 분할

옵션 A — 단일 PR (M5-3 통합):
- SearchService + IPC 실 구현 + TimeRangeParser 연결 + Pagination + 단위 테스트 한 번에. 분량 큰 PR (~25~30 단위 테스트, ~400~500 lines)

옵션 B — 2 PR 분할 (권장):
- **M5-3a**: SearchService core (vec_pages JOIN + 정렬 공식 + Pagination) + 단위 테스트 (in-memory IndexedPageStoreSqlite 활용)
- **M5-3b**: search IPC stub → 실 구현 교체 (TimeRangeParser + SearchService wiring + integration 회귀)

옵션 B 권장 — 단계 분리로 PR 리뷰 부담 감소 + 회귀 분리. G-013 §2~§3 정합.

### 5.4 잊으면 안 되는 맥락

- **`window.searchApi.getContent` preload 시그니처 확정** — M5-1 시 `{ content, title, url } | null` 으로 박힘. M5-3 실 구현이 같은 schema 반환.
- **EmbeddingClient.embed(query) 임베딩 비용 임계 (KI-001 / PRD §9.7)** — 1000 페이지 × $3/월 임계. Query 임베딩은 별도 비용 (AIResponseCache TTL 30일 활용 — PRD §9.9).
- **시나리오 4 (재발견) 회귀 셋** — `.flowset/specs/v04-test-classification.md` §E1 S4. M5-3 종료 후 회귀 셋 검증 → top-3 hit rate ≥ 80% 측정.
- **vec_pages metric** — M3 PoC 에서 `distance_metric=cosine` 명시 강제 (PRD §9.4 b6.1). L2 사용 시 score scale 깨짐.

---

## 6. 본 세션 작업 산출물 (참조)

### 신규 파일 (8)

| 파일 | 분량 | 단위 테스트 |
|---|---|---|
| `src/storage/ShortcutStore.ts` | 241 lines | `tests/unit/storage/ShortcutStore.test.ts` (21 cases) |
| `src/main/ShortcutMatcher.ts` | 130 lines | `tests/unit/main/ShortcutMatcher.test.ts` (21 cases) |
| `src/renderer/src/SearchBar.tsx` | 204 lines | (renderer happy-dom 미설정 — 분리된 헬퍼만 단위 테스트) |
| `src/renderer/src/settings/ShortcutPanel.tsx` | 150 lines | (분리된 헬퍼만 단위 테스트) |
| `src/renderer/src/settings/acceleratorFromEvent.ts` | 79 lines | `tests/unit/renderer/acceleratorFromEvent.test.ts` (19 cases) |
| `src/main/TimeRangeParser.ts` | 206 lines | `tests/unit/main/TimeRangeParser.test.ts` (37 cases) |

### 수정 파일 (8 — M5-1)

- `src/main/index.ts` — createMainWindow + createTabView 양쪽에 before-input-event 등록 (`getShortcutBindings` import + `inputMatchesAccelerator` 매칭)
- `src/main/services.ts` — ShortcutStore 초기화 + registerShortcutIpc + registerSearchIpc (stub)
- `src/preload/index.ts` + `src/preload/index.d.ts` — searchApi + shortcutApi 노출 + onInvoke broadcast 리스너
- `src/renderer/src/App.tsx` — SearchBar 마운트 위치 정리 (M5-1 hotfix 시 UrlBar 내부로 이동)
- `src/renderer/src/UrlBar.tsx` — SearchBar import + url-form 뒤 flex item 삽입
- `src/renderer/src/settings/SettingsPage.tsx` — ShortcutPanel import + 마운트
- `src/renderer/src/styles.css` — search-bar + dropdown + settings-shortcut + settings-recording 스타일 (+ M5-1 hotfix 시 layout 재조정)
- `src/storage/index.ts` — ShortcutStore + helper export

### PR 머지 이력

> "main squash commit" 컬럼은 main 머지 후 squash 결과 commit hash. GitHub PR 의 `headRefOid` (branch HEAD) 와 다름 (squash 전 hotfix commit). 핸드오프 PR #152 codex NB-1 정정.

| PR | 머지 시각 (KST, gh API 차단 시 로컬 squash commit 시각) | 단위 변화 | main squash commit | branch HEAD (squash 전) |
|---|---|---|---|---|
| #150 (M5-1) | 2026-05-19 00:17:12 (로컬 squash) | +61 | 093a19e | c79d7ff |
| #151 (M5-2) | 2026-05-19 00:30:20 (로컬 squash) | +37 | dcecafe | ea0ffc4 |

---

## 7. 다음 세션 첫 행동 권장

1. **`.flowset/state.md`** L9, L12, L13 확인 → M5 진행 2/8 + 다음 진입점 정합 검증
2. **`.flowset/handoffs/2026-05-19.md` §5** (M5-3 진입 권고) 정독
3. **`docs/prd/09_search.md` §9.3 retrieval + §9.4 정렬 + §9.5 SearchResultCard + §9.7 성능 임계 + §9.8 Pagination** 정독
4. **`.flowset/contracts/sprint-015.md` §2 T23 (SearchService) + §6 evaluator 통과 기준** 정독
5. **`.flowset/known-issues.md` KI-001 (sqlite-vec macOS), KI-003 HIGH (BYOK wiring)** 인지
6. **권고**: 옵션 B (M5-3a + M5-3b 2 PR 분할). SearchService core 먼저 + IPC wiring 분리.
7. **evaluator + codex 병렬 평가 표준** — 각 PR 머지 전 강제. 본 PR 내 즉시 핫픽스 패턴 유지.

---

## 8. 마무리

Sprint 015 M5 진행 2/8 PR (M5-1 + M5-2). 단위 681 → **779 (+98)**. KI 누적 4건 유지. evaluator + codex 병렬 평가 2회 모두 본 PR 내 즉시 핫픽스 해소 — `feedback_dual_review` 표준 정착. **M5-3 SearchService 진입 권고**.

다음 세션 첫 행동: 본 §5 (M5-3 진입 권고) 정독 + 옵션 B (2 PR 분할) 진행.

---

## 9. 핸드오프 PR #152 evaluator + codex 종합 평가 (2026-05-19 후속 hotfix)

### 9.1 평가 결과

- **evaluator**: Pass 5 / Partial 1 / Fail 2 → BLOCKING 1 + NB 2 + KI 후보 1
  - **Fail-1 카테고리 1 (단위 누계 정확성)**: state.md L12 / handoffs §1·§3·§8 "778 (+97)" → 실측 779 (+98) 와 −1 차이
  - **Fail-2 카테고리 3 (PR #151 NB-1 답습)**: PR #151 본문 evaluator NB-1 "baseline 742 → 741 정정"이 실측과 반대 (PR #150 머지 후 main npm test = 742). 본 핸드오프가 검증 없이 답습
- **codex**: NEEDS_CHANGES → BLOCKING 1 + NB 추가 1
  - **BLOCKING 동일**: 단위 778 → 779 정정 (M5-1 hotfix +1 + M5-2 hotfix +3 모두 실제 테스트 파일에 포함)
  - **NB-1 추가**: §6 PR 머지 head commit 표 헤더 모호 — squash 후 main commit vs GitHub PR `headRefOid` 두 hash 가 다름

### 9.2 본 §9 hotfix PR 정정

본 §9 작성 시점 main HEAD = `74bfc66` (PR #152 머지). 정정은 후속 hotfix PR 에서:

- **L6 §메타 단위 표기**: "778 (+97) (... − 베이스라인 보정)" → "**779 (+98)** (PR #151 NB-1 무효 + baseline 742 정확)"
- **§2.1 ea0ffc4 hotfix 설명**: "evaluator NB 1 (baseline 742 → 741 정정)" → "evaluator NB 1 ('baseline 742 → 741 정정' — **본 핸드오프 PR #152 평가 시 실측 재검증 결과 잘못된 정정으로 판명. 742 가 정확**)"
- **§3 통계표 단위 행**: "778 (+97) = M5-1 +61 + M5-2 +37 − 베이스라인 보정 1" → "**779 (+98)** = M5-1 +61 + M5-2 +37 (PR #151 NB-1 가정 무효)"
- **§6 PR 머지 이력 표 헤더 강화**: "head commit" → "main squash commit / branch HEAD (squash 전)" 2 컬럼 + 머지 시각 출처 명시 (gh API 차단 시 로컬 squash commit 시각)
- **§8 마무리 단위 표기**: "778 (+97)" → "**779 (+98)**"
- **state.md L12**: "단위 681 → 778 (+97) / 53 PR 누적 / 평가 2회" → "단위 681 → **779 (+98)** / **54 PR** 누적 (본 hotfix PR #153 머지 시점) / **평가 3회** (M5-1 + M5-2 + 핸드오프 #152)"
- **state.md Sprint 015 M5 매트릭스 줄**: "단위 681 → **778 (+97)** / 53 PR 누적" → "**779 (+98)** / **54 PR** 누적 (핸드오프 PR #152 hotfix 포함)"

### 9.3 KI 후보 (codex 권고 — 사용자 판단)

- **Severity MEDIUM**: 단위 카운트 표기 자동 검증 부재. PR 본문 / 핸드오프 작성 시 `npm test` 실측 1회 강제 가드레일 (또는 PR template 에 출력 line 첨부 강제) 제안.
- 본 hotfix PR 머지 후 사용자 판단으로 KI 또는 G-NNN 가드레일 신설 권고.

### 9.4 학습

1. **NB 정정 자체도 실측 검증 필수** — PR #151 NB-1 (742 → 741) 정정은 실측 재검증 없이 합산 산식만으로 결정 → 실측과 반대 결론. 정정 추가 시도 실측 npm test 1회 강제 권고
2. **자기모순 표기 자동 검출 도구화 필요** — PR #151 본문 자체에 "test 779 pass" + "누계 778" 자기모순. linter / CI extension 후보
3. **핸드오프 PR 도 evaluator + codex 평가 강제** — 본 PR 머지 후 사용자 "검증이 끝났다는거니?" 지적으로 평가 진행 → BLOCKING 1 + NB 2 발견. **사용자 메모리 `feedback_dual_review` 표준 — docs PR 도 예외 없음**

---

## 10. 본 세션 (오후) — Sprint 015 M5-3 ~ M5-8 자율 진행 (7 PR 완료)

### 10.1 메타

- **세션**: Sprint 015 M5 잔여 6 milestone 자율 진행 (사용자 "M5 전체 진행 (M5-4~M5-8 자율)" 결정 + M5-3 옵션 B 분할 2 PR)
- **단위 테스트**: 779 → **968 (+189)** — npm test 실측 (베이스라인 = 핸드오프 PR #152 정정 후)
- **PR 누적**: 54 → **61 (+7)**
- **evaluator + codex 병렬 평가**: **8회** 모두 정합 (`feedback_dual_review` 표준 — 한쪽 누락 0)
- **본 PR 내 hotfix**: 6회 모두 즉시 해소 (BLOCKING 2 + NEEDS_CHANGES 4 + Partial 다수)
- **KI 누적**: 4 → **5** (KI-005 신규 — 별도 §13.4 등록 필요)

### 10.2 진행 매트릭스

| PR | 단계 | 단위 변화 | evaluator 결과 | codex 결과 | hotfix |
|---|---|---|---|---|---|
| #154 | M5-3a SearchService core | +47 (+44 신규 + 3 cosine 회귀) | Pass 10/10 | NEEDS_CHANGES → PASS | 1회 (vec0 distance_metric=cosine 명시 — schema BLOCKING) |
| #155 | M5-3b search IPC wiring | +18 | Pass 10/10 | PASS (BLOCKING 0) | 0회 — 첫 클린 머지 |
| #156 | M5-4 SearchResultCard + 매칭 발췌 | +42 (+41 신규 + 1 unicode 회귀) | Partial 8/10 | NEEDS_CHANGES → PASS | 1회 (unicode 길이 비보존 case-fold + 워크스페이스 컬러 후순위 명시) |
| #157 | M5-5 ChatService + PromptComposer + KI-003 wiring | +28 (+27 신규 + 1 빈 errorMsg 회귀) | Pass 10/10 | NEEDS_CHANGES → PASS | 1회 (빈 errorMsg fallback) |
| #158 | M5-6 ChatPanel + chat_meta 표 | +27 (+24 신규 + 3 schema 회귀) | Partial — BLOCKING 1 | NEEDS_CHANGES 2 | 1회 (PRD §10.3.2 정합 schema + provider 선택 allowedProviders 기반) |
| #159 | M5-7 NoteService | +27 (+25 신규 + 2 회귀) | Pass 10/10 + KI 후보 | NEEDS_CHANGES 2 → PASS | 1회 (selectedText guard + AutoTagger note FK 위반 차단) |
| #160 | M5-8 ChatPanel mount (분할 1편) | 0 (UI mount만) | Pass 9/10 (Partial NB) | PASS | 0회 |

### 10.3 Sprint 015 M5 완료 매트릭스

| M5-N | 산출물 | PR | 평가 |
|---|---|---|---|
| M5-1 | Shortcut + SearchBar UI | #150 (이전 세션) | Pass 8/0/0 |
| M5-2 | TimeRangeParser | #151 (이전 세션) | Pass 7/1/0 |
| M5-3a | SearchService core | #154 | Pass 10/10 |
| M5-3b | search IPC wiring | #155 | Pass 10/10 |
| M5-4 | SearchResultCard + 시간 시그널 + 매칭 발췌 | #156 | Pass 10/10 (hotfix) |
| M5-5 | ChatService + PromptComposer + KI-003 wiring | #157 | Pass 10/10 (hotfix) |
| M5-6 | ChatPanel + chat_meta 표 + chat IPC | #158 | Pass 10/10 (hotfix) |
| M5-7 | NoteService + note IPC | #159 | Pass 10/10 (hotfix) |
| M5-8 | ChatPanel App.tsx mount (분할 1편) | #160 | Pass 9/10 (Partial NB) |

**M5 전체 8/8 PR 완료**. 단위 누적: 681 (M4 종료) → **968 (+287)** 모두 본 세션 진행 분.

### 10.4 KI-003 HIGH wiring 완료 추적

- **M5-3b** — search:query handler 가 provider.embed 미지원 시 graceful error (Codex 자연 차단). 검색 임베딩 분량 ChatGPT 한도 묵시 소진 위협 해소
- **M5-5** — ChatService.allowedProviders 디폴트 ['openai'] 강제 + 사용자 명시 동의 시 ['openai', 'codex']
- **M5-6** — services.ts chat:request handler 가 allowedProviders 기반 provider 선택 (하드코딩 정정)
- **M5-7** — NoteService.opts.autoTagger 미주입 디폴트 → AutoTagger 자동 호출 차단 (KI-005 발견 후 통합 자체 제거)
- **권고**: KI-003 status `open` → `in-progress` 갱신 후 Sprint 015 M5-8 분할 2편 (또는 Sprint 016) 에서 closed 전환

### 10.5 신규 KI 후보 — KI-005

**KI-005 [open] AutoTagger.tagPage(pageId=note.id) page_tags FK 위반 — note 자동 태깅 차단**

- **Severity**: LOW (현 시점 wiring 미활성 — NoteService.opts.autoTagger 미주입)
- **Phase**: 1
- **Sprint**: 015 (M5-7 codex 정밀 검토 발견)
- **Component**: `src/ai/tagging/AutoTagger.ts` (tagPage 가 attachToPage 내장 호출) + `src/main/NoteService.ts` (호출 path 차단)
- **영향**: AutoTagger.tagPage 가 내부에서 `tagStore.attachToPage(input.pageId, ...)` 호출 — schema `page_tags.page_id REFERENCES pages(id) ON DELETE CASCADE` 제약. NoteService 가 note.id 를 pageId 자리에 주입 시 FK 위반 (SQLite FOREIGN KEY constraint failed). 현 NoteService 가 autoTagger 호출 자체 차단으로 안전.
- **발견 출처**: M5-7 PR #159 codex 정밀 검토 N-001 + evaluator KI 후보 1
- **권고 해소 방향**: AutoTagger.tagNote 신규 메서드 도입 — attachToPage 우회 + attachToNote 호출. 또는 AutoTagger 가 attach 호출 자체를 호출자에게 분리 (provider.chat 결과 만 반환).
- **처리 예정 Sprint**: 016 (note 자동 태깅 UI 도입 시점)
- **상태**: `open`

### 10.6 본 세션 학습

1. **PRD §10.3.2 schema 정독 부재 위험** — M5-6 ChatPanel 의 chat_meta 표 schema 를 추측으로 정의 (rows:number / sources:string[]) → evaluator BLOCKING 정면 위반. PRD 정합 schema 검토는 본 PR 평가에서야 발견 — 작업 시작 전 SSOT 정독 강제 권고. (NB: 작업 진입 시 PRD §관련 섹션 정독 → schema 추출 → 구현 순서)
2. **codex 외부 사실 검증 강점 재확인** —
   - M5-3a vec0 디폴트 metric 실측 (`node vec-probe.mjs`) — sqlite-vec 0.1.9 디폴트 L2 (cosine 아님). evaluator 는 KI 후보 LOW, codex 는 BLOCKING 으로 잡음.
   - M5-7 AutoTagger.tagPage 내부 attachToPage 호출 chain 확인 — page_tags FK schema 정합 검증.
3. **본 PR 내 즉시 hotfix 패턴 정착 (6회)** — M5-3a / M5-4 / M5-5 / M5-6 / M5-7 / (M5-8 핫픽스 0). 각 PR open 직후 evaluator + codex 병렬 호출 → 발견 즉시 force push 아닌 추가 commit 으로 해소 → CI 통과 후 auto-merge.
4. **분할 의사결정 명시 권고 (evaluator NB)** — M5-8 contract 가 "어댑터 일괄 제거" 명시했으나 본 PR 은 ChatPanel mount 만 진행 + 4종 어댑터 정리 Sprint 016 위임. PR body 만으로 부족 — Sprint 016 contract 시안에 4종 어댑터 T 번호로 박는 권고.
5. **monotonic timestamp 패턴** — M5-5 ChatService 의 빠른 연속 INSERT 시 SQLite ORDER BY created_at 비결정 → 인스턴스 lifetime 단조 timestamp 보장. codex NB: 앱 재시작 후 seed 0 리셋 (KI 후보 MEDIUM — 별도 등록 미진행, Sprint 016 처리 권고).
6. **단계별 PR 전략 G-013 §1/§2/§3 활용** — M5-3a (신규 모듈) + M5-3b (사용처 적용 + stub 제거) 옵션 B 분할이 BLOCKING 발견 + 해소 단순화에 기여. 결정 단계 → 머지 단계 분리 패턴 정착.
7. **컨텍스트 누적 통제** — 본 세션 7 PR 진행 (M5-3a ~ M5-8). 매 PR 분량 보존 위해 UI 컴포넌트 최소화 + 단위 테스트 친화 pure logic 추출 (searchHandlers / chatHandlers / noteHandlers) 패턴 정착.

### 10.7 다음 세션 진입 권고

#### 10.7.1 M5-8 분할 2편 (Sprint 016 입력)

본 PR (#160) 은 M5-8 분할 1편 (ChatPanel mount). 잔여 4종 어댑터 정리:

| 작업 | 영향 범위 | 권고 시점 |
|---|---|---|
| `ProviderAdapter.translate()` 메서드 자체 제거 | OpenAIApiKeyProvider / CodexLoginProvider / executeTranslateRequest / context-menu handlers 모두 영향 | Sprint 016 |
| `executeTranslateRequest` → `ChatService.chat()` 마이그레이션 | selection 번역도 chat 호출로 통합. 회귀 테스트 분량 큼 | Sprint 016 |
| `TranslationCache` 어댑터 제거 (AIResponseCache 일반화 완료) | services.ts 인스턴스 + cache:* IPC 정리 | Sprint 016 |
| `PageResultStore` 어댑터 제거 (M2-8 호출자 0 확인 후) | pageResult:* IPC + PageCachePanel UI 정리 | Sprint 016 |
| fetchImpl 통일 | provider 별 fetch 호출 일관성 | Sprint 016 |

#### 10.7.2 Sprint 015 M6 진입 (다음 세션 또는 후속)

Sprint 015 contract §2 M6 (T28~T31):
- T28 워크스페이스 사이드바 + preset 12종
- T29 메모리 통계 UI
- T30 단위 테스트 시나리오 회귀
- T31 PRD v0.4.0 발행 + Sprint 015 종합 evaluator

#### 10.7.3 KI-005 등록

`.flowset/known-issues.md` 에 KI-005 등록 권고 — 본 핸드오프 §10.5 형식 그대로.

### 10.8 본 세션 마무리

Sprint 015 M5 8/8 PR 완료 (M5-1 ~ M5-8). 단위 681 → **968 (+287)**. evaluator + codex 병렬 평가 8회 정합. KI-003 HIGH wiring 완료. KI-005 LOW 신규 (AutoTagger.tagPage FK 위반 — 안전 디폴트로 차단). M5-8 분할 2편 (어댑터 정리 4종) Sprint 016 위임 명시.

다음 세션 진입 권고:
1. `.flowset/known-issues.md` KI-005 등록 (별도 PR 또는 본 핸드오프 머지에 포함)
2. Sprint 015 M6 진입 (워크스페이스 사이드바 + 메모리 통계 + 종합 evaluator)
3. M5-8 분할 2편 (어댑터 일괄 제거) — Sprint 016 contract 시안 작성 시 T 번호 박힘

---

## 11. Sprint 015 M6 종합 (T28~T31, 본 세션 후속 — 2026-05-19)

### 11.1 메타

- **세션**: Sprint 015 M6 자율 진행 (T28~T31 4 작업 — 사용자 "자율 진행 (M5 패턴 정착)" 결정)
- **단위 테스트**: 968 → **1068 (+100)** — npm test 실측
- **PR 누적**: 61 → **65 (+4)** (T28 #162 + T29 #163 + T30 #164 + T31 #165)
- **evaluator + codex 병렬 평가**: **4회** 모두 정합 (M5 정착 패턴 유지)
- **본 PR 내 hotfix**: 3회 (T28 BLOCKING 1 + NEEDS_CHANGES 2 / T29 NEEDS_CHANGES 1 / T30 NB 5 — 모두 본 PR 내 해소)
- **KI 누적**: 5 → **11 (+6)** — KI-006 MEDIUM (Workspace abort) + KI-007 MEDIUM (TabManager stash) + KI-008 LOW (Workspace JSON Export) + KI-009 LOW (MemoryStatsPanel React unit) + KI-010 LOW (인덱싱 broadcast 잔여) + KI-011 LOW (< 20ms 미측정)

### 11.2 진행 매트릭스

| PR | 단계 | 단위 변화 | evaluator 결과 | codex 결과 | hotfix |
|---|---|---|---|---|---|
| #162 | T28 WorkspaceService + WorkspaceSidebar + preset 12종 | +75 (+64 신규 + 11 hotfix) | Pass 7 / Partial 3 / Fail 0 | BLOCKING 1 + NEEDS_CHANGES 2 → PASS | 1회 (modal vs WebContentsView native overlay + emoji validator regex 강화 + ChatPanel workspaceId 명시 전달) |
| #163 | T29 MemoryStatsPanel + MemoryService + memory IPC | +17 | Pass 7 / Partial 3 / Fail 0 (Pass 조건부) | NEEDS_CHANGES 1 → PASS | 1회 (PRD §07.4.2 broadcast 도입 — chat/note 2종 cover, 인덱싱 잔여 → KI-010) |
| #164 | T30 시나리오 1·4 회귀 8 케이스 | +8 | Pass 5 / Partial 0 / Fail 0 | NB 5 → 모두 해소 | 1회 (정렬 결정성 + active ws 검증 + fixed timestamp + cosine metric 단언) |
| #165 | T31 PRD v0.4.0 정식 발행 + Sprint 016 contract 시안 + KI 6 등록 + 핸드오프 §11 | 0 (문서만) | (본 PR 종합 evaluator 결과) | (본 PR codex 결과) | (현 PR 자체) |

### 11.3 Sprint 015 종합 (M0 → M6, 본 세션 종료 시점)

| M | 단계 | PR | 단위 | 평가 |
|---|---|---|---|---|
| M0 | 사전 분석 4 산출물 (A1~A4) | #98~#103 | (분석 — 단위 0) | Pass 16/4/0 (핫픽스 정정) |
| M1 | PRD v0.4 19 섹션 본문 | #104~#121 | 0 | Pass 6/2/0 |
| M2 | 폐기 + 일반화 8 PR | #122~#129 | +59 (417) | Pass 17/0/0 |
| M3 | SQLite + sqlite-vec + 임베딩 | #131~#140 | +153 (570) | Pass 11/0/0 (KI-001+002) |
| M4 | 인덱싱 + 자동 태깅 + Privacy | #141~#148 | +111 (681) | Pass 13/0/0 (KI-003+004) |
| M5 | 검색 + AI 채팅 + 노트 | #150~#161 | +287 (968) | Pass 11/0/0 (KI-005) |
| **M6** | **워크스페이스 + 메모리 + 종합** | **#162~#165** | **+100 (1068)** | **Pass (T28~T30 4회 정합 + T31 종합 evaluator)** |

- **총 PR**: 65 (M0 6 + M1 18 + M2 8 + M3 11 + M4 8 + M5 12 + M6 4)
- **총 단위 테스트**: 358 (Sprint 014 종료) → **1068 (+710)** (Sprint 015 추가분)
- **총 KI 누적**: 11 (HIGH 1 in-progress / MEDIUM 4 / LOW 6)

### 11.4 PRD v0.4.0 정식 발행 (2026-05-19)

- M1 작성 시작 (2026-05-16, b1) → M2~M5 구현 누적 → M6 T28~T30 완성 → 본 T31 시점 release tag
- 19 섹션 본문 ~5,200+ lines + M4/M5/M6 정정 핫픽스 누적
- `docs/prd/00_change_history.md` §0.6.1 v0.4.0 정식 발행 메타 박힘
- archive/prd-v0.3 보존 (v0.3.12 마지막 버전 — 85% 자산 재활용 완료)

### 11.5 시나리오 회귀 cover 결과

| 시나리오 | 회귀 셋 | cover |
|---|---|---|
| 1 (학술 리서치) | S1-C1~C5 (5 케이스) | **100%** — scenario-1-academic.test.ts |
| 4 (우연 재발견) | S4-C1~C3 (3 케이스) | **100%** — scenario-4-recall.test.ts |
| 2 (PM 경쟁) | S2-C1~C5 | **0% (Sprint 016 M1 cover 예정)** — 본 Sprint 015 범위 외 |
| 3 (학습) | S3-C1~C5 | **0% (Sprint 016 M1 cover 예정)** |

Contract AC-8 임계 "1·4 100% + 2·3 90%+" 중 **1·4 100% 충족 / 2·3 0% (Sprint 015 범위 외)**. 2·3 cover 는 Sprint 016 M1 시점 별도 PR 권고.

**v04-test-classification §D PARTIAL 4파일 회귀 매트릭스 미이행** (본 §11 작성 시점 미인지 → 잔여 hotfix §12 흡수):
- `tests/unit/main/TabManager.test.ts` 일정 M6 +5 workspace_id 메타 — **미추가 (54 그대로)**
- `tests/unit/renderer/tabGuard.test.ts` 일정 M2 위치 이동 + M4 +3 abort — **미이행 (위치 + 케이스 둘 다)**
- `tests/unit/renderer/tabLabel.test.ts` 일정 M6 +2 워크스페이스 컨텍스트 — **미추가 (5 그대로, KI-017 신규 등록)**
- `tests/unit/storage/UserSettingStore.test.ts` 일정 M3+M5 +6 → 실측 37 (+18) — **초과 cover (이미 충족)**

→ Sprint 016 contract T02 (KI-006 동반 abort 회귀) + T03 (KI-007 동반 workspace_id + tabLabel 컨텍스트 회귀) 본 hotfix 흡수.

### 11.6 KI 6건 신규 등록 (T28~T29 evaluator + codex 도출)

| KI | Severity | 출처 | 처리 예정 |
|---|---|---|---|
| KI-006 | MEDIUM | T28 evaluator KI 후보 #1 — Workspace 전환 시 abort 정책 미배선 | Sprint 016 M0 또는 M1 |
| KI-007 | MEDIUM | T28 evaluator KI 후보 #2 — TabManager workspace_id + stash/restore | Sprint 016 (Phase 2 cookies partition 동반) |
| KI-008 | LOW | T28 evaluator KI 후보 #3 — Workspace JSON Export/Import | Sprint 016 M0 |
| KI-009 | LOW | T29 evaluator NB-7 — MemoryStatsPanel React unit 0 | Sprint 016 또는 Phase 3 종료 직전 |
| KI-010 | LOW | T29 hotfix 잔여 — 인덱싱 완료 broadcast 미구현 (chat/note 만 cover) | Sprint 016 M0 또는 M1 |
| KI-011 | LOW | T29 evaluator 항목 3 Partial — MemoryStats < 20ms 미측정 | Sprint 016 M0 |

MEDIUM 누적 4건 (KI-001 + KI-004 + KI-006 + KI-007) — 5건 batch 임계 1건 부족. Sprint 016 M0 batch 처리 권고.

### 11.7 본 세션 학습 (M6 진행 중 발견)

1. **codex 의 CSS 시각적 BLOCKING 가치 재확인** — T28 modal vs WebContentsView native overlay 는 evaluator 코드 grep 만으로 발견 불가 (M5-1 사례 재현). codex 의 layout 분석 필수.
2. **codex 의 정밀한 Unicode/emoji 검증 강점** — T28 emoji validator 의 FE0F/ZWJ 단독 허용 약점 + skin tone modifier base 인정 약점은 codex 만 발견. Extended_Pictographic regex 제안 적용.
3. **broadcast vs 폴링 타협 — broadcast 추가 권고는 valid** — T29 evaluator + codex 둘 다 PRD §07.4.2 broadcast 미구현 지적. 5초 폴링만으로는 contract 미달. 본 PR 내 broadcast 도입 (chat/note 2종) + 인덱싱 1종은 IndexingService wiring 미흡으로 KI-010 등록.
4. **시나리오 통합 테스트 의 결정성 보장** — T30 codex NB 5건 (정렬 결정성 + active ws + 빈 결과 회피 + fixed timestamp + cosine metric) 은 통합 테스트 견고성 권고. 본 PR 내 모두 해소.
5. **단위 테스트 카운트 미스카운트 패턴 잔존** — T28 evaluator NB-3 (32 → 실측 33 + +8 → +7) 가 본 hotfix 에서 정정. 추후 PR body 작성 시 npm test 실측 명령 출력 첨부 권고.
6. **G-013 §1 단계별 PR 정합 — 신규 도입 PR 의 단일 PR 패턴 허용** — WorkspaceService / MemoryService 모두 신규 도입이라 폐기 단계 없음. 단일 PR + IPC + UI 통합 정합.

### 11.8 Sprint 016 진입 권고 (다음 세션)

#### 11.8.1 Sprint 016 contract 시안 (본 PR §`.flowset/contracts/sprint-016.md` 작성)

핵심 작업:
- **M0 KI batch 처리** — KI-001 macOS sqlite-vec / KI-004 ChatRequest.response_format / KI-006 abort 정책 / KI-007 TabManager / KI-008 JSON Export / KI-010 인덱싱 broadcast / KI-011 < 20ms 측정
- **M1 시나리오 2·3 90%+ cover** — scenario-2-pm + scenario-3-learning 회귀 셋 +10 케이스
- **M2 어댑터 정리 (M5-8 분할 2편)** — ProviderAdapter.translate / executeTranslateRequest / TranslationCache / PageResultStore / fetchImpl 5종 일괄 제거
- **M3 Phase 2 진입 — cookies/storage partition** — WorkspacePartitionManager 신규 + workspace 별 session 격리
- **M4 백그라운드 번역 + 하이라이트** — 논문/PDF Codex OAuth + DOM anchor 노트 하이라이트

#### 11.8.2 즉시 행동 (다음 세션 첫 행동)

1. `.flowset/state.md` Phase 2 진입 메타 갱신
2. `.flowset/contracts/sprint-016.md` 본문 확장 (시안 → 정식)
3. M0 KI-001 + KI-006 batch 시작 (MEDIUM 우선)
4. ChatPanel / MemoryStatsPanel 실디스크 사용자 검증 1차 보고

### 11.9 본 세션 마무리

Sprint 015 M0~M6 모두 완료. **Phase 1 종료 선언**. PRD v0.4.0 정식 발행. 단위 358 → **1068 (+710)** (Sprint 015 전체). PR 누적 65. KI 누적 11 (HIGH 1 in-progress / MEDIUM 4 / LOW 6).

evaluator + codex 병렬 평가 표준 정착 — 본 Sprint 015 누계 15회 평가 모두 정합.

다음 세션 진입: Sprint 016 (Phase 2 진입 + KI batch + 어댑터 정리 + 시나리오 2·3 cover).

---

## 12. Sprint 015 잔여 hotfix (본 핸드오프 머지 후 후속 — `docs/WI-S016M0-docs-sprint-015-residual-hotfix`)

### 12.1 배경

PR #165 (T31) 머지 직전 사용자 "되돌려진게 머지과정에서 문제가있었던건 아니냐?" 의문 제기 + Sprint 015 종합 evaluator + codex 병렬 평가 결과 잔여 NB 3건 + WARN 2건 (총 5건) + 본 hotfix 시점 추가 발견 PARTIAL 4파일 회귀 매트릭스 누락 3건 = **총 8건 처리**. 4 파일 회귀 자체는 head working tree 가 main 브랜치로 전환된 시각적 착시로 종결 — PR #165 정상 squash 머지 (`a177496`).

### 12.2 처리 매트릭스

| # | 항목 | 종류 | 파일 |
|---|---|---|---|
| 1 | PRD §0.6.1 시나리오 2·3 "90%+" → "0% (Sprint 016 cover 예정)" 정정 | evaluator NB-1 | `docs/prd/00_change_history.md` L123 |
| 2 | Sprint 016 기간 "3~4주" ↔ "3~3.5주 (18~24일)" 통일 (§4 마일스톤 합산 정합) | codex WARN-1 | `.flowset/contracts/sprint-016.md` L7 |
| 3 | Phase 1 종료 적정성 메모 (형식적 종료 정합 / 실용 검증 Sprint 016 M0~M1 후 확정) | evaluator NB-2 | `.flowset/state.md` L8~L13 + handoff §11.4 |
| 4 | 정량 임계 5종 KI 신규 등록 (KI-012~KI-016) | evaluator NB-3 | `.flowset/known-issues.md` |
| 5 | tabLabel.test.ts +2 워크스페이스 컨텍스트 누락 KI 신규 (KI-017) | 본 hotfix 발견 | `.flowset/known-issues.md` |
| 6 | contract T02 회귀 매트릭스 강화 (tabGuard +3 abort + 위치 이동) | 본 hotfix 발견 | `.flowset/contracts/sprint-016.md` T02 |
| 7 | contract T03 회귀 매트릭스 강화 (TabManager +5 + tabLabel +2 KI-017 동반) | 본 hotfix 발견 | `.flowset/contracts/sprint-016.md` T03 |
| 8 | handoff §11.5 cover 표기 정정 + §11.5 PARTIAL 4파일 매트릭스 누락 메모 | 본 hotfix 발견 | `.flowset/handoffs/2026-05-19.md` §11.5 + §12 |

### 12.3 codex WARN-2 ("prebuilt 부재" 표현)

codex 검토는 KI-001 본문 + Sprint 016 contract §7 §1 "prebuilt 부재" 표현 부정확이라 지적했으나, 코드베이스 grep 검증 결과 두 위치 모두 이미 **"prebuilt 동작 미검증"** 표현. codex WARN 자체가 오인 — 본 hotfix 정정 불필요 (메모만 보존).

### 12.4 단위 테스트 영향

본 hotfix 는 **docs only 변경** (5 파일: state.md / handoff / KI / contract / PRD). 코드 변경 0 → 단위 1068 유지. `npm test` PASS 보존.

### 12.5 KI 누적 갱신

11 → **17** (HIGH 1 / MEDIUM 4 / LOW 6 → 12). KI-012~KI-016 5종은 Sprint 016 M0 perf bench 셋 (`tests/perf/{indexing,search}.bench.ts` 신규) 일괄 측정. KI-017 은 KI-007 (Sprint 016 T03) 동반 해소.

### 12.6 학습

1. **PARTIAL 회귀 매트릭스 일정 누락은 평가 게이트에서 자연 발견** — Sprint 015 M6 종합 evaluator 가 시나리오 1·4 cover 100% 만 측정 → §D PARTIAL 4파일 (TabManager / tabGuard / tabLabel) 일정 M6 미이행은 본 hotfix 다음 세션 첫 grep 으로 발견. **다음 Sprint 종료 evaluator 정의에 §D PARTIAL 매트릭스 일정 검사 추가 권고**.
2. **정량 임계 표 자체가 KI 트리거** — Sprint 016 contract §6 정량 임계 7종 표만 정의되고 측정 셋 미정의 → 임계 자체가 KI 후보. PRD 정량 임계 표 박힐 때 측정 셋 신규 task 동반 등록 권고.
3. **"4 파일 회귀" 시각적 착시 — 후속 PR working tree 검증 시 브랜치 명시 권고** — head working tree 가 main 브랜치 일 때 PR 브랜치 변경은 표시 안 됨. 검증 시 `git branch --show-current` + `gh pr view <#> --json mergeable,mergeStateStatus` 동반 권고.

### 12.7 본 hotfix PR 평가 정책

본 hotfix 도 docs only 변경이지만 **`feedback_dual_review` 표준 적용** — evaluator + codex 병렬 평가 강제. Sprint 014 M3 핸드오프 PR #152 학습 (§9.4 #3) 정합.
