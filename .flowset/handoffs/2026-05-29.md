# 2026-05-29 — Sprint 018 M2 T17e (임베딩 provider/dimension 격리 E2E) — T17 라인 종결

> 직전: 2026-05-28 (write-path wiring #262 + T17b/c/d). T17e 작업이 자정 넘겨 2026-05-29 진입.

## 메타

- **Sprint**: 018 M2 T17e (S018-T05 CO-1 종결). 진입: write-path wiring(#262/#263) 머지 후 PR 머지 경계에서 AskUserQuestion → 사용자 명시 선택 **"T17e E2E 격리 검증 (codex 순차 권고)"** (G-022 명시 선택 진입). codex 019e6e4d 순차상 write-path 다음 단계.
- 누적 main first-parent S018 PR: **19** (#246~#264) → 본 docs PR 머지 시 20.
- 단위 회귀: 1942 → **1947** (+5; 104 파일 = CI 정합 — 신규 t17e-embedding-isolation.test.ts).
- KI 변동 0 (잔여 9 유지). 자기 검증 루프 62 → **63**.
- carryover **5 → 4** (CO-1 T17 로컬 임베딩 통합 **완전 종결** — T17a~e + write-path wiring 전부 머지).

## 1. 배경 — T17 라인 종결

write-path wiring(#262)으로 query/write 양쪽이 provider-aware 가 됐다. T17e 는 그 end-to-end 가치를 통합 테스트로 증명 — v06 schema(vec_pages_1024/768)에서 워크스페이스별 embedding_model 격리가 write path(인덱싱 적재) + query path(검색) 양쪽에서 성립함. scope/단언은 codex 019e6ea0 협의(write-path 와 동일 thread).

## 2. codex scope 협의 (019e6ea0)

- **Q1 write path** = processNextEmbeddingJob 직접 반복 drain (worker scheduler 안 씀 — 이미 단위 테스트, 통합은 결정적·간결). buildEmbeddingClientForModel(ws.embedding_model, getProvider) 로 resolveEmbeddingClient.
- **Q2 query path** = handleSearchQuery (프로덕션 경로 — embedding_model 해소 + provider 선택 + EmbeddingClient dim 주입까지 E2E). SearchService 직접 호출은 부족.
- **Q3 단언 보강**: countPages 는 dim 합산이라 cross-dim 부적합 → searchPages per-dim. provider spy 는 dimensions+modelHint 까지. handleSearchQuery 결과 type/pageId/url/title. dimension mismatch → 'failed'(provider_unavailable 아님)+vector 0.
- **Q4** 신규 파일 (m4-pipeline 은 v05 applySchema 기반). scope = page only (vec_notes 분리는 VectorIndex unit 커버).

## 3. 산출물 (PR #264 — +325 / -0 / 1 파일)

| 파일 | 변경 | 내용 |
|---|---|---|
| `tests/integration/t17e-embedding-isolation.test.ts` | 신규 (+325) | provider/dimension 격리 E2E 5 테스트 |

5 테스트 (ws1=openai:text-embedding-3-small:1024 / ws2=ollama:nomic-embed-text:768):
1. **write path** — recordVisit→enqueue→processNextEmbeddingJob(buildEmbeddingClientForModel) → ws1 page = vec_pages_1024 에만 / ws2 = vec_pages_768 에만 (searchPages per-dim).
2. **provider 선택 격리** — spy: openai embed 전부 dimensions=1024+modelHint=text-embedding-3-small / ollama 전부 768+nomic-embed-text.
3. **query path E2E** — handleSearchQuery(provider 선택+dim 해소) → 워크스페이스별 자기 page 만 (type/pageId/url/title 확인 + provider spy 음성 조건).
4. **워크스페이스 격리** — 동일 content 양 ws 적재 후 ws1 검색에 ws2 안 섞임 (양방향).
5. **dimension 안전** — wrong dim 반환 → 'failed'(provider_unavailable 아님)+queue failed=1+vector 0.

### 핵심 결정
- fixed 벡터 fake provider (query=page 동일 → exact hit) — 검색 정확도가 아니라 **격리**를 검증하는 의도 설계. exact hit 보장 후 "올바른 ws/dim 의 page 인지"만 단언.
- drainUntilIdle maxIterations(=20) 가드 — regression 이 provider_unavailable 반복 시 runner timeout hang 대신 수집 status throw.

## 4. dual review (G-021 — 실 호출 증거)

- **evaluator** (`.claude/agents/evaluator.md`, agentId `a67b8734cc2b067d8`): **Pass 6 / Partial 0 / Fail 0** (AC1~AC6). 거짓 통과(behavior-blind) 우려를 **뮤테이션 테스트 2종**으로 반증 — (1) VectorIndex.searchPages WHERE workspace_id 무력화 → 테스트 fail (2) SearchService dimensions 고정 → 테스트 fail. 단언이 실 격리 로직(SQL workspace 필터 + dim 테이블 선택)에 mutation-sensitive 함을 실증.
- **codex** (raw MCP sandbox=read-only, threadId `019e6ea0-d9d8-7aa0-a84e-b88b8fc71509` — scope 협의 + 리뷰 동일 thread): round-1 **BLOCKING 0 / NEEDS_CHANGES 2 / NOTABLE 3** → 정정 → round-2 **0 / 0 / 1**.
  - NC(해소): (1) drainUntilIdle 무한루프 → maxIterations 가드 + 수집 status throw (2) ws2 query 후 openai.embedReqs length=1 단언 추가(ws2 가 openai 안 건드림 음성 조건).
- hotfix 흡수: NC 정정 = 동일 커밋 amend (push 전).

## 5. 검증

- lint PASS / typecheck(node+web) PASS / `npm test` 전체 **1947** PASS (**104 파일**) / build PASS / CI **6/6 SUCCESS** (typecheck+test+build 5m42s / 커밋 메시지 / FlowSet Policy Check / macOS·macOS Intel 11m43s·Windows 9m10s sqlite-vec PoC). verify-pr-body 로컬 사전 검증 후 PR.

## 6. 다음 진입점 (codex 019e6e4d 순차 — 본 docs PR 머지 후)

1. **T21** (S018-T07, M2, 독립 spec) — 워크스페이스 공유 설계 시안 (PRD §11 공유 path 정합).
2. **T22/T09 → PRD v0.5.0 / Phase 3 종료 / Sprint 019 시안** (S018-T08~T13, M3~M5). **T20 = Sprint 019 defer** (학습 데이터셋 부재).
- **MVP 의도 한계 (write-path wiring 코드 주석 명시, 후속 추적)**: (a) provider 미가용 고우선 잡 starvation (b) in-flight embed 비취소 (c) query path 인라인 체인 중복(G-013 의도적 비대칭).

⚠️ 다음 작업(T21) 은 **사용자 명시 선택** 후 진입 (G-022). 본 §6 박힘 ≠ 본 세션 진입 박음 권고.

본 세션 누적(T17e): PR #264 (feat) 머지 + 본 docs PR. codex thread 1개 (019e6ea0 — write-path 설계/리뷰 + T17e scope/리뷰 통합, T17e 리뷰 2라운드 → 0/0/1).

---

# 이어쓰기 4 — Sprint 018 M2 T21 (S018-T07) 워크스페이스 공유 설계 시안 (SharedWorkspaceFormat)

> 직전: T17e(#264) + state sync(#265) 머지로 T17 라인 종결. 본 이어쓰기는 다음 세션 진입 — 사용자 entry 발화 "핸드오프 읽고 작업 진행해" 후 T21 진입 (G-022 entry 해제).

## 7. 메타 (T21)

- **Sprint**: 018 M2 T21 (S018-T07, carryover CO-4). 진입: 사용자 명시 entry 발화 후 (G-022). codex 019e6e4d 순차상 T17 라인 종결 다음.
- 산출물: `.flowset/specs/v05-collab-spike.md` 신규 (+318/-0/1 파일, **spec only — 코드 변경 0**). PR **#266** 머지 `0bb01fa`.
- 누적 main first-parent S018 PR: **21** (#246~#266) → 본 state sync 머지 시 22.
- 단위 회귀 변동 0 (코드 미변경, 1947 유지). KI 변동 0 (잔여 9). carryover 4 → **3** (T21 종결).

## 8. 배경 — 공유 ≠ 자기 백업

기존 `WorkspaceExportImportService` JSON export/import 는 **자기 백업·복구**(KI-008/KI-022 closed). T21 은 **타 사용자/기기로 단방향 전달**하는 공유 포맷 설계 — 자기 백업엔 없던 위협: (1) authenticity/tamper-evidence (2) untrusted import (압축 폭탄/악성 payload/DoS) (3) 받는 사람 노출 메타 프라이버시. v04-direction §10.3 "공유=Phase 3=포맷+import/export" + 로드맵 §16.4.1 `SharedWorkspaceFormat`(Sprint 021 "압축·서명") 정합. 실시간 협업 = Phase 4 (contract §2/§8, scope 외).

## 9. codex 사전 설계 협의 (019e6fcf, read-only)

round-1 BLOCKING 2 / NEEDS_CHANGES 5 / NOTABLE 1:
- BLOCKING #1: 서명 입력 = payloadHash 만이 아닌 **domain-separated canonical signing object** {format,formatVersion,payload_sha256,schemaVersion,producedAt} + downgrade 정책(signed→unsigned 경고).
- BLOCKING #2: untrusted **deep schema/limits validator P0** (byte/count/length/depth cap + 구조 cap) — 기존 validatePayload(child cast 수준) 위에.
- NC: TOFU UX 문구 "신원 확인" 금지(4상태만) / installFingerprint 제거(public key 자체가 추적자) / payload_sha256 필드명 통일 / 노출 메타 카테고리 export preview / import 후 자동 embedding 외부 호출 privacy·cost.

## 10. 산출물 (PR #266 — +318/-0/1 파일)

`.flowset/specs/v05-collab-spike.md` 신규. 14 섹션(§1~§14): Purpose/Non-goals → 가드레일 제약 → Scope 경계(Phase 3↔4) → 산출물 책임 → envelope+canonicalization → 무결성/Ed25519 TOFU 서명 → Export+ExportPrivacyGate → **untrusted import 위협 모델(§8 + 의미 검증 §8.1.1)** → import 흐름 → round-trip → Sprint 021 분해(S021-a~k) → AC-1~6 → 참조 → 변경 이력.

핵심 결정:
- `.fbworkspace` = gzip(envelope) 단일 파일. envelope = {format, formatVersion, producedAt, producer, integrity.payload_sha256, signature?, payload:WorkspaceExportV1(filtered)}.
- 서명 = **신원 증명 아님** — Ed25519 self-signed per-install (private key=safeStorage/Keychain G-005), TOFU(trust-on-first-use) = tamper-evidence + 발신자 연속성만. local-first 무PKI 정직 한정.
- "공유 파일 = 공격자 입력" MUST — untrusted validator(byte/count/depth cap + enum/finite int/FK 무결성) fail-closed.
- 기존 `WorkspaceExportV1` v06 + `ExportPrivacyGate` filtered cascade(Notion spec §13) 재사용.

## 11. dual review (G-021 — 실 호출 증거)

- **evaluator** (agentId `ac2951b4aa4fb4e5b`): **Pass 5 / Partial 1 / Fail 0**. Partial 1 = §2 "실시간=Phase 4" 를 v04-direction §10.4 인용처럼 표기했으나 v04-direction 에 Phase 4 표현 부재(grep 0 자체 검증) → 실제 출처는 **contract §2/§8**. G-012/G-006 정합 결함 → 동일 커밋 정정.
- **codex** (raw MCP read-only, thread `019e6fcf` round-2): **BLOCKING 0 / NEEDS_CHANGES 3 / NOTABLE 3**. NC: (1) AC-2 서명 strip/re-sign 탐지 과대주장(strip=downgrade 경고/re-sign=pinned mismatch 한정) (2) duplicate id fail-closed reject(정규화 X) (3) 의미 검증(enum/finite int/FK) MUST 신설.
- hotfix 흡수: 7건(evaluator Partial 1 + codex NC 3 + NOTABLE 2 + evaluator 보강 1) 모두 **동일 커밋 `c51b68d`** (dual review 전 작성 시점 amend 아닌 작성 후 정정, push 전).

## 12. 검증 + CI

- 로컬 verify-pr-body PASS (G-018 +318/-0/1 정합 + G-021 evaluator/codex 카운트+UUID 인라인).
- CI **6/6 SUCCESS**: FlowSet Policy Check / 커밋 메시지 형식 / typecheck+test+build 5m47s / macOS·macOS Intel·Windows sqlite-vec PoC (코드 미변경 무영향).
- 머지 `0bb01fa` squash + 원격 브랜치 삭제(G-017).

## 13. 다음 진입점 (codex 019e6e4d 순차 — 본 state sync PR 머지 후)

1. **T22/T09** (S018-T08~T09, M3) — 단위 회귀 셋 + 신규 컴포넌트 cover(+80~120) + 시나리오 1·2·3·4 cover 90%+.
2. **PRD v0.5.0 + Phase 3 종료 검토 + Sprint 019 시안** (S018-T10~T13, M4~M5). **T20 = Sprint 019 defer**.
- **T21 구현**: Sprint 021 (S021-a~k = v05-collab-spike.md §11). G-013 단계별 — 설계 시안 머지됨 / 구현 별도.
- **drift 추적**: notion-export-spec 본문 일부 구버전 `schemaVersion: v05` (현 코드 v06) — T19 spec 별도 정정 후보.

⚠️ 다음 작업(T22/T09) 은 **사용자 명시 선택** 후 진입 (G-022). 본 §13 박힘 ≠ 본 세션 진입 박음 권고.

본 세션 누적(T21): PR #266 (docs/spec) 머지 + 본 state sync docs PR. codex thread 1개 (019e6fcf — 설계 협의 round-1 + 작성 spec 리뷰 round-2).

---

# 이어쓰기 5 — Sprint 018 M3 T22/T09 (S018-T08~T09) 임베딩 gap-fill + 시나리오 cover 검증

> 직전: T21(#266) + state sync(#267) 머지. 사용자 AskUserQuestion 명시 선택 "T22/T09 진입(M3)" 후 진입 (G-022).

## 14. 메타 (T22/T09)

- **Sprint**: 018 M3 T22(S018-T08, 단위 cover) + T09(S018-T09, 시나리오 cover). PR **#268** 머지 `ff55dd5`.
- 산출물: `src/ai/embedding/EmbeddingClient.ts` +7/-1 + `tests/unit/ai/EmbeddingClient.test.ts` +88 (총 +95/-1/2 파일). 단위 회귀 1947 → **1950** (+3).
- 누적 main first-parent S018 PR: **23** (#246~#268) → 본 state sync 머지 시 24. carryover 3 → **2** (T22/T09 종결).

## 15. 핵심 — 정직한 scope (codex 019e7031)

T22/T09 진입 시 실측: AC-4 수치 목표(누적 ≥1840, Sprint 018 +80~120)가 **T17 라인으로 이미 초과 충족**(contract §0 시작 1760 → 1947 = +187 > +120). codex 협의 결론: 숫자 패딩 PR 금지 — **진짜 미커버 공백만 fill + 시나리오 cover 형식 증명**. (codex BLOCKING 0, scope framing NEEDS_CHANGES = "신규 테스트 목표 달성"이 아닌 "검증 + gap-fill + 형식 증명"으로 기술.)

## 16. 산출물 (PR #268)

코드 fix (embedPage 대칭 비대칭 해소):
- `EmbeddingClient.embedNote` 빈-source 가드. `NoteStore.create`(:109)가 빈 문자열만 막고 whitespace(`'   '`)는 통과 → `selected_text='   '`+`body=null` 이면 빈 문자열 provider 전송하던 결함. `parts.join('\n\n').trim()` 후 `if (!source) throw` (embedPage 와 동일). codex NOTABLE — NoteService(:94)/import(:489) 이미 trim 가드라 정상 UI 경로 영향 없는 **last-line defense**.

회귀 3건 (전부 진짜 미커버 분기, mutation-sensitive):
1. embedNote whitespace-only → throw + provider 미호출 단언.
2. note orphan → markFailed(orphan) — page orphan(:513)만 있던 비대칭 해소.
3. resolveEmbeddingClient 일반 Error → failed(markFailed) + `pending=0`로 release/provider_unavailable 배제 — 잘못된 embedding_model = 영구 실패, release 무한루프 금지. provider_unavailable 경로(:608)와 반대 분기 상보.

T09 시나리오 cover 검증 (신규 테스트 불요, 기존 유지 확인):
- 케이스 수 S1=5/S2=5/S3=5/S4=3 = PRD §18.4.1 (전부 100% 회귀) + §16.5 value cover S1 100%/S2 90%/S3 90%/S4 100% = **Phase 1 평균 95.0%** ≥ 90% (PRD §16.5 line 137 "Phase 1 평균 95%"). 회귀 0. **drift 주의**: #268 PR body/commit 은 "평균 97.5%" 로 표기했으나 97.5% 는 §16.5 **Phase 2 열** 평균이고, 인용한 per-scenario 값(100/90/90/100)은 Phase 1 열 → 정확 평균 95.0%. ≥90% 게이트는 양쪽 모두 불변 (drift 추적 등록).
- code coverage 도구(@vitest/coverage-v8) 미설치 + vitest.config coverage disabled → 매트릭스 cover 로 형식 증명, code coverage % 미주장 (codex 정합).

## 17. dual review (G-021 — 실 호출 증거)

- **evaluator** (agentId `af5dd6427c05774e7`): **Pass 6 / Partial 0 / Fail 0**. **mutation testing 3종 변이** 실측 — (M1) 가드 제거 → whitespace 테스트 FAIL (M2) markFailed 제거 → orphan 테스트 FAIL (M3) instanceof 분기 제거 → 일반-Error 테스트만 FAIL + 기존 provider_unavailable(:696) PASS 유지(양방향 구분 입증). 워킹트리 무오염 복원 확인. 거짓 통과 차단 입증.
- **codex** (raw MCP read-only, thread `019e7031` round-2): **BLOCKING 0 / NEEDS_CHANGES 0 / NOTABLE 3** (가드 동작보존 + 3테스트 mutation-sensitive[pending=0 release 배제력] + NoteService/import 이미 trim 가드라 last-line defense).
- hotfix 흡수: 없음 (NEEDS_CHANGES 0).

## 18. 검증 + CI

- lint PASS / typecheck(node+web) PASS / `npm test` 1947→**1950** (104 파일) / build PASS (53 modules 283ms). 로컬 verify-pr-body PASS.
- CI **6/6 SUCCESS**: FlowSet Policy Check / 커밋 메시지 형식 / typecheck+test+build 5m39s / macOS·macOS Intel·Windows sqlite-vec PoC. 머지 `ff55dd5` squash + 원격 브랜치 삭제(G-017).

## 19. 다음 진입점 (codex 019e6e4d 순차 — 본 state sync PR 머지 후)

1. **PRD v0.5.0 발행** (S018-T10, M4) + **Phase 3 종료 검토** (S018-T11 — `phase3-exit-checklist.md`) — Phase 3 진입 + 로컬 LLM + Notion Export + Schema v06 메타.
2. **Sprint 018 종합 + Sprint 019 시안** (S018-T12~T13, M5). **T20 = Sprint 019 defer**.
- **drift 추적 (유지)**: notion-export-spec 본문 구버전 `schemaVersion: v05` (현 v06) — T19 spec 별도 정정 후보.

⚠️ 다음 작업(PRD v0.5.0) 은 **사용자 명시 선택** 후 진입 (G-022). 본 §19 박힘 ≠ 본 세션 진입 박음 권고.

본 세션 누적(T22/T09): PR #268 (fix) 머지 + 본 state sync docs PR. codex thread 1개 (019e7031 — scope 협의 round-1 + diff 리뷰 round-2). 본 세션 전체: T21(#266/#267) + T22/T09(#268/sync) = 코드/spec PR 2 + sync PR 2, codex thread 2개(019e6fcf, 019e7031).

---

# 이어쓰기 6 — Sprint 018 M4 (T10 PRD v0.5.0 발행 + T11 Phase 3 종료 검토) — **M4 종료**

> 직전: T22/T09(#268) + state sync(#269) 머지. 사용자 entry 발화 "핸드오프 읽고 작업 진행해" 후 M4 진입 (G-022 해제). codex 019e6e4d 순차 우선순위 마지막 단계.

## 20. 메타 (M4)

- **Sprint**: 018 M4 (S018-T10 PRD v0.5.0 + S018-T11 Phase 3 종료 검토). PR **#270**(`1e864a8` T10) + **#271**(`d49bca0` T11) 둘 다 머지. carryover 2 → **1** (M5 = T12 종합 + T13 Sprint 019 시안 잔여).
- 누적 main first-parent S018 PR: **26** (#246~#271 실측). 단위 회귀 **1950 PASS (104 파일)** — docs/spec only 무변동.
- codex thread 1개 (`019e718f-0901-7443-989a-320f2011b111`) — scope 협의(Q1~Q4) + T10 리뷰 4라운드 + T11 리뷰 2라운드 통합.

## 21. T10 — PRD v0.5.0 정식 발행 (Phase 3 진입) — PR #270

### 21.1 핵심 — scope (codex 019e718f Q1~Q2)

contract 의 "신규 §13 로컬 LLM + §11.6 Notion Export" 는 **플래닝 시점 placeholder, 실제 PRD 구조와 불일치** (§13=보안 / §12.8=로컬LLM / §11.5.6=Notion / §11.6=사용자수준). codex BLOCKING — **신규 섹션 생성 금지, 기존 섹션에 shipped drift 정정**. scope = "메타 bump + shipped drift 정정 + roadmap 재정렬", **구현 선반영 금지** (Notion/공유/LocalLLM Sprint 020+ 상세 본문 박지 않음).

### 21.2 산출물 (8 파일 +181/-72)

- `00_change_history` §0.8 v0.5.0 + 발행 메타 (단위 1950 / KI 31등록·23closed·잔여8 / 시나리오 Phase1 95% / first-parent 267) + 주요 변경 6항
- `README` v0.4→v0.5 + 메타 이력
- `04_data_model` Schema v06 — `embedding_model` 컬럼 + `vec_pages_{1024,768}`/`vec_notes_{1024,768}` + allowlist/DB-level enforcement 한계 + 단수 vec 정정 + umbrella note
- `08_indexing` provider-aware embedding (OpenAI 1024 BYOK / Ollama 768 로컬, resolve-then-embed)
- `12_provider_adapter` `OllamaProvider` chat✅(S017 T14)/embed✅(S018 T17c)/chatStream✗ defer + 명칭 수렴 + Phase3 종료 검증 미완
- `06_architecture` `LocalLLMProvider`/`LocalEmbeddingProvider` → 단일 `OllamaProvider` 명칭 수렴 (박스/트리/모듈목록 + Export 분리[JSON/MD ✅ 기존 서비스 / Notion=ExportArtifactBuilder planned])
- `11_workspace` §11.5.6 Export 상태(JSON/MD ✅ / Notion 📝 Sprint020) + §11.12 SharedWorkspaceFormat 신규 소절(Sprint021)
- `16_roadmap` Phase 3 진행 실측 + Sprint 매핑(S017~018 선행) + Phase 3 종료 미충족 명시

### 21.3 G-006 검증 (실 코드 대조 — codex 추측 차단)

- `OllamaProvider.chat()`+`embed()` 실재 + `supportsChat/supportsEmbed=true` + chatStream 부재 (`src/ai/providers/OllamaProvider.ts`)
- `V06_SCHEMA_VERSION=3` + workspaces.embedding_model + vec_pages_1024/768 (`Database.ts`/`v06.sql`)
- `providers.set('local', new OllamaProvider())` (`services.ts`) + `defaultProviderId='local'` fallback 금지 (`chatProviderSelect.ts`)
- WorkspaceExportV1 = workspace(embedding_model)+pages+visits+notes+aiChatHistory+tags+pageTags+noteTags+highlights, schemaVersion 'v06'

### 21.4 dual review (G-021)

- **evaluator** (`ad805572ce75bfd49`): **Pass 3/0/0** — 7개 사실 주장 mutation-style 코드 대조, 거짓 0. 경미 2 = §통계 off-by-one(T12) / phase3-exit-checklist 미생성(T11) 둘 다 PRD 외부.
- **codex** (`019e718f` round-1~4): BLOCKING 1(KI severity HIGH0/MEDIUM0/LOW9 오류 — 실 MEDIUM3/LOW5) + NC 4(§12.1.3 stale / §04 단수 vec / §11.5.6 entity 목록 / §06 명칭) → 정정 → **BLOCKING 0 / NEEDS_CHANGES 0**.
- **핵심 학습**: KI 잔여 severity 를 state.md/contract("잔여 9") 믿고 "MEDIUM 0/LOW 9" 박았다가 codex BLOCKING → **known-issues.md 헤더 status 실측** = 잔여 8 (HIGH 0/MEDIUM 3/LOW 5). §통계 "총 잔여 9" 자체가 Phase 1 open LOW off-by-one. G-006 — state.md 인용도 실 파일 재대조 필수.

### 21.5 검증 + CI

lint/typecheck(node+web)/build(53 modules) PASS / `npm test` **1950 PASS (104)** / 로컬 verify-pr-body PASS. CI **6/6 SUCCESS**. 머지 `1e864a8` (#270) squash.

## 22. T11 — Phase 3 종료 검토 체크리스트 — PR #271

- 산출물: `.flowset/specs/phase3-exit-checklist.md` 신규 (+80). #270 위 rebase.
- **결론: MVP 아직 아님** — PRD §16.4.3 임계 5영역 **완전충족 0/5**: ❌ 시나리오 100%(현 95%) / ❌ 1주 실사용 / ⚠️ 오프라인 E2E 검증(wiring O/검증 X) / ⚠️ Export 3종 round-trip(JSON·MD ✅ / Notion 미구현) / ⚠️ 로컬 LLM 정량(<2초 미측정).
- **wiring ≠ exit 검증 구분** (G-006) — codex Q4 권고 정합. `chatStream` = 비필수(§16.4.3 offline = 검색+채팅+인덱싱, streaming 불요) 분리 (codex round-1 NC).
- 잔여 backlog: S019(로컬LLM 오프라인 검증+정량+UserLevelEstimator 실 학습) / S020(Notion 구현) / S021(공유 구현) / S022(MVP 최종 딥검증).
- dual review: evaluator **Pass 8/0/0** (`af77b14d72369719a`) + codex `019e718f` round-1 NC 1(chatStream) → round-2 **0/0**. 머지 `d49bca0` (#271).

## 23. 설정 (사용자 요청)

`.claude/settings.local.json` (gitignore, 개인) broad allow 정리 — `mcp__codex__codex`+`codex-reply`+`Skill(codex:rescue)`+`PowerShell(*)`+`Bash(*)`. **원인**: 기존 PowerShell 엔트리가 특정 prefix만 매칭(일회성 명령 누적) → 새 명령마다 재프롬프트. **안전망**: PreToolUse hook 이 Bash|PowerShell 모두 가로채 main push/.flowset 삭제/G-022 를 exit 2 로 막아 broad 허용에도 critical 가드레일 보존. codex MCP 툴 권한은 원래도 있었음(잔존 프롬프트 시 MCP 서버 연결 승인 = permissions 무관).

## 24. closeout 상태 (G-022 게이트)

사용자 "세션종료하고 다음 세션에서 진행하자" = 마무리 의도 → **G-022 PreToolUse 게이트 활성** (`git checkout -b` 차단 실측). 사용자 선택 = "종료"(PR 생성 아님). 따라서:
- 본 state.md + handoff 이어쓰기 6 = **closeout 편집만** (hook 허용), **미커밋**.
- 다음 세션 entry 발화 후 → 본 closeout 을 `WI-S018M4-docs` state/handoff 동기화 docs PR 로 박음 (dual review G-021) → 이어 M5 (T12 종합[§통계 9→8 정정 포함] + T13 Sprint 019 시안).

## 25. 다음 진입점 (M5 — 사용자 명시 선택 후)

1. **closeout 커밋·PR** (state.md + 본 handoff) — entry 발화 후 최우선.
2. **M5 T12** — Sprint 018 종합 evaluator + state/known-issues 갱신 (§통계 off-by-one 9→8 정정).
3. **M5 T13** — Sprint 019 contract 시안 (T20 학습 데이터셋 defer).

⚠️ M5 진입 = 사용자 명시 선택 후 (G-022). 본 §25 박힘 ≠ 진입 박음 권고.

본 세션 누적(M4): PR #270(T10 PRD v0.5.0) + #271(T11 Phase 3 체크리스트) 머지 + 설정 1건 + 본 closeout(미커밋). codex thread 1개 (019e718f — Q1~Q4 scope + T10 4라운드 + T11 2라운드).
